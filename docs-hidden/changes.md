# Alterações recentes

- 2026-05-12: Frontend/Recurrences — a modal de nova recorrência passou a exibir feedback visual para campos obrigatórios que antes ficavam silenciosos: `postingMode`, `dayOfMonth`, `monthOfYear`, `endOccurrences`, `endDate`, `fromAccountId` e `toAccountId`. O schema de `postingMode` foi ajustado para não abortar as demais validações quando o placeholder fica vazio, permitindo mostrar todos os erros no mesmo submit. Arquivos: `opa-finance-front/src/schemas/recurrence.schema.ts`, `opa-finance-front/src/features/recurrences/model/recurrences.helpers.ts`, `opa-finance-front/src/features/recurrences/components/recurrence-form-modal.tsx`, `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx -t "deve exibir erros"`, `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx -t "dia do mês"`, `npm run build`.
- 2026-05-12: Frontend/Recurrences — a modal de nova recorrência agora preenche automaticamente `dayOfWeek` com base em `startDate` no valor inicial do formulário, mantendo a sincronização quando a data muda depois de aberta. Arquivos: `opa-finance-front/src/features/recurrences/model/recurrences.helpers.ts`, `opa-finance-front/src/features/recurrences/components/recurrence-form-modal.tsx`, `opa-finance-front/test/unit/features/recurrences/model/recurrences.helpers.test.ts`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npx vitest run test/unit/features/recurrences/model/recurrences.helpers.test.ts`, `npm run build`.
- 2026-05-12: Backend/Recurrences — REC-REV-020 diferencia por status a rejeição de `POST /recurrences/:id/anticipate` quando a data já possui ocorrência persistida, retornando `409` para `materialized/pending_review` e `422` para `skipped/failed` com orientação de recuperação. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence-occurrence.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `opa-finance-api/docs/modules/modules_recurrences.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "anticipate"`, `npm run build`.
- 2026-05-12: Backend/Recurrences — REC-REV-019 refatora o fluxo `editByScope this_and_next` em `recurrence-edit.service.ts`, extraindo métodos privados para re-select/validação, ajuste de `endOccurrences`, fechamento da regra antiga, criação da sucessora e migração de overrides, sem mudança comportamental. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence-edit.service.ts`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "this_and_next"`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts`, `npm run build`.
- 2026-05-12: Backend/Recurrences — REC-REV-018 simplifica `resolveTimelineLabel` removendo um branch redundante que retornava a mesma label padrão, sem alterar comportamento runtime. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence-timeline.service.ts`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "timeline|projection"`, `npm run build`.
- 2026-05-11: Backend/Recurrences — REC-REV-017 diferencia as mensagens de rejeição de override em data já persistida conforme o status da ocorrência, orientando o usuário para a ação correta em cada caso. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence-override.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "override"`, `npm run build`.
- 2026-05-11: Backend/Recurrences — REC-REV-016 confirma e ancora que a paginação `desc` de timeline para recorrências `never` já usa `paginationTotal` do horizonte operacional; removido comentário desatualizado e adicionado teste de regressão para o comportamento. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence-timeline.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "timeline|desc|never"`.
- 2026-05-11: Backend+Frontend/Recurrences — REC-REV-014 documenta a duplicação consciente de `addOneYearIsoDate` entre os dois workspaces e ancora a paridade com testes unitários simétricos para datas de borda. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence.helpers.ts`, `opa-finance-api/test/unit/recurrence-helpers.unit.test.ts`, `opa-finance-front/src/features/recurrences/model/recurrences.helpers.ts`, `opa-finance-front/test/unit/features/recurrences/model/recurrences.helpers.test.ts`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npx vitest run test/unit/recurrence-helpers.unit.test.ts`, `npm run build`, `npm run lint`, `npx vitest run test/unit/features/recurrences/model/recurrences.helpers.test.ts`, `npm run build`.
- 2026-05-11: Backend/Recurrences — REC-REV-013 documenta no código os formatos aceitos por `recurrenceForecastAccountIdsSchema` (`undefined/null/vazio`, CSV e array com CSV aninhado), preservando a intenção dos consumers atuais do frontend. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence.schemas.ts`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npm run build`.
- 2026-05-11: Backend/Recurrences — REC-REV-012 torna `getNowIsoDateInTimezone` resiliente e observável: a resolução da data atual agora segue SQL -> Intl -> UTC, com logs de warn distintos quando a query do banco falha/retorna inválido ou quando o fallback por Intl também falha. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence.validators.ts`, `opa-finance-api/src/modules/recurrences/recurrence.service.ts`, `opa-finance-api/test/unit/recurrence-validators-now.unit.test.ts`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "timezone|materialize|forecast"`, `npm run build`.
- 2026-05-11: Backend/Recurrences — REC-REV-011 documenta a semântica não retroativa da troca de `postingMode` e adiciona teste de regressão cobrindo que ocorrências já persistidas mantêm o status original após tentativa de troca global bloqueada. Arquivos: `opa-finance-api/docs/modules/modules_recurrences.md`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "postingMode|retroage|retroativo"`, `npm run build`.
- 2026-05-11: Tests/Recurrences — reduzida a superfície de falha do caso `retorna mensagens em pt-BR para erros críticos de confirm e update`, que passou a montar apenas conta e categoria realmente usadas no cenário, sem depender de `createBaseContext` completo. Arquivos: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`. Validação: `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "retorna mensagens em pt-BR para erros críticos de confirm e update"`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "endDate|update|edit-scope|término"`.
- 2026-05-11: Backend/Recurrences — REC-REV-010 documenta e ancora com testes a validação cruzada `endDate < startDate` em update de recorrências. O schema continua cobrindo o caso com ambos os campos no payload e o service mantém a cobertura do caso parcial com apenas `endDate`, agora com comentários explícitos para preservar a defesa em profundidade. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence.schemas.ts`, `opa-finance-api/src/modules/recurrences/recurrence-edit.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `opa-finance-api/test/unit/recurrences-edit-scope-schema.unit.test.ts`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "endDate|update|edit-scope|término"`, `npm run build`.
- 2026-05-11: Backend/Recurrences — REC-REV-009 centraliza a rejeição de `originType` no parser de update com `z.never({ message })`, removendo a validação tardia via `superRefine` e tornando a imutabilidade do tipo de origem explícita no schema. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence.schemas.ts`, `opa-finance-api/test/unit/recurrences-edit-scope-schema.unit.test.ts`, `opa-finance-api/docs/modules/modules_recurrences.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "originType|update|edit-scope"`, `npm run build`.
- 2026-05-11: Backend/Recurrences — ajustada a ordem de validação na atualização de recorrências para priorizar o bloqueio específico de `review_required -> automatic` quando há pendências abertas, antes do bloqueio estrutural genérico por consumo. Também foram atualizados cenários legados de `critical-rules` que usavam recorrências `automatic` com datas históricas e acabavam materializadas já no `POST /recurrences`, invalidando o setup original. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence-edit.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run build`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "garante idempotencia na materializacao|bloqueia single em ocorrência projetada e orienta uso de override|retorna 409 em conflito otimista de versao|bloqueia review_required para automatic quando ha pendencia aberta|altera postingMode em recorrencia ativa nos dois sentidos sem pendencias abertas"`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts`.
- 2026-05-10: Backend/Recurrences — REC-REV-008 unifica a definição de ocorrência consumida no módulo com a constante `CONSUMED_OCCURRENCE_STATUSES` (`materialized`, `pending_review`, `skipped`, `failed`). O status `failed` passa a contar também para `by_occurrences`, alinhando materialize, anticipate, forecast, timeline e this_and_next com o bloqueio estrutural já documentado. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence.helpers.ts`, `opa-finance-api/src/modules/recurrences/recurrence-crud.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence.validators.ts`, `opa-finance-api/src/modules/recurrences/recurrence-timeline.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence-forecast.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence-edit.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence-occurrence.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence-materialize.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `opa-finance-api/docs/modules/modules_recurrences.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts`, `npx vitest run test/integration/recurrences/recurrences-forecast.test.ts`, `npm run build`, grep final por arrays inline e nome antigo da constante.
- 2026-05-10: Backend/Recurrences — REC-REV-007 corrigido na timeline para refletir edições de valor em transferências materializadas. O serviço agora carrega a perna `expense` por `transferId` em batch e a usa como fonte de verdade do `amount`, mantendo fallback para `reviewPayload` e regra-mãe quando a perna não existir. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence-timeline.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `opa-finance-api/docs/modules/modules_recurrences.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "timeline|transferência|transferencia"`, `npm run build`.
- 2026-05-10: Tests/Recurrences — estabilizado o cenário de forecast para ano passado em `recurrences-forecast.test.ts`. O teste agora usa recorrência `review_required` em vez de `automatic`, evitando materialização imediata histórica no `POST /recurrences` e preservando a intenção original de validar `projectionStartDate = null` com totais apenas reais. Arquivos: `opa-finance-api/test/integration/recurrences/recurrences-forecast.test.ts`, `docs-hidden/changes.md`. Validação: `npx vitest run test/integration/recurrences/recurrences-forecast.test.ts -t "não projeta para ano passado"`, `npx vitest run test/integration/recurrences/recurrences-forecast.test.ts`.
- 2026-05-10: Backend/Recurrences — REC-REV-006 corrigido no forecast para aplicar overrides pontuais em projeções futuras. O serviço agora carrega overrides em batch por recorrência/data e usa `override.amount ?? recurrence.amount` no loop de projeção, alinhando forecast com timeline e materialização. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence-forecast.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-forecast.test.ts`, `opa-finance-api/docs/modules/modules_recurrences.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-forecast.test.ts`, `npm run build`.
- 2026-05-10: Frontend/Recurrences — REC-REV-005 corrigido nos helpers de payload do formulário: `toRecurrenceCreatePayload` e `toRecurrenceUpdatePayload` agora falham rápido com erro em pt-BR quando IDs obrigatórios de conta/categoria chegam vazios, em vez de propagar `""` para a API. Arquivos: `opa-finance-front/src/features/recurrences/model/recurrences.helpers.ts`, `opa-finance-front/test/unit/features/recurrences/model/recurrences.helpers.test.ts`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npx vitest run test/unit/features/recurrences/model/recurrences.helpers.test.ts`, `npm run build`.
- 2026-05-10: Backend/Recurrences — REC-REV-004 corrigido em `anticipate`: o fluxo agora consulta override pontual da data antes de montar o `reviewPayload`, aplica precedência `input > override > regra` para `amount`, `description` e `notes`, e consome o override dentro da mesma transação após materializar a ocorrência. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence-occurrence.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `opa-finance-api/docs/modules/modules_recurrences.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "override|anticipate"`, `npm run build`.
- 2026-05-10: Backend/Recurrences — REC-REV-003 corrigido em `validateConfirmDate`: `confirm` deixou de usar `today + 1 ano` como teto genérico e agora respeita `endDate` para `until_date`, horizonte operacional baseado em `startDate` para `never` e ausência de teto por data para `by_occurrences`. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence-occurrence.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `opa-finance-api/docs/modules/modules_recurrences.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "confirm|range|horizonte|by_occurrences"`, `npm run build`.
- 2026-05-10: Backend/Recurrences — REC-REV-002 corrigido no fluxo `this_and_next` para regras `by_occurrences`: a nova regra agora desconta as ocorrências já consumidas (`materialized`, `pending_review`, `skipped`) da regra original quando o usuário preserva o mesmo término sem enviar `endOccurrences`, e retorna `422` quando não restam ocorrências para abrir uma nova regra futura. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence-edit.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `opa-finance-api/docs/modules/modules_recurrences.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "this_and_next|by_occurrences|consumid"`, `npm run build`.
- 2026-05-10: Tests/Recurrences — ajustados testes de integração obsoletos após a investigação do REC-REV-001. Asserções de `timeline.summary.appliedLimit` agora refletem o default real do endpoint (`12`) e os cenários de recorrência `automatic` passaram a usar datas futuras fixas para não depender da materialização imediata executada no `POST /recurrences`. Arquivos: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`. Validação: `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "materializa recorrência automatic sem regressão|retorna timeline com persistidas, projetadas, sequência e ações|retorna timeline com persistidas e projetadas em recorrência automatic|conta corretamente pendências e projeções na timeline de recorrência review_required"`, `npm run lint`, `npm run test:unit`, `npm run build`.
- 2026-05-10: Backend/Recurrences — REC-REV-001 corrigido para bloquear `edit-scope single` em ocorrência projetada sem registro persistido; o fallback em `applySingleScopeEdit` não cai mais em update global e agora retorna `422` orientando uso de `PUT /recurrences/:id/occurrences/override`. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence-edit.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `opa-finance-api/docs/modules/modules_recurrences.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "single|projetada|projeção"`, `npm run build`.
- 2026-05-10: Frontend/Recurrences — ajustado o fluxo de edição de ocorrência `projected` na timeline para expor também `Esta e próximas`. O modal `Editar ocorrência` agora mostra o seletor de escopo tanto para `pending_review` quanto para `projected`, mantendo `Somente esta` como default e explicando explicitamente que só `Valor`, `Descrição` e `Observações` são propagados naquele fluxo, sem sugerir alteração de datas. No submit, `projected + single` continua usando override pontual, enquanto `projected + this_and_next` passa a usar `PUT /recurrences/:id/edit-scope`, preservando o split de regra futura como caminho correto para alterações das próximas ocorrências. Testes de integração cobrem a abertura do seletor e o payload `this_and_next` para linha projetada. Validação: `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx -t "projetada|this_and_next|Somente esta"` passou; `npm run lint` sem erros (warnings pré-existentes de import/order).
- 2026-05-08: Frontend/Recurrences — reforçado bloqueio estrutural local do modal de edição global após confirmação/antecipação de ocorrência. A página agora marca a recorrência como consumida imediatamente no client (`onConfirmed`) e usa esse estado como fallback ao abrir detalhes/edição, bloqueando campos estruturais e `Valor` mesmo se `GET /recurrences/:id` ainda responder stale logo após a confirmação. Teste de integração cobre o fluxo confirmar -> editar sem reload e com resposta stale. Validação: `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx -t "confirmar ocorrência mesmo com GET stale|consumida|bloqueio"` passou; `npm run build` passou; `npm run lint` sem erros (warnings pré-existentes de import/order).
- 2026-05-07: Backend+Frontend/Recurrences — implementado bloqueio estrutural da regra-mãe após consumo de qualquer ocorrência. Backend: nova validação central em `recurrence-edit.service.ts` usando `validators.hasConsumedOccurrences(recurrenceId)` para rejeitar com `422` qualquer edição global estrutural (`PUT /recurrences/:id` e `edit-scope` `scope=all`) quando já existir ocorrência `materialized`, `pending_review`, `skipped` ou `failed`; após esse ponto, apenas `description`, `notes` e `expectedVersion` são aceitos. Decisão documentada: `failed` também conta para o bloqueio estrutural por já representar tentativa de execução. Contrato: `GET /recurrences`, `GET /recurrences/:id` e `GET /recurrences/:id/timeline` agora expõem `hasConsumedOccurrences`. Frontend: modal de edição global mostra aviso contextual, desabilita campos estruturais e `Valor`, mantém editáveis apenas `Descrição`/`Observações` e restringe o payload salvo a esses campos; mudanças futuras seguem por `Esta e próximas`. Testes focados adicionados no backend e frontend. Validação: backend `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "estrutura|consumida|this_and_next"` passou, `npm run build` passou, `npm run lint` sem erros (warnings pré-existentes em testes de account); frontend `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx -t "consumida|estrutura|bloqueio"` passou, `npm run build` passou, `npm run lint` sem erros (warnings pré-existentes de import/order).
- 2026-05-07: Frontend/Recurrences — adicionadas mensagens de escopo no modal `Editar ocorrência`. Para ocorrência projetada sem seletor, a UI informa que a edição será aplicada somente naquela ocorrência selecionada. Para pendência com escopo selecionável, a mensagem diferencia `Somente esta` de `Esta e próximas`, deixando claro quando a alteração passa a valer da linha escolhida para frente. Testes de integração cobrem as mensagens. Validação: `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx -t "edição contextual|projetada|this_and_next"` passou; `npm run lint` sem erros (warnings pré-existentes de import/order); `npm run build` passou.
- 2026-05-07: Frontend/Recurrences — modal `Confirmar lançamento` passou a exibir conta, categoria, subcategoria e contas de transferência como campos somente leitura, preenchidos a partir da regra/pendência, mantendo os IDs no formulário apenas para envio do payload. Isso impede alteração de vínculo da regra-mãe no momento de confirmar uma ocorrência e corrige o estado visual "Selecione" quando a ocorrência projetada vinha da timeline. Teste de integração reforçado para override projetado confirmar com valor ajustado e vínculos readonly. Validação: `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx -t "override|Confirmar lançamento"` passou; `npm run lint` sem erros (warnings pré-existentes de import/order); `npm run build` passou.
- 2026-05-07: Frontend/Recurrences — corrigido preenchimento dos modais após override em ocorrência projetada. Ao reabrir "Editar ocorrência" ou "Confirmar lançamento" em uma linha `projected` com `hasOverride`, os formulários agora usam o `amount` efetivo vindo da timeline (`item.amount`) em vez do `amount` da regra-mãe. Adicionados testes de regressão para edição e confirmação com valor ajustado. Validação: `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx -t "override"` passou; `npm run lint` sem erros (warnings pré-existentes de import/order); `npm run build` passou.
- 2026-05-07: Backend+Frontend/Recurrences — implementados overrides pontuais para ocorrências projetadas. Backend: nova tabela `recurrence_occurrence_overrides` com migration `0026_recurrence_occurrence_overrides.sql`, endpoints `PUT /recurrences/:id/occurrences/override` e `DELETE /recurrences/:id/occurrences/override/:date`, service dedicado com validação de ownership/data/série, timeline com `hasOverride`, materialização consumindo override e removendo o registro, migração de overrides em `this_and_next` e limpeza no `skip`. Frontend: `RecurrenceTimelineItem.hasOverride`, mutations de override, edição de linha `projected` usando override sem seletor de escopo, badge `Ajustada` na timeline e testes pontuais. Documentação de recorrências atualizada. Validação: migrations dev/test aplicadas; backend `npm run lint` (somente warnings pré-existentes em testes de conta), `npm run build`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t "override"`; frontend `npm run lint` (somente warnings pré-existentes de import/order), `npm run build`, `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx -t "override|projetada"`.
- 2026-05-07: Frontend/Recurrences — corrigido escopo de edição por ocorrência na timeline: ocorrências `projected` não possuem registro individual no banco, fazendo o backend cair em `update()` global quando `single` era enviado. Fix: para `projected` o formulário exibe apenas `Esta e próximas` com esse valor como default; para `pending_review` mantém `Somente esta` (default) e `Esta e próximas`. Adicionado prop `occurrenceEditStatus` no `RecurrenceFormModal` e estado `occurrenceEditStatus` na page. Teste adicional cobre o comportamento de `projected`. Arquivos: `recurrence-form-modal.tsx`, `recurrences-page.tsx`, `recurrences.integration.test.tsx`. Validação: 26 testes passando, build ✓.
- 2026-05-07: Frontend/Recurrences — fluxo de edição contextual de ocorrência a partir da timeline do modal de detalhes. Botão "Editar" por linha para ocorrências `projected` e `pending_review` em recorrências ativas. Modal de edição reutiliza `RecurrenceFormModal` com novo prop `isOccurrenceEdit`: exibe apenas valor, descrição e observações; seletor de escopo restrito a `Somente esta` (default) e `Esta e próximas`; oculta data da ocorrência (pré-preenchida). Edição global pelo topo não mostra mais "Aplicar edição em". Payload envia apenas `{ amount, description, notes }` via `editByScopeSchema`. Adicionado `toOccurrenceChangesPayload` em helpers. 5 testes de integração cobrem: edição global sem seletor, abertura contextual, default de escopo, ausência de "Todas" e payloads `single`/`this_and_next`. Arquivos: `recurrence-details-modal.tsx`, `recurrence-form-modal.tsx`, `recurrences-page.tsx`, `recurrences.helpers.ts`, `recurrences.integration.test.tsx`. Validação: lint 0 erros, build ✓, 25 testes passando.
- 2026-05-06: Backend/Recurrences — aplicado horizonte operacional máximo de 1 ano para recorrências `endType = never`. Timeline agora limita projeções a `startDate + 1 ano` mesmo quando `untilDate` vem maior, mantendo `summary.totalOccurrences = null` e calculando `pagination.total` dentro do horizonte. Materialização também limita o processamento ao mesmo horizonte e finaliza a regra sem fim ao ultrapassá-lo quando não houver pendência aberta. Antecipação passa a rejeitar ocorrência futura fora do horizonte. Documentação e testes de recorrência atualizados.
- 2026-05-06: Frontend/Recurrences — modal de detalhes passou a enviar `untilDate = startDate + 1 ano` ao consultar timeline de recorrências `Sem fim`, alinhando a UI ao horizonte operacional do backend e evitando projeção visual além do contrato mesmo em paginação/cache. Adicionado teste unitário para cálculo do horizonte operacional semanal/mensal.
- 2026-05-06: Frontend/Recurrences — campo `Dia da semana` do modal de criação/edição passou a ser somente leitura e derivado da `Data inicial` para frequências semanal/quinzenal. O valor técnico `dayOfWeek` continua sendo atualizado pelo formulário, mas o usuário não interage diretamente com o campo, evitando divergência entre data e dia exibido.
- 2026-05-06: Frontend/Recurrences — modal de detalhes passou a exibir `Observações` da recorrência na última linha do resumo, logo após `Última materialização`, `Atualizada em` e `Finalizada em`, com fallback `-` quando vazio. Teste de integração do detalhe atualizado para cobrir o campo.

- 2026-05-06: Backend+Frontend/Recurrences — corrigida troca/criação de término `Por data final`. Frontend agora envia payload limpo para `until_date` tanto em criação quanto edição, marca/valida a alteração de `endType` e limpa campos incompatíveis ao alternar o tipo de término. Backend normaliza o payload criado em `this_and_next` e a timeline agora trata `until_date` como série finita, interrompendo projeções em `endDate` e preenchendo `summary.totalOccurrences`/`pagination.total`. Documentação de recorrências atualizada. Arquivos: `opa-finance-front/src/features/recurrences/components/recurrence-form-modal.tsx`, `opa-finance-front/src/features/recurrences/model/recurrences.helpers.ts`, `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`, `opa-finance-front/test/unit/features/recurrences/model/recurrences.helpers.test.ts`, `opa-finance-api/src/modules/recurrences/recurrence.helpers.ts`, `opa-finance-api/src/modules/recurrences/recurrence-timeline.service.ts`, `opa-finance-api/test/unit/recurrence-service.unit.test.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, docs de recorrências. Validação: testes focados frontend/backend ok, integração completa de recorrências do frontend ok, `npm run lint` e `npm run build` ok nos dois pacotes com warnings pré-existentes.

- 2026-05-05: Backend+Frontend/Recurrences — revisão correta do fluxo de edição de recorrência. Causa raiz confirmada: `updateRecurrenceSchema` herdava defaults da criação via `.partial()`, preenchendo `endType: never` em updates parciais e convertendo recorrências `by_occurrences` para "sem fim" ao editar só descrição. Backend: schema de update agora é explícita e sem defaults, aceita `subcategoryId: null`, valida agenda/término apenas quando esses campos vêm no update e o serviço normaliza campos incompatíveis de frequência/término sem sobrescrever campos omitidos. Frontend: edição passou a buscar a recorrência mais recente antes de abrir o modal, detectar alterações por comparação com a entidade carregada e enviar snapshot tipado por escopo (`all` regra completa, `this_and_next` regra completa sem `startDate`, `single` apenas campos de negócio). Documentação atualizada em `opa-finance-api/docs/API_REFERENCE_FRONTEND.md` e `opa-finance-api/docs/modules/modules_recurrences.md`. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence.schemas.ts`, `opa-finance-api/src/modules/recurrences/recurrence-edit.service.ts`, `opa-finance-front/src/features/recurrences/model/recurrences.helpers.ts`, `opa-finance-front/src/features/recurrences/components/recurrences-page.tsx`, testes de recorrências. Validação: testes focados backend/frontend ok, `npm run lint` ok nos dois pacotes (somente warnings pré-existentes fora do escopo), `npm run build` ok nos dois pacotes.

- 2026-05-05: Frontend/Recurrences — refinado payload de edição para não enviar `null` em campos opcionais que não foram alterados (`notes`, `description`, `subcategoryId`). O snapshot completo continua sendo enviado para regra/agenda, mas limpezas nulas agora só entram no payload quando representam alteração real do usuário. Isso evita rejeição por APIs/processos ainda com validação antiga e mantém suporte a limpar campos quando necessário. Arquivos: `opa-finance-front/src/features/recurrences/model/recurrences.helpers.ts`, `opa-finance-front/src/features/recurrences/components/recurrences-page.tsx`, testes de helpers e integração de recorrências. Validação: testes focados ok, `npm run lint` front ok com warnings pré-existentes, `npm run build` front ok.

- 2026-05-05: Frontend/Recurrences — após salvar edição com escopo `Todas`, o modal de detalhes agora é atualizado imediatamente com a resposta do backend, sem exigir fechar e abrir novamente. O cache individual da recorrência também é preenchido com a resposta do `PUT /recurrences/:id` antes da invalidação/refetch. Arquivos: `opa-finance-front/src/features/recurrences/components/recurrences-page.tsx`, `opa-finance-front/src/features/recurrences/recurrences.api.ts`, teste de integração de recorrências. Validação: teste focado ok, `npm run lint` front ok com warnings pré-existentes, `npm run build` front ok.

- 2026-05-05: Frontend/Recurrences — corrigido o fluxo de exclusão pelo modal de detalhes: após excluir uma recorrência finalizada, o modal agora fecha e os caches da recorrência/timeline removida são limpos, evitando que a timeline refaça consulta para um ID excluído e mostre "Recorrência não encontrada" dentro do modal. Arquivos: `opa-finance-front/src/features/recurrences/components/recurrences-page.tsx`, `opa-finance-front/src/features/recurrences/recurrences.api.ts`, teste de integração de recorrências. Validação: teste focado ok, `npm run lint` front ok com warnings pré-existentes, `npm run build` front ok.
- 2026-05-05: Frontend/Recurrences — após finalizar recorrência pelo modal de detalhes, o estado local é atualizado imediatamente com a resposta do backend. Recorrência finalizada agora oculta os botões `Editar` e `Finalizar`, mantendo apenas `Excluir`, conforme contrato de ciclo de vida. Arquivos: `opa-finance-front/src/features/recurrences/components/recurrence-details-modal.tsx`, `opa-finance-front/src/features/recurrences/components/recurrences-page.tsx`, `opa-finance-front/src/features/recurrences/recurrences.api.ts`, teste de integração de recorrências. Validação: teste focado ok, `npm run lint` front ok com warnings pré-existentes, `npm run build` front ok.

- 2026-05-05: Frontend/Recurrences — em recorrências semanais/quinzenais, alteração da `Data inicial` agora recalcula automaticamente o `Dia da semana` correspondente, evitando inconsistência visual entre data e agenda. A sincronização não roda apenas por abrir o modal, para não alterar dados carregados sem ação do usuário. Arquivos: `opa-finance-front/src/features/recurrences/components/recurrence-form-modal.tsx`, teste de integração de recorrências. Validação: teste focado ok, `npm run lint` front ok com warnings pré-existentes, `npm run build` front ok.

- 2026-05-05: Backend/Recurrences — corrigido bug de update parcial que convertia recorrências com término por ocorrências em `never` ao salvar campos não relacionados. Causa raiz: `updateRecurrenceSchema` herdava `default("never")` de `createRecurrenceSchema` via `.partial()`, então o parse do `PUT` preenchia `endType` mesmo quando ele não vinha no payload. Solução: schema de update passou a ser explícita e sem defaults de criação; o update parcial agora preserva `endType`/`endOccurrences` quando não alterados. Testes de integração do front foram ajustados para cobrir o caso real com `endType = by_occurrences` e `endOccurrences = 5`. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence.schemas.ts`, `opa-finance-api/test/unit/recurrences-create-materialize-schema.unit.test.ts`, `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`. Validação: `npx vitest run test/unit/recurrences-create-materialize-schema.unit.test.ts` ok, `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx -t 'deve enviar somente a descrição quando editar apenas a descrição'` ok, `npm run lint` ok com warnings pré-existentes.

- 2026-05-05: Frontend/Recurrences — corrigido o update parcial do modal de edição: agora o payload de salvamento é montado a partir dos campos realmente alterados (`dirtyFields` do `react-hook-form`), evitando que editar só a descrição altere `endType`/`endOccurrences` por acidente. A integração ganhou cobertura específica garantindo que edição de descrição não envia término nem quantidade de ocorrências. Arquivos: `opa-finance-front/src/features/recurrences/model/recurrences.helpers.ts`, `opa-finance-front/src/features/recurrences/components/recurrences-page.tsx`, `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`. Validação: `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx -t 'deve enviar somente a descrição quando editar apenas a descrição|deve mostrar erro de conflito 409 ao salvar edição|deve recarregar os dados mais recentes antes de abrir a edição'` ok, `npm run lint` ok com warnings pré-existentes.

- 2026-05-05: Frontend/Recurrences — corrigido o fluxo de edição para não salvar com versão stale: ao clicar em `Editar recorrência`, o frontend agora busca a recorrência mais recente no backend antes de abrir o formulário, evitando 409 falso quando a lista/detalhe estava defasado. Também foi criado teste de integração cobrindo a recarga prévia dos dados. Arquivos: `opa-finance-front/src/features/recurrences/components/recurrences-page.tsx`, `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`. Validação: `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx -t 'deve mostrar erro de conflito 409 ao salvar edição|deve recarregar os dados mais recentes antes de abrir a edição'` ok, `npm run lint` ok com warnings pré-existentes.

- 2026-05-05: Frontend/Recurrences — correção do modal de edição de recorrência: os selects principais passaram a ter `Label` associado (`Modo de lançamento`, `Origem`, `Frequência`, `Término`) e o campo `Dia do mês` recebeu `id` próprio, deixando o formulário editável corretamente acessível e testável. O mock da integração foi ajustado para incluir `postingMode: automatic`, e o teste de conflito 409 agora abre a linha da recorrência pelo nome real da tabela antes de entrar no modal de edição. Arquivos: `opa-finance-front/src/features/recurrences/components/recurrence-form-modal.tsx`, `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`. Validação: `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx -t 'deve mostrar erro de conflito 409 ao salvar edição'` ok, `npm run lint` ok com warnings pré-existentes.

- 2026-05-05: Frontend/Recurrences — correção do modal de edição de recorrência: o campo `Dia da semana` voltou a ser controlado pelo próprio formulário em vez de ser derivado de `startDate`, os estados locais do modal são limpos ao abrir uma edição e a troca de frequência limpa campos dependentes para evitar valores stale. A integração de recorrências passou a validar que os valores carregados ao abrir edição batem com a recorrência existente. Arquivos: `opa-finance-front/src/features/recurrences/components/recurrence-form-modal.tsx`, `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`. Validação: pendente (`npm run lint` será executado).

- 2026-05-05: Frontend/Recurrences — Correção de bug: campo "Término" não pré-preenchia ao editar recorrência. `endType` e `frequency` eram derivados via `form.watch()` no pai e passados como props ao modal, criando janela de timing onde o modal abria antes do `form.reset()` ser refletido no pai. Fix: removidos `endType` e `frequency` dos props de `RecurrenceFormModal`; ambos passam a ser lidos via `form.watch()` diretamente dentro do modal (o `form` já era prop). Arquivos: `recurrence-form-modal.tsx`, `recurrences-page.tsx`. Validação: lint ok (0 erros), build ok.

- 2026-05-04: Frontend/Recurrences — Campo Descrição do form de recorrência agora tem autocomplete igual ao form de transação: duas queries (`baseDescriptionsQuery` e `filteredDescriptionsQuery`) com debounce de 1000ms, usando `useTransactionDescriptions` e `buildDescriptionSuggestions`. Prop `enableSuggestions` ativado. Arquivos: `recurrences-page.tsx`, `recurrence-form-modal.tsx`. Validação: build ok.

- 2026-05-04: Frontend/Recurrences — Subcategorias do form de recorrência agora carregam igual ao form de transação: uma única query com `Promise.all` busca subcategorias de todas as categorias ao abrir o modal, montando um `Record<categoryId, Subcategory[]>`. Removida a query lazy (por categoria selecionada) e o `useEffect` de validação. Arquivos: `recurrences-page.tsx`, `recurrence-form-modal.tsx`. Validação: build ok.

- 2026-05-04: Frontend/Recurrences — Reorganização do form de recorrências: (1) removido campo "Tipo (derivado da categoria)" e import/variável `TransactionTypeField`/`selectedTransactionType`; (2) nova ordem: Origem|Frequência|Modo → Data inicial|Dia → Término|Data final/Qtd → Conta|Valor → Categoria/Sub → Descrição → Observações. Arquivo: `recurrence-form-modal.tsx`. Validação: lint ok, build ok.

- 2026-05-04: Frontend/Recurrences — Substituídos todos os `window.confirm` e `window.prompt` do módulo de recorrências pelo componente `AlertDialog` do shadcn (`@radix-ui/react-alert-dialog`). Diálogos afetados: Finalizar recorrência, Excluir recorrência, Ignorar ocorrência (com campo de motivo), Ignorar pendências em massa (com campo de motivo). Arquivos: `recurrences-page.tsx`, `recurrence-details-modal.tsx`, `src/components/ui/alert-dialog.tsx` (criado via CLI). Validação: lint ok (0 erros), build ok.

- 2026-05-04: Frontend/Recurrences — Erros de Finalizar e Excluir agora aparecem dentro do `RecurrenceDetailsModal` em vez de na página. `handleFinalize` e `handleDelete` passaram a usar `setActionError`; `RecurrenceDetailsModal` recebe nova prop `errorMessage` e exibe o erro abaixo do header. Arquivos: `recurrences-page.tsx`, `recurrence-details-modal.tsx`. Validação: build ok.

- 2026-05-04: Backend/Recurrences — Recorrência `automatic` com `startDate <= hoje` agora materializa imediatamente ao ser criada, sem esperar o job diário. Implementado em `RecurrenceService.create()`: após inserção, se `postingMode === 'automatic'` e `nextOccurrenceDate <= hoje`, chama `materializeService.materialize(userId, { recurrenceId })`. Adicionado filtro opcional `recurrenceId` ao `materializeRecurrencesSchema` e à query de `RecurrenceMaterializeService`. Erro de materialização imediata é logado como warning (não quebra o fluxo de criação). Arquivos: `recurrence.schemas.ts`, `recurrence-materialize.service.ts`, `recurrence.service.ts`. Validação: lint ok, build ok.

- 2026-05-04: Backend+Frontend/Recurrences — Antecipação de ocorrência projetada. Backend: novo endpoint `POST /recurrences/:id/anticipate` que valida data na série, verifica duplicata, respeita limite `by_occurrences`, insere ocorrência e materializa na mesma transação. Frontend: hook `useAnticipateRecurrenceOccurrence`, tipo `AnticipateRecurrenceOccurrencePayload`, `canConfirm: true` para itens projetados na timeline, modal `ConfirmRecurrenceOccurrenceModal` adaptado para detectar item projetado (sem `reviewPayload`) e chamar endpoint correto. Arquivos modificados: `recurrence.schemas.ts`, `recurrence-occurrence.service.ts`, `recurrence.service.ts`, `recurrence.routes.ts`, `recurrence-timeline.service.ts`, `recurrences.api.ts`, `confirm-recurrence-occurrence-modal.tsx`. Validação: lint ok (0 erros), unit tests 54/54 ok, build backend e frontend ok.

- 2026-05-04: Frontend/Transactions — `invalidateTransactionDependents()` agora invalida também `['recurrence-timeline']`, fazendo a timeline no modal de recorrências refletir imediatamente edições de valor feitas em transações. Arquivo: `opa-finance-front/src/features/transactions/transactions.api.ts`. Validação: lint ok (0 erros), build ok.

- 2026-05-04: Backend/Recurrences — `RecurrenceTimelineService.timeline()` agora busca o `amount` atual da tabela `transactions` para ocorrências com `status === "materialized"` e `transactionId != null`, em vez de usar o valor do `reviewPayload`. Implementado via query `IN` separada após buscar `persistedRows`, montando um `Map<transactionId, amount>` para lookup. Filtro `eq(transactions.userId, userId)` adicionado por defesa. Arquivos: `opa-finance-api/src/modules/recurrences/recurrence-timeline.service.ts`. Validação: lint ok (0 erros), unit tests 54/54 ok, build ok.

- 2026-05-03: Frontend/Audit — `AuditPage` reestruturada para layout `h-full flex flex-col`: título e filtros `shrink-0`, área de tabela `flex-1 min-h-0 overflow-hidden rounded-lg border` com scroll apenas no `tbody` (`overflow-y-auto flex-1 min-h-0`) e `thead sticky`; `TablePagination` fixo na base (`shrink-0`). Grid CSS substituído por `<table>` com `<thead>`/`<tbody>` seguindo o padrão do `RecurrenceDetailsModal`. Arquivo: `opa-finance-front/src/features/audit/components/audit-page.tsx`. Validação: lint + build ok (zero erros).

- 2026-05-03: Frontend — criado componente reutilizável `TablePagination` em `src/components/ui/table-pagination.tsx` com props: `page`, `totalPages` (null = infinito), `hasMore`, `onPageChange`, `pageSize?`, `onPageSizeChange?`, `pageSizeOptions?`, `totalRecords?`, `isLoading?`. Adotado em: `recurrence-details-modal.tsx`, `transactions-page.tsx` (removido bloco de ~70 linhas inline e imports de ChevronLeft/ChevronRight/buildPaginationItems), `audit-page.tsx` (ganhou selector de page size e botões numerados), `recurrences-list.tsx` (props `onPrevPage`/`onNextPage` → `onPageChange`, `recurrences-page.tsx` atualizado). Validação: lint + build ok (zero erros).

- 2026-05-03: Frontend/Recurrences — refinamento final de densidade e tipografia nos três modais da feature: headers, descrições e footers foram compactados, o `RecurrenceDetailsModal` teve a seção de metadados reindentada e os cartões/tabela receberam ritmo visual mais consistente; o `ConfirmRecurrenceOccurrenceModal` ficou mais compacto e com ações estáveis. Arquivos: `opa-finance-front/src/features/recurrences/components/recurrence-form-modal.tsx`, `opa-finance-front/src/features/recurrences/components/recurrence-details-modal.tsx`, `opa-finance-front/src/features/recurrences/components/confirm-recurrence-occurrence-modal.tsx`. Validação: pendente (`npm run lint` será executado).

- 2026-05-03: Frontend/Recurrences — outros modais da feature alinhados ao mesmo padrão do formulário: `RecurrenceDetailsModal` e `ConfirmRecurrenceOccurrenceModal` passaram a usar shell consistente com backdrop mais forte, header com botão explícito de fechar, área interna com scroll controlado e grids mais densos; o modal de confirmação também ganhou fechamento por `Escape` e botão principal como `submit`. Arquivos: `opa-finance-front/src/features/recurrences/components/recurrence-details-modal.tsx`, `opa-finance-front/src/features/recurrences/components/confirm-recurrence-occurrence-modal.tsx`. Validação: pendente (`npm run lint` será executado).

- 2026-05-03: Frontend/Recurrences — compactação do bloco de periodicidade no `RecurrenceFormModal`: a grade passou a usar 2 colunas para `weekly`/`biweekly`/`monthly` e 3 colunas para `yearly`, reduzindo vazios laterais e deixando a leitura do formulário mais densa. Arquivo: `opa-finance-front/src/features/recurrences/components/recurrence-form-modal.tsx`. Validação: pendente (`npm run lint` será executado).

- 2026-05-03: Frontend/Recurrences — ajuste fino no campo `Dia do mês` do `RecurrenceFormModal`: o input passou a usar `h-10 w-full` para ficar consistente com os demais campos do formulário e eliminar a sensação de desalinhamento visual no bloco de periodicidade. Arquivo: `opa-finance-front/src/features/recurrences/components/recurrence-form-modal.tsx`. Validação: pendente (`npm run lint` será executado).

- 2026-05-03: Frontend/Recurrences — refinamento de layout do `RecurrenceFormModal` para corrigir desalinhamento visual: grids principais migraram para `auto-fit` com `minmax`, bloco `Conta`/`Tipo` reorganizado em colunas 7/5 no desktop, `Categoria/Subcategoria` voltou a ocupar linha cheia, e os campos de descrição/observações ficaram com colunas equivalentes. O bloco de edição também ganhou fundo e borda mais suaves para separar a área sem pesar a tela. Arquivo: `opa-finance-front/src/features/recurrences/components/recurrence-form-modal.tsx`. Validação: pendente (`npm run lint` será executado).

- 2026-05-03: Frontend/Recurrences — modal de formulário de recorrência reestruturado para melhor UX e acessibilidade: shell agora usa `role="dialog"`/`aria-modal`, foco automático ao abrir, fechamento por `Escape`, backdrop mais consistente, container com `max-h` e área interna com scroll próprio, cabeçalho com botão explícito de fechar e rodapé fixo para ações. A lógica de `formError`/erros gerais foi mantida, com pequena limpeza de estado derivado (`isEditingActive`, `hasFormErrors`, `isSubmitDisabled`). Arquivo: `opa-finance-front/src/features/recurrences/components/recurrence-form-modal.tsx`. Validação: pendente (`npm run lint` será executado).

- 2026-05-03: Frontend/Recurrences — tabela da timeline com scroll próprio e rodapé de paginação travado: modal convertido para `flex flex-col overflow-hidden` (sem scroll global); seção timeline e conteúdo de sucesso recebem `flex-1 min-h-0` para crescer; tabela usa `overflow-auto flex-1 min-h-0` com `thead sticky top-0 z-10 bg-muted`. Arquivo: `recurrence-details-modal.tsx`. Validação: lint + build ok.

- 2026-05-03: Backend+Frontend/Recurrences — paginação e ordenação server-side na timeline de recorrências. Backend: adicionados parâmetros `page` (int ≥1, default 1) e `dir` ('asc'|'desc', default 'asc') ao `recurrenceTimelineQuerySchema`; default de `limit` alterado de 24 → 12. Service `recurrence-timeline.service.ts` refatorado para suportar offset de paginação (`effectiveOffset = (page-1)*limit`) com loop revisado que salta itens antes da janela e detecta `hasMore` (para recorrências com `dir:'desc'` e total finito, calcula `reverseOffset` e inverte os itens). Novo método privado `countOccurrences` para calcular total em `until_date`. Tipo `TimelineResponse` expandido com campo `pagination: {page, limit, hasMore, total}`. Frontend: tipos `RecurrenceTimelineQueryParams` e `RecurrenceTimelineResponse` atualizados; modal adiciona estado `page`, `pageSize` (default 12), `dir`; cabeçalho "Data" é clicável (toggle asc/desc com ícone); controles de paginação com Select de itens/página [12,24,48] e botões Anterior/Próxima. Arquivos: `recurrence.schemas.ts`, `recurrence-timeline.service.ts`, `recurrences.api.ts`, `recurrence-details-modal.tsx`. Validação: unit tests 54/54 ✓, lint + build backend ✓, lint + build frontend ✓.

- 2026-05-03: Frontend/Recurrences — reestruturação do grid de metadados do `RecurrenceDetailsModal`: layout convertido para linhas explícitas (`grid-cols-3 divide-x` por linha, container com `divide-y`); Descrição ocupa 2 colunas com Status na 1ª; Modo de lançamento movido para linha própria; Conta e Categoria separadas em células distintas; Subcategoria exibida abaixo da Categoria na mesma célula; para transferências exibe De/Para conta; removido `formatRecurrenceTarget` dos imports. Arquivo: `opa-finance-front/src/features/recurrences/components/recurrence-details-modal.tsx`. Validação: lint + build ok.

- 2026-05-03: Frontend/Recurrences — modal de detalhes de recorrência ampliado: `max-w-3xl` → `max-w-5xl`, padding do backdrop reduzido para `p-2` em mobile (`sm:p-4`) e `max-h` ajustado por breakpoint para aproveitar mais a tela em dispositivos menores. Arquivo: `opa-finance-front/src/features/recurrences/components/recurrence-details-modal.tsx`. Validação: lint + build ok.

- 2026-05-03: Frontend/Recurrences — ajuste visual do `RecurrenceDetailsModal`: metadados agora em container único com `divide-y/divide-x` sem bordas por célula, labels com `text-[11px] text-muted-foreground/70`; cards de resumo convertidos para `flex divide-x` com `flex-1` por card (distribuição uniforme). Arquivo: `opa-finance-front/src/features/recurrences/components/recurrence-details-modal.tsx`. Validação: lint + build ok.

- 2026-05-03: Frontend/Recurrences — refinamento visual do `RecurrenceDetailsModal` para reduzir altura/largura percebida: container `max-w-3xl` com paddings e gaps menores, grid de metadados convertido em linhas compactas (label inline + valor truncado), cards de resumo da timeline em uma linha (`lg:grid-cols-7`) com tipografia menor, banner de pendências em uma linha, e tabela com `px-3 py-2`. Botões de ação por linha agora são icon-only (`CheckCircle2` / `SkipForward`, `size="icon-sm"`) com `ShortcutTooltip` e estado desabilitado/esmaecido (`disabled:opacity-40`) quando indisponíveis. Arquivo editado: `opa-finance-front/src/features/recurrences/components/recurrence-details-modal.tsx`. Validação: `cd opa-finance-front && npm run lint && npm run build` (ok).

- 2026-05-03: Backend/Fastify — adicionado preflight obrigatório de conexão com Postgres no bootstrap da API (`SELECT 1` com timeout de 5s em `opa-finance-api/src/server.ts`); quando a checagem falha/expira, o processo encerra com erro (`process.exit(1)`) e o servidor não sobe. Validação: `cd opa-finance-api && npm run lint`.

- 2026-05-03: Backend/Fastify — correção de timeout no boot do plugin de unaccent: arquivo opa-finance-api/src/core/plugins/unaccent.ts agora aplica timeout defensivo (2s) na consulta de pg_extension; quando o banco demora/indisponível, o plugin finaliza sem bloquear bootstrap e define unaccentEnabled=false como fallback. Validação: cd opa-finance-api && npm run lint.

- 2026-05-03: Backend/Recorrências — PE concluído e movido para `docs-hidden/melhorias/done/recorrencia/`; a única validação não executada (`RCREV-S4-21`) foi explicitamente pulada por decisão do usuário. Task `RCREV-S5-04` marcada no PE. Arquivos editados/criados: `docs-hidden/melhorias/done/recorrencia/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `git diff --check`.

- 2026-05-03: Backend/Recorrências — consolidação final da documentação do backend: `docs/API_REFERENCE_FRONTEND.md`, módulo de recorrências e runbook do job diário ficaram alinhados ao fluxo `automatic` / `review_required`, timeline, confirm/skip, auditoria e diagnóstico operacional. Task `RCREV-S5-03` marcada no PE. Arquivos editados/criados: `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: revisão documental e `git diff --check`.

- 2026-05-03: Backend/Recorrências — runbook operacional do job diário atualizado com o comportamento atual de `review_required`, `failed` terminal no MVP, eventos de log úteis e comandos de diagnóstico por status. Task `RCREV-S5-02` marcada no PE. Arquivos editados/criados: `docs-hidden/melhorias/done/recorrencia/PE_recorrencia_runbook_job_diario.md`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `git diff --check`.

- 2026-05-03: Backend/API — referência técnica atualizada para recorrências: `confirm` documenta `metadata.adjustments`, mensagens 422/409 em pt-BR e `skip` documenta `metadata.skipReason`/`metadata.skippedAt`. Task `RCREV-S5-01` marcada no PE. Arquivos editados/criados: `opa-finance-api/docs/API_REFERENCE_FRONTEND.md`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: revisão documental e `git diff --check`.

- 2026-05-03: Backend/Recorrências — teste integrado de `metadata.adjustments`: confirmação com ajustes em `occurrenceDate`, `amount`, `description` e `categoryId` agora persiste `adjustments.fields` e `adjustedAt` no `metadata` da ocorrência materializada. Task `RCREV-S4-24` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'confirma pendência de transação com lock otimista e ajustes'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de mensagens pt-BR para erros críticos: confirm com data fora do range retorna `422` com `detail` exato e conflitos de versão em confirm/update retornam `409` com mensagens localizadas. Task `RCREV-S4-23` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'retorna mensagens em pt-BR para erros críticos de confirm e update'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de `EXPLAIN ANALYZE` para o count de pendências por recorrência: a query do badge passou a demonstrar uso do índice composto `recurrence_occurrences_recurrence_status_idx` em ambiente de teste determinístico, sem alterar o schema permanente. Task `RCREV-S4-22` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'usa o indice composto no count de pendencias por recorrencia'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de `sequence` com gap: a timeline passou a expor `skipped` ocupando a posição esperada na sequência, mantendo `materialized`, `skipped` e `pending_review` alinhados com a ordem de consumo. Task `RCREV-S4-20` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'mantem skipped ocupando posicao na sequence da timeline'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de confirm com `occurrenceDate` fora do range: a confirmação agora retorna `422` com `detail` em pt-BR quando a data ajustada sai de `startDate..endDate`, preservando a pendência sem alterar versão ou criar transação. Task `RCREV-S4-19` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'rejeita confirm com occurrenceDate fora do range permitido'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de primeira ocorrência aninhada em transferência automática: além do caso `review_required`, a criação recorrente em `POST /transfers` também é validada com `postingMode=automatic`, mantendo a primeira ocorrência como `materialized`. Task `RCREV-S4-18` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/transfers/create-transfer.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/transfers/create-transfer.test.ts -t 'deve materializar ocorrência inicial em transferência recorrente automática'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de deduplicação no forecast com `review_required`: uma recorrência mensal com 3 pendências persistidas passa a retornar `projectedOccurrences=3` e totais mensais corretos, sem duplicar projeções. Task `RCREV-S4-17` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-forecast.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-forecast.test.ts`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de cap de geração em `by_occurrences` com `review_required`: o materializador rodado 5 vezes na mesma janela gera exatamente 3 pendências e trava novas criações após o limite. Task `RCREV-S4-16` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'conta pendências no limite by_occurrences em modo de revisão'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — fechamento documental de `POST /recurrences/materialize` em `review_required`: a cobertura já existente para geração de `pending_review` em transação e transferência foi consolidada no PE como `RCREV-S4-15`. Arquivos editados/criados: `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: auditoria de cobertura existente, sem nova execução de código.

- 2026-05-03: Backend/Recorrências — teste integrado de bloqueio de finalize/exclusão com pendência aberta: além da resposta 422 para os dois endpoints, a recorrência permanece ativa e sem `finalizedAt`/`deletedAt` após as tentativas bloqueadas. Task `RCREV-S4-14` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'bloqueia finalize e exclusao quando ha pendencia aberta'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de alteração de `postingMode` em recorrência ativa: a recorrência agora é validada no fluxo `automatic -> review_required -> automatic` sem pendências abertas, cobrindo ambos os sentidos permitidos. Task `RCREV-S4-13` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'altera postingMode em recorrencia ativa nos dois sentidos sem pendencias abertas'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de `GET /transactions` sem pendências: a listagem passa a ser validada com uma transação manual coexistindo com uma pendência `review_required`, garantindo que apenas lançamentos reais aparecem no endpoint. Task `RCREV-S4-12` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'nao retorna pendencias review_required em GET /transactions'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de avanço de `nextOccurrenceDate` em `review_required`: a materialização da pendência avança a próxima data sem atualizar `lastMaterializedDate`, preservando a semântica de revisão. Task `RCREV-S4-11` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'avanca nextOccurrenceDate em review_required sem alterar lastMaterializedDate'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de idempotência em `review_required`: rodar o job duas vezes na mesma janela não duplica `pending_review`, preservando `createdOccurrences` e a sequência de ocorrências. Task `RCREV-S4-10` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'nao duplica pendencias review_required ao rodar o job duas vezes na mesma janela'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de concorrência em confirm: a corrida entre duas confirmações simultâneas agora é validada com `409` na segunda requisição, preservando a confirmação vencedora e a versão da ocorrência. Task `RCREV-S4-09` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'rejeita confirm concorrente com 409 para a segunda requisição'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Frontend/Recorrências — teste integrado do modal de detalhe: além da abertura da timeline, o modal agora é validado também no fechamento via `Escape`, cobrindo o comportamento básico da janela de detalhes. Task `RCREV-S4-08` marcada no PE. Arquivos editados/criados: `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-front && npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx`, `cd opa-finance-front && npm run lint`, `cd opa-finance-front && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de timeline com persistidas e projetadas em recorrência automatic: o endpoint passa a ser validado também no cenário parcialmente materializado, com sequência, `source` persistido/projetado e resumo agregado coerentes. Task `RCREV-S4-07` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'retorna timeline com persistidas e projetadas em recorrência automatic'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de skip consumindo limite `by_occurrences`: o `skip` agora é validado também como ocorrência consumida para impedir que o materializador ultrapasse o total permitido após uma pendência ignorada. Task `RCREV-S4-06` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'faz skip consumir o limite by_occurrences'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de confirmação de transferência com rollback: a confirmação agora é validada também pelo cenário em que a segunda perna falha e a transação é revertida sem persistir lançamento parcial ou alterar a ocorrência pendente. Task `RCREV-S4-05` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'reverte confirm de transferencia quando a segunda perna falha'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de confirmação de pendência de transação: além do ajuste de campos, a confirmação passa a ser validada também pela criação efetiva do lançamento materializado com `expectedVersion` e incremento de versão. Task `RCREV-S4-04` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'confirma pendência de transação e cria lançamento materializado'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de geração de pendência em materialização: além do fluxo de transação, a recorrência de transferência em `review_required` também gera `pending_review` sem criar transação, cobrindo os dois tipos suportados. Task `RCREV-S4-03` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'gera pendência sem criar transação quando postingMode exige revisão|gera pendência de transferencia sem criar transacao quando postingMode exige revisão'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de materialização automática sem regressão: cenário `automatic` confirma criação de transações, estado `materialized`, avanço de `nextOccurrenceDate` e atualização de `lastMaterializedDate`. Task `RCREV-S4-02` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'materializa recorrência automatic sem regressão'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Backend/Recorrências — teste integrado de cálculo e contagem da timeline: cenário `review_required` com três pendências materializadas valida `consumedOccurrences`, `pendingReviewOccurrences`, `projectedOccurrences`, `totalAmount` e a ocupação de sequência sem projeção extra. Task `RCREV-S4-01` marcada no PE. Arquivos editados/criados: `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t 'retorna timeline com persistidas, projetadas, sequência e ações|conta corretamente pendências e projeções na timeline de recorrência review_required'`, `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`.

- 2026-05-03: Frontend/Recorrências — confirmação de pendência agora traduz a 422 de `occurrenceDate` fora do range com mensagem clara em PT-BR, com cobertura de integração e teste unitário do helper. Task `RCREV-S3-11` marcada no PE. Arquivos editados/criados: `opa-finance-front/src/features/recurrences/components/confirm-recurrence-occurrence-modal.tsx`, `opa-finance-front/src/features/recurrences/model/recurrences.helpers.ts`, `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`, `opa-finance-front/test/unit/features/recurrences/model/recurrences.helpers.test.ts`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-front && npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx test/unit/features/recurrences/model/recurrences.helpers.test.ts`, `cd opa-finance-front && npm run lint`, `cd opa-finance-front && npm run build`.

- 2026-05-03: Backend/Recorrências + Frontend/Recorrências — badge/contador de pendências por linha na listagem: a API de `GET /recurrences` passou a devolver `pendingReviewCount` por recorrência, e a listagem do frontend exibe o contador em destaque na linha correspondente. A documentação do backend foi atualizada para refletir o novo contrato. Task `RCREV-S3-10` marcada no PE. Arquivos editados/criados: `opa-finance-api/src/modules/recurrences/recurrence-crud.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence-timeline.service.ts`, `opa-finance-api/docs/modules/modules_recurrences.md`, `opa-finance-api/docs/API_REFERENCE_FRONTEND.md`, `opa-finance-front/src/features/recurrences/recurrences.api.ts`, `opa-finance-front/src/features/recurrences/components/recurrences-list.tsx`, `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-api && npm run lint`, `cd opa-finance-api && npm run build`, `cd opa-finance-front && npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx test/unit/features/recurrences/model/recurrences.helpers.test.ts`, `cd opa-finance-front && npm run lint`, `cd opa-finance-front && npm run build`.

- 2026-05-03: Frontend/Recorrências — UI de finalização/exclusão passou a exibir alerta quando há pendências abertas na timeline, com ação de ignorar pendências em massa e cobertura de integração para o banner e a operação coletiva. Task `RCREV-S3-09` marcada no PE. Arquivos editados/criados: `opa-finance-front/src/features/recurrences/components/recurrence-details-modal.tsx`, `opa-finance-front/src/features/recurrences/components/recurrences-page.tsx`, `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: pendente.

- 2026-05-03: Frontend/Recorrências — tratamento dos estados vazios e de erro na listagem: a paginação agora fica oculta quando a consulta está em loading, vazia ou com erro, e a integração ganhou cobertura para vazio e falha do endpoint. Task `RCREV-S3-08` marcada no PE. Arquivos editados/criados: `opa-finance-front/src/features/recurrences/components/recurrences-list.tsx`, `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`, `docs-hidden/changes.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `cd opa-finance-front && npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx test/unit/features/recurrences/model/recurrences.helpers.test.ts`, `cd opa-finance-front && npm run lint`, `cd opa-finance-front && npm run build`.

- 2026-05-03: Frontend/Transações — revisão da criação recorrente para payload aninhado: `useTransactionForm` e `useTransferForm` passaram a enviar `recurrence` dentro do create de transação/transferência, removendo a dupla mutação, a lógica de rollback e as mutações separadas de recorrência. Arquivos editados/criados: `opa-finance-front/src/features/transactions/transactions.api.ts`, `opa-finance-front/src/features/transfers/transfers.api.ts`, `opa-finance-front/src/features/transactions/hooks/use-transaction-form.ts`, `opa-finance-front/src/features/transactions/hooks/use-transfer-form.ts`, `opa-finance-front/src/features/transactions/components/transactions-page.tsx`, `opa-finance-front/src/features/transactions/components/transactions-create-modal.tsx`, `opa-finance-front/src/features/transactions/components/transactions-transfer-modal.tsx`, `opa-finance-front/test/unit/features/transactions/hooks/use-transaction-form.test.ts`, `opa-finance-front/test/unit/features/transactions/hooks/use-transfer-form.test.ts`, `opa-finance-front/test/integration/features/transactions/transactions.transfer.integration.test.tsx`, `docs-hidden/changes.md`. Validação: `cd opa-finance-front && npx vitest run test/unit/features/transactions/hooks/use-transaction-form.test.ts test/unit/features/transactions/hooks/use-transfer-form.test.ts test/integration/features/transactions/transactions.create-edit.integration.test.tsx test/integration/features/transactions/transactions.transfer.integration.test.tsx`, `cd opa-finance-front && npm run lint`, `cd opa-finance-front && npm run build`.

- 2026-05-03: Frontend/Transactions — limpeza de warnings preexistentes em `transactions-page.tsx`: removidos imports e variáveis mortas, prefixados callbacks órfãos com `_` e alinhada a ordem dos imports; o lint do frontend voltou a ficar sem warnings. Arquivos editados: `opa-finance-front/src/features/transactions/components/transactions-page.tsx`, `docs-hidden/changes.md`. Validação: `cd opa-finance-front && npm run lint`.

- 2026-05-02: Frontend/Recorrências — finalização do S3-06 do PE de modo de revisão: adicionadas as ações de confirmar e ignorar pendência no modal de detalhes, com envio de `expectedVersion`, fallback dos dados da revisão no payload, tratamento de 409 com refetch e modal de confirmação reutilizando a timeline do backend. Task `RCREV-S3-06` marcada no PE. Arquivos editados/criados: `opa-finance-front/src/features/recurrences/components/confirm-recurrence-occurrence-modal.tsx`, `opa-finance-front/src/features/recurrences/components/recurrence-details-modal.tsx`, `opa-finance-front/src/features/recurrences/components/recurrences-page.tsx`, `opa-finance-front/src/features/recurrences/recurrences.api.ts`, `opa-finance-front/src/features/transactions/components/transaction-account-field.tsx`, `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx`, `npm run lint` (86 warnings preexistentes, sem erros), `npm run build`.

- 2026-05-02: Frontend/Recorrências — evolução do modal de detalhes do S3-04/S3-05: a timeline passou a ser exibida em tabela com colunas de data, parcela, status, origem, valor, vínculo e ações, mantendo o consumo do endpoint `GET /recurrences/:id/timeline` com limite 24. Tasks `RCREV-S3-04` e `RCREV-S3-05` marcadas no PE. Arquivos editados/criados: `opa-finance-front/src/features/recurrences/components/recurrence-details-modal.tsx`, `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint` (80 warnings preexistentes em `transactions-page.tsx`), `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx test/unit/features/recurrences/model/recurrences.helpers.test.ts`, `npm run build`.

- 2026-05-02: Frontend/Recorrências — ajuste de revisão do S3-03: o modal de detalhes passou a consumir a timeline com limite 24, alinhado ao default do backend, e o teste de integração foi ajustado para validar o `limit=24` na query sem depender do texto da janela parcial. Arquivos editados/criados: `opa-finance-front/src/features/recurrences/components/recurrence-details-modal.tsx`, `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`, `docs-hidden/changes.md`. Validação: `npm run lint` (80 warnings preexistentes em `transactions-page.tsx`), `npm run build`, `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx test/unit/features/recurrences/model/recurrences.helpers.test.ts`.

- 2026-05-02: Backend/Recorrências — revisão documental do contrato de recorrências para consumo do frontend: `modules_recurrences.md`, `modules_audit.md` e `API_REFERENCE_FRONTEND.md` foram atualizados com `postingMode`, endpoints de `timeline`, `confirm` e `skip`, semântica de `pending_review`/`skipped`, filtros de auditoria expandido e exemplos alinhados ao backend atual. Arquivos editados/criados: `opa-finance-api/docs/modules/modules_recurrences.md`, `opa-finance-api/docs/modules/modules_audit.md`, `opa-finance-api/docs/API_REFERENCE_FRONTEND.md`, `docs-hidden/changes.md`. Validação: revisão documental.

- 2026-05-02: Frontend/Recorrências — Fase C.3 do PE de modo de revisão: adicionada a ação `Ver detalhes` na listagem, com modal dedicado exibindo os dados da recorrência e a timeline consumida do endpoint `GET /recurrences/:id/timeline`, incluindo resumo e itens persistidos/projetados. Task `RCREV-S3-03` marcada no PE. Arquivos editados/criados: `opa-finance-front/src/features/recurrences/components/recurrences-list.tsx`, `opa-finance-front/src/features/recurrences/components/recurrences-page.tsx`, `opa-finance-front/src/features/recurrences/components/recurrence-details-modal.tsx`, `opa-finance-front/src/features/recurrences/model/recurrences.constants.ts`, `opa-finance-front/src/features/recurrences/model/recurrences.helpers.ts`, `opa-finance-front/test/integration/features/recurrences/recurrences.integration.test.tsx`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint` (80 warnings preexistentes em `transactions-page.tsx`), `npm run build`, `npx vitest run test/integration/features/recurrences/recurrences.integration.test.tsx test/unit/features/recurrences/model/recurrences.helpers.test.ts`.

- 2026-05-02: Frontend/Recorrências — Fase C.2 do PE de modo de revisão: o formulário de recorrência passou a expor `postingMode` (`Automático`/`Com revisão`) com valor padrão em criação, preservação no carregamento de edição e bloqueio em escopo `single`; o mapper do formulário agora serializa o modo no create/update global. Task `RCREV-S3-02` marcada no PE. Arquivos editados/criados: `opa-finance-front/src/schemas/recurrence.schema.ts`, `opa-finance-front/src/features/recurrences/components/recurrence-form-modal.tsx`, `opa-finance-front/src/features/recurrences/model/recurrences.helpers.ts`, `opa-finance-front/test/unit/features/recurrences/model/recurrences.helpers.test.ts`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run build`, `npx vitest run test/unit/features/recurrences/model/recurrences.helpers.test.ts`.

- 2026-05-02: Frontend/Recorrências — Fase C.1 do PE de modo de revisão: contrato do feature ampliado com `postingMode`, tipos de ocorrência/timeline e hook `useRecurrenceTimeline`, além de labels do modo de lançamento para uso futuro na UI. Task `RCREV-S3-01` marcada no PE. Arquivos editados/criados: `opa-finance-front/src/features/recurrences/recurrences.api.ts`, `opa-finance-front/src/features/recurrences/model/recurrences.constants.ts`, `opa-finance-front/test/unit/features/recurrences/model/recurrences.helpers.test.ts`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run build`.

- 2026-05-02: Backend/Recorrências — Fase B.4(e) do PE de modo de revisão: documentado que `failed` é estado terminal no MVP no módulo `recurrences` e no runbook operacional do job diário, incluindo SQL de diagnóstico por `recurrence_id` para ocorrências com falha. Task `RCREV-S2-13` marcada no PE. Arquivos editados/criados: `opa-finance-api/src/modules/recurrences/recurrence-materialize.service.ts`, `opa-finance-api/docs/modules/modules_recurrences.md`, `docs-hidden/melhorias/done/recorrencia/PE_recorrencia_runbook_job_diario.md`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: revisão documental; sem mudança de comportamento funcional.

- 2026-05-02: Backend/Recorrências — Revisão profunda do PE de modo de revisão identificou e corrigiu 4 inconsistências entre código e plano. (1) `recurrence-timeline.service.ts`: `hasMoreProjected` em `by_occurrences` agora soma `consumedOccurrences + projectedOccurrences` antes de comparar com `endOccurrences`, evitando que a timeline completa apareça como parcial; também corrigida a precedência de `lastProcessedDate` sobre `untilDate` no branch `until_date`. (2) `recurrence-materialize.service.ts`: a query de contagem virou `GROUP BY status` para extrair `consumedCount` e `openPendingReviewCount` simultaneamente; finalização automática (`shouldFinalize`) só ocorre quando não há `pending_review` em aberto, preservando `RCREV-DEF-16` no caminho automático tanto em `by_occurrences` quanto em `until_date`. (3) `recurrence-forecast.service.ts`: pendências persistidas no range agora contribuem para `projected[type].months` usando `reviewPayload.amount` (ou `recurrence.amount` como fallback), com filtro de conta da regra-mãe e contagem em `metadata.projectedOccurrences`, alinhando com `RCREV-DEF-01`. (4) `recurrence-edit.service.ts`: a verificação de pendência em `applySingleScopeEdit` agora roda antes do guard de data passada, permitindo atualizar o `reviewPayload` de uma pendência vencida via `edit-scope` (`RCREV-DEF-18`). Testes ajustados para refletir o novo comportamento: `recurrences-forecast.test.ts` valida que 2 pendências de R$100 entram em `projected.expense=200`; `recurrences-critical-rules.test.ts` valida que recorrência permanece `active` enquanto há pendências, mesmo após atingir `endOccurrences`. Arquivos editados: `opa-finance-api/src/modules/recurrences/recurrence-timeline.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence-materialize.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence-forecast.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence-edit.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `opa-finance-api/test/integration/recurrences/recurrences-forecast.test.ts`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npm run build`. Integração pendente de execução manual com tunnel ativo.

- 2026-05-02: Backend/Recorrências — Fase B.4(d) do PE de modo de revisão: `single` agora roteia ocorrências `pending_review` para atualização do `reviewPayload` da pendência, preserva a regra-mãe quando a data não tem pendência e mantém o bloqueio de alteração de agenda em escopo `single`. A cobertura foi ampliada com testes de integração para pendência em revisão, fallback em data futura sem pendência e bloqueio de agenda. Task `RCREV-S2-12` marcada no PE. Arquivos editados/criados: `opa-finance-api/src/modules/recurrences/recurrence-edit.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t \"atualiza reviewPayload da pendência em single sem tocar na regra-mae|atualiza a regra-mae em single futuro sem pendencia|bloqueia escopo single quando payload tenta alterar agenda\"`, `npm run build`.

- 2026-05-02: Backend/Recorrências — Fase B.4(c) do PE de modo de revisão: `review_required -> automatic` agora é bloqueado com 422 quando a recorrência possui pendências `pending_review` em aberto, retornando os IDs bloqueantes no detalhe em PT-BR. A validação foi adicionada em `recurrence-edit.service.ts` e coberta por teste de integração dedicado. Task `RCREV-S2-11` marcada no PE. Arquivos editados/criados: `opa-finance-api/src/modules/recurrences/recurrence-edit.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t \"bloqueia review_required para automatic quando ha pendencia aberta\"`, `npm run build`.

- 2026-05-02: Backend/Recorrências — Fase B.4(b) do PE de modo de revisão: finalização e exclusão de recorrência agora bloqueiam com 422 quando há ocorrências `pending_review` em aberto, retornando a lista de IDs bloqueantes no detalhe em PT-BR. A validação foi centralizada em `recurrence-crud.service.ts` e coberta por teste de integração dedicado. Task `RCREV-S2-10` marcada no PE. Arquivos editados/criados: `opa-finance-api/src/modules/recurrences/recurrence-crud.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts -t \"bloqueia finalize e exclusao quando ha pendencia aberta\"`, `npm run build`.

- 2026-05-02: Backend/Recorrências — Fase B.4(a) do PE de modo de revisão: auditoria expandida para `recurrence_occurrence`, com novos valores em `AuditAction` (`materialize_pending`, `confirm`, `skip`, `fail`), novo `audit_entity_type` para ocorrências, logging de pendências geradas no materialize e logging de confirm/skip com `beforeData`/`afterData` específicos. Teste de integração dedicado passou cobrindo criação de pendência, confirmação e skip; `RCREV-S2-09` marcado no PE. Arquivos editados/criados: `opa-finance-api/src/db/schemas/audit.schema.ts`, `opa-finance-api/src/modules/audit/audit.routes.ts`, `opa-finance-api/src/modules/audit/audit.schemas.ts`, `opa-finance-api/src/modules/audit/audit.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence-materialize.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence-occurrence.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence.audit.ts`, `opa-finance-api/src/modules/recurrences/recurrence.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-audit.test.ts`, `opa-finance-api/src/db/migrations/0023_audit_recurrence_occurrence_actions.sql`, `opa-finance-api/src/db/migrations/0024_audit_recurrence_occurrence_entity.sql`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npx vitest run test/integration/recurrences/recurrences-audit.test.ts`, `npm run build`.

- 2026-05-02: Backend/Recorrências — Fase B.3(d) do PE de modo de revisão: forecast passou a tratar `pending_review` e `skipped` como ocorrências consumidas, renomeando as estruturas de deduplicação e contagem para `consumedDateSet` e `consumedCountByRecurrence`. O teste de forecast foi ajustado para recorrências `review_required` em `by_occurrences`, garantindo que pendências consumidas não voltem para a projeção. Task `RCREV-S2-07` marcada no PE. Arquivos editados/criados: `opa-finance-api/src/modules/recurrences/recurrence-forecast.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-forecast.test.ts`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, integração direcionada `npx vitest run test/integration/recurrences/recurrences-forecast.test.ts`, `npm run build`.

- 2026-05-02: Backend/Recorrências — Fase B.3(c) do PE de modo de revisão: implementado `GET /recurrences/:id/timeline` para combinar ocorrências persistidas e projetadas com sequência cronológica, `canConfirm`/`canSkip`, resumo por status e metadados de janela parcial (`appliedLimit`, `isPartial`, `hasMoreProjected`, `projectionWindowLabel`). Query aceita `limit`, `untilDate` e `includeProjected`. Testes de integração cobrem timeline completa, janela parcial e bloqueio cross-user. Task `RCREV-S2-06` marcada no PE. Arquivos editados/criados: `opa-finance-api/src/modules/recurrences/recurrence.schemas.ts`, `opa-finance-api/src/modules/recurrences/recurrence-timeline.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence.routes.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, integração direcionada `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts`, `npm run build`.

- 2026-05-02: Backend/Recorrências — Fase B.3(b) do PE de modo de revisão: implementado `POST /recurrences/occurrences/:id/skip` para ignorar ocorrências `pending_review` com `expectedVersion`, lock otimista via `UPDATE ... WHERE status = 'pending_review'`, incremento de `version`, preservação do `reviewPayload` e gravação de `metadata.skippedAt`/`metadata.skipReason` quando enviado. Testes de integração cobrem skip sem criação de transação e duplicidade com 409. Task `RCREV-S2-05` marcada no PE. Arquivos editados/criados: `opa-finance-api/src/modules/recurrences/recurrence.schemas.ts`, `opa-finance-api/src/modules/recurrences/recurrence-occurrence.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence.routes.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, integração direcionada `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts`, `npm run build`.

- 2026-05-02: Backend/Recorrências — Fase B.3(a) do PE de modo de revisão: implementado `POST /recurrences/occurrences/:id/confirm` para confirmar ocorrências `pending_review` com `expectedVersion`, lock otimista via `UPDATE ... WHERE status = 'pending_review'`, criação atômica de transação ou transferência dentro de transação de banco, persistência de `reviewPayload` ajustado e metadata de confirmação/ajustes. Erros 409 cobrem concorrência/duplicidade e erros 422 cobrem pendência fora de domínio ou data fora do range permitido. Task `RCREV-S2-04` marcada no PE. Arquivos editados/criados: `opa-finance-api/src/core/errors/problems.ts`, `opa-finance-api/src/modules/recurrences/recurrence.schemas.ts`, `opa-finance-api/src/modules/recurrences/recurrence-occurrence.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence.routes.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, integração direcionada `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts`, `npm run build`.

- 2026-05-02: Backend/Recorrências — Fase B.2(c) do PE de modo de revisão: criação aninhada em `POST /transactions` e `POST /transfers` revisada e coberta por testes para garantir que a primeira ocorrência nasce `materialized` mesmo quando `postingMode = review_required`, preservando o vínculo com `transactionId`/`transferId` e sem `reviewPayload`. Comentários curtos adicionados nos services para documentar o invariante de submit (`RCREV-DEF-26`). Tasks `RCREV-S2-03C` e `RCREV-S2-08` marcadas no PE. Arquivos editados/criados: `opa-finance-api/src/modules/transactions/transaction.service.ts`, `opa-finance-api/src/modules/transfers/transfer.service.ts`, `opa-finance-api/test/integration/transaction/create-transaction.test.ts`, `opa-finance-api/test/integration/transfers/create-transfer.test.ts`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, integração direcionada `npx vitest run test/integration/transaction/create-transaction.test.ts test/integration/transfers/create-transfer.test.ts -t "materializar ocorrência inicial"`, `npm run build`.

- 2026-05-02: Backend/Recorrências — Fase B.2(a) do PE de modo de revisão: materialização passa a respeitar `postingMode`; recorrências `review_required` geram ocorrência `pending_review` com `reviewPayload` e metadata, sem criar transação/transferência; recorrências `automatic` preservam comportamento atual. Contagem de `by_occurrences` agora usa `materialized`, `pending_review` e `skipped` (`RCREV-CNF-10`). `nextOccurrenceDate` avança ao gerar pendência e `lastMaterializedDate` só é atualizado quando há ocorrência `materialized` (`RCREV-CNF-07`). `POST /recurrences/materialize` passa a respeitar o modo por usar o service ajustado (`RCREV-CNF-09`). Testes de integração cobrem geração de pendência sem transação e limite `by_occurrences` com pendências. Tasks `RCREV-S2-03`, `RCREV-S2-03A` e `RCREV-S2-03B` marcadas no PE. Arquivos editados/criados: `opa-finance-api/src/modules/recurrences/recurrence-materialize.service.ts`, `opa-finance-api/test/integration/recurrences/recurrences-critical-rules.test.ts`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, integração direcionada `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts`, `npm run build`.

- 2026-05-02: Backend/Recorrências — Fase B.1(d) do PE de modo de revisão: adicionados schemas Zod de `postingMode` (`automatic`/`review_required`) em recorrências, criação aninhada de transações e transferências; `postingMode` agora é persistido em criação direta, criação aninhada e preservado em payloads `this_and_next`; listagem de recorrências aceita filtro `postingMode`; documentação Swagger inline de transações/transferências inclui o novo campo. Testes unitários de schemas cobrem default `automatic`, modo `review_required` e valor inválido. Task `RCREV-S2-01` marcada no PE. Arquivos editados/criados: `opa-finance-api/src/modules/recurrences/recurrence.schemas.ts`, `opa-finance-api/src/modules/recurrences/recurrence-crud.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence-edit.service.ts`, `opa-finance-api/src/modules/recurrences/recurrence.helpers.ts`, `opa-finance-api/src/modules/transactions/transaction.schemas.ts`, `opa-finance-api/src/modules/transactions/transaction.service.ts`, `opa-finance-api/src/modules/transactions/transaction.routes.ts`, `opa-finance-api/src/modules/transfers/transfer.schemas.ts`, `opa-finance-api/src/modules/transfers/transfer.service.ts`, `opa-finance-api/src/modules/transfers/transfer.routes.ts`, testes unitários de schemas/recurrence service, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, integração direcionada `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts test/integration/recurrences/recurrences-audit.test.ts test/integration/transaction/create-transaction.test.ts test/integration/transfers/create-transfer.test.ts`, `npm run build`.

- 2026-05-02: Backend/Recorrências — Fase B.1(c) do PE de modo de revisão: adicionado índice composto `recurrence_occurrences_recurrence_status_idx` em `(recurrence_id, status)` para suportar agregações e badge de pendências por recorrência sem table scan. Schema Drizzle, migration `0022_recurrence_occurrences_recurrence_status_idx.sql`, journal e snapshot atualizados. Task `RCREV-S2-02D` marcada no PE. Arquivos editados/criados: `opa-finance-api/src/db/schemas/recurrences.schema.ts`, `opa-finance-api/src/db/migrations/0022_recurrence_occurrences_recurrence_status_idx.sql`, `opa-finance-api/src/db/migrations/meta/_journal.json`, `opa-finance-api/src/db/migrations/meta/0022_snapshot.json`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npm run build`. Integração não repetida por ser mudança isolada de índice/schema sem alteração de comportamento de runtime.

- 2026-05-02: Backend/Recorrências — Fase B.1(b) do PE de modo de revisão: adicionadas migrations `0020_recurrence_review_columns.sql` e `0021_recurrence_posting_mode_backfill.sql` para criar enum `recurrence_posting_mode`, coluna `recurrences.posting_mode` com default `automatic`, coluna `recurrence_occurrences.version` com default `1`, coluna `recurrence_occurrences.review_payload` (`jsonb`) e backfill explícito de `posting_mode`. Schema Drizzle atualizado com `postingMode`, `version` e `reviewPayload`; auditoria de recorrência passa a incluir `postingMode`. Tasks `RCREV-S2-02A`, `RCREV-S2-02B`, `RCREV-S2-02C` e `RCREV-S2-02E` marcadas no PE. Arquivos editados/criados: `opa-finance-api/src/db/schemas/recurrences.schema.ts`, `opa-finance-api/src/db/migrations/0020_recurrence_review_columns.sql`, `opa-finance-api/src/db/migrations/0021_recurrence_posting_mode_backfill.sql`, `opa-finance-api/src/db/migrations/meta/_journal.json`, `opa-finance-api/src/db/migrations/meta/0020_snapshot.json`, `opa-finance-api/src/modules/recurrences/recurrence.audit.ts`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, integração direcionada `npx vitest run test/integration/recurrences/recurrences-critical-rules.test.ts test/integration/recurrences/recurrences-forecast.test.ts test/integration/recurrences/recurrences-audit.test.ts`, `npm run build`. Suíte completa de integração interrompida por decisão operacional devido ao custo alto para esta sub-fase.

- 2026-05-02: API/Scripts — Ajuste de encerramento por `Ctrl+C` no `opa-finance-api/scripts/db-tunnel.sh`: adicionado `trap` para `EXIT`, `INT` e `TERM` com função de cleanup que encerra explicitamente o processo SSH aberto pelo script (`SSH_PID`). Corrige cenário em que `Ctrl+C` interrompia apenas o `wait` do script, deixando o túnel ativo em background. Mensagem operacional adicionada para indicar encerramento do PID do túnel.

- 2026-05-02: API/Scripts — Correção no `opa-finance-api/scripts/db-tunnel.sh` para encerramento robusto de túnel quando há múltiplos processos ouvindo a mesma `LOCAL_DB_PORT`. O script deixou de tratar PID como string única e passou a encerrar todos os PIDs retornados por `lsof` via `xargs -r kill` nos fluxos de limpeza de zombie, `--force` e confirmação interativa. Mensagens de status também foram ajustadas para `PID(s)`. Motivo: evitar falha de `kill "$PID"` quando o retorno do `lsof` contém múltiplas linhas. Validação: lint pendente.

- 2026-05-02: Backend/Recorrências — Fase B.1(a) do PE de modo de revisão: adicionada migration manual `0019_recurrence_occurrence_review_statuses.sql` para expandir o enum Postgres `recurrence_occurrence_status` com `pending_review` e `skipped` usando `ALTER TYPE ... ADD VALUE IF NOT EXISTS`, isolada de qualquer uso dos novos valores conforme `RCREV-CNF-08`/`RCREV-DEF-29`. Journal do Drizzle atualizado e task `RCREV-S2-02` marcada no PE. Arquivos editados/criados: `opa-finance-api/src/db/migrations/0019_recurrence_occurrence_review_statuses.sql`, `opa-finance-api/src/db/migrations/meta/_journal.json`, `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`, `docs-hidden/changes.md`. Validação: `npm run lint`, `npm run test:unit`, `npm run test:integration`, `npm run build` no `opa-finance-api`.

- 2026-05-02: Docs/Melhorias — Adição da seção "Melhorias futuras (v2 e além)" no fim do PE `PE_recorrencias_modo_revisao_timeline_mvp.md`. Consolida itens conscientemente fora do MVP em 7 grupos temáticos: (1) Notificações e lembretes ativos (banner in-app, `notifyDaysBefore`, distinção visual de urgência, push/email — origem `RCREV-DEF-35`); (2) Recuperação de erros `failed` (`failedAttempts`, cap, rota retry, métricas — origem `RCREV-CNF-12`/`DEF-06/17/32`); (3) Visibilidade global de pendências (endpoint global, tela inbox, ações em massa — origem `DEF-27`); (4) Recuperação de ações do usuário (reabertura de skipped, aviso pré-skip — origem `DEF-03/11`); (5) Limpeza e manutenção automática (job de cleanup, política configurável — origem `DEF-33`); (6) Navegação e UX avançada (página dedicada com deep-link, drawer, filtros na timeline — origem `DEF-13`); (7) Modelo de domínio expandido (modo `notify_only`, entidade `Installment` separada, histórico de versões — origem `DEF-10/12/04`); (8) Telemetria e observabilidade. Cada bloco lista motivação, escopo proposto, gatilho para priorização e referência cruzada com a DEF/CNF que originou o item. Arquivos editados: `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: revisão visual; nenhum código alterado (modo planejamento).

- 2026-05-02: Docs/Melhorias — Adição de `RCREV-DEF-35` no PE `PE_recorrencias_modo_revisao_timeline_mvp.md` formalizando estratégia de "lembrete passivo" para casos de uso recorrentes (aluguel, mensalidade, serviço). MVP cobre via badge na listagem (`RCREV-DEF-27`); sem notificação ativa (push/email/SMS/banner/antecedência). Objetivo expandido para citar o caso "lembrar de pagar" como exemplo de uso. Seção "Fora deste passo" recebeu 3 itens explícitos: notificação ativa, campo `notifyDaysBefore` e distinção visual de urgência — todos planejados para v2. Arquivos editados: `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: revisão visual; nenhum código alterado (modo planejamento).

- 2026-05-02: Docs/Melhorias — Análise final de prontidão e fechamento de 13 lacunas no PE `PE_recorrencias_modo_revisao_timeline_mvp.md` antes da implementação. Adicionadas: (1) 6 novas definições fechadas `RCREV-DEF-29` a `34`: ordem técnica das 3 migrations + estratégia drizzle-kit (SQL bruto manual para `ALTER TYPE`), mensagens de erro em PT-BR para 4xx críticos, modal de edição na confirmação de pendência, `failedAttempts` fora do MVP, cleanup de pendências antigas fora do MVP, formato padrão de `metadata` com `adjustments`. (2) 2 novas tasks `RCREV-S2-02D` (índice composto `(recurrence_id, status)` para badge de pendências) e `RCREV-S2-02E` (sequência das migrations). (3) Sub-tasks adicionadas em `RCREV-S2-04/05/06` (auth preHandler, ownership, mensagens PT-BR, gravação de `metadata.adjustments`), `RCREV-S2-07` (validar branch `endOccurrences = null`), `RCREV-S2-10/11/12` (referência explícita ao `recurrence-edit-service`), `RCREV-S2-13` (SQL de diagnóstico no doc operacional), `RCREV-S3-06` (criar `ConfirmRecurrenceOccurrenceModal`). (4) Seção "Fora deste passo" expandida com 3 itens (cleanup, retry de failed, endpoint global). (5) 4 novos testes `RCREV-S4-21` a `24`. (6) 5 novos critérios de aceite. (7) 5 novos riscos. (8) 2 novos itens de validação manual (mobile do modal de detalhe e modal de confirmação). (9) Checklists E1/E6 expandidos. (10) Matriz E1 atualizada para citar 3 migrations + índice. Arquivos editados: `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: revisão visual; nenhum código foi alterado (modo planejamento).

- 2026-05-02: Docs/Melhorias — Fechamento das 5 últimas definições pendentes em `PE_recorrencias_modo_revisao_timeline_mvp.md`. Decisões: `RCREV-DEF-24` `sequence` conta todos os status (skipped ocupa posição); `RCREV-DEF-25` `occurrenceDate` no confirm aceita `[startDate, endDate]` quando definidos, ou `>= startDate` com cap em `now() + 1 ano` quando série aberta; `RCREV-DEF-26` primeira ocorrência em criação aninhada sempre `materialized` independente de `postingMode`; `RCREV-DEF-27` badge/contador de pendências por linha na listagem (sem endpoint global no MVP); `RCREV-DEF-28` filtro de conta no forecast usa `recurrences.accountId/fromAccountId/toAccountId` (não `reviewPayload`). Seção "Definições pendentes para implementação" zerada — todas as 28 DEF estão fechadas. Arquivos editados: `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: revisão visual; nenhum código foi alterado (modo planejamento).

- 2026-05-02: Docs/Melhorias — Revisão de bugs/incoerências/lacunas no PE `PE_recorrencias_modo_revisao_timeline_mvp.md`. Após validar bugs no código real (`recurrence-materialize.service.ts:231` e `recurrence-forecast.service.ts:132/150` filtrando só `materialized`), foram adicionados: (1) 3 novos conflitos `RCREV-CNF-10` (contagem de `by_occurrences` no materialize ignora novos status — bug crítico que geraria pendências infinitas), `RCREV-CNF-11` (forecast duplica datas de pendências por filtro incompleto), `RCREV-CNF-12` (`failed` é terminal por índice idempotente — aceite consciente no MVP). (2) 5 novas definições pendentes `RCREV-DEF-24` a `28` com propostas: cálculo de `sequence` em séries com gaps, range para `occurrenceDate` no confirm (proposta ±31 dias), status da primeira ocorrência em criação aninhada (proposta `materialized` sempre), visibilidade global de pendências (proposta badge na listagem para MVP), filtro de conta no forecast (proposta usar conta da regra-mãe, não `reviewPayload`). (3) Sub-tasks explícitas em `RCREV-S2-03` (atualizar query de contagem com `inArray`) e `RCREV-S2-07` (renomear variáveis para `consumedDateSet`/`consumedCountByRecurrence`). (4) Novas tasks `RCREV-S2-03C` (primeira ocorrência aninhada) e `RCREV-S2-13` (documentar `failed` terminal). (5) 2 novas tasks frontend `RCREV-S3-10/11`. (6) 5 novos testes `RCREV-S4-16` a `20`. (7) 5 novos critérios de aceite. (8) Checklists E2/E3/E4/E5/E6 expandidos. (9) 6 novos riscos. Itens descartados como já cobertos: INC-01 (CNF-07), INC-02 (S2-02C), INC-03 (DEF-20). Arquivos editados: `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: `grep` confirmou bugs no código; nenhum código foi alterado (modo planejamento).

- 2026-05-01: Docs/Melhorias — Adição de bloco "Avaliação" em todas as 14 definições já fechadas (`RCREV-DEF-01` a `RCREV-DEF-14`) no PE `PE_recorrencias_modo_revisao_timeline_mvp.md`. Cada avaliação contém: **Recomendação** (manter/ajustar), **Alternativa avaliada** (com justificativa de por que não foi escolhida), **Trade-off** explícito, **Risco se mudar depois** quando aplicável, e **Pendente correlacionado** apontando para outras DEF/CNF. Pontos críticos destacados: DEF-02 sugere bloquear `originType`/`recurrenceId` no payload de confirm; DEF-05 alerta para sub-task implícita de detalhe de transferência por `transferId`; DEF-06 propõe `failedAttempts` + cap configurável; DEF-08 documenta divisão clara de responsabilidades entre `reviewPayload` e `metadata`; DEF-09 dá exemplos concretos de `projectionWindowLabel`; DEF-12 sugere etiqueta "Parcela X de N" no modal de detalhe; DEF-14 recomenda documentar rollback da migration. Arquivos editados: `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: revisão visual, sem código alterado (modo planejamento).

- 2026-05-01: Docs/Melhorias — Revisão e expansão do PE `PE_recorrencias_modo_revisao_timeline_mvp.md` após cruzamento com o estado atual do código (backend e frontend). Adições: (1) seção "Contexto atual observado" expandida com 4 novos fatos (descrição correta da divisão do form de recorrência no front, restrição de `ALTER TYPE ADD VALUE`, vinculação transação↔ocorrência via `recurrence_occurrences.transactionId` + papel de `resolveSubmitOccurrence()`, filtros atuais do forecast, escopo atual de `AuditAction`). (2) 4 novos conflitos `RCREV-CNF-06` a `RCREV-CNF-09` (concorrência no confirm, avanço de `nextOccurrenceDate` em pending, migration de enum, endpoint admin materialize com review). (3) 9 novas definições pendentes `RCREV-DEF-15` a `RCREV-DEF-23` com propostas (edição de `postingMode`, finalização/exclusão com pendências, reprocessamento de `failed`, edição `single` em pending, tipo de `reviewPayload`, regras de `canConfirm`/`canSkip`, idempotência via unique constraint, expansão de `AuditAction`, semântica de `sequence`/`hasMoreProjected`). (4) Modelo de domínio agora inclui coluna `version` para lock otimista e campo `originalScheduledDate` no `reviewPayload`. (5) Contratos de `confirm`/`skip` exigem `expectedVersion` com erros 409/422/404 documentados. (6) 5 critérios de aceite adicionais. (7) 6 novas tasks no Sprint B (`RCREV-S2-02B/02C/03A/03B/10/11/12`), 1 no Sprint C (`RCREV-S3-09`) e 7 no Sprint D (`RCREV-S4-09` a `15`). (8) Checklists E1/E2/E3/E7 expandidos com novos casos (concorrência, idempotência, rollback, listagem de transações, `nextOccurrenceDate`). (9) Plano de validação manual detalhado: item 7 dividido em 7a/7b/7c, novos itens 10 e 11. (10) Riscos atualizados com 7 novos itens. (11) Critérios de engenharia ganham seções "migrations" e "concorrência". Arquivos editados: `docs-hidden/melhorias/PE_recorrencias_modo_revisao_timeline_mvp.md`. Validação: revisão visual da estrutura do PE; nenhum código foi alterado (modo planejamento).

- 2026-04-30: Frontend/Transactions — Correção de tipo em `use-category-tree-interaction.ts`: parâmetro `setValue` alterado de `(name: string, ...)` para `(name: 'subcategoryId', ...)`, refletindo o único campo que o hook realmente escreve. Resolve incompatibilidade com `UseFormSetValue<T>` do React Hook Form (TS2322). Arquivo editado: `use-category-tree-interaction.ts`.

- 2026-04-29: Frontend/Transactions — Extração de `TransactionAccountField` (componentização do campo Conta): select de conta extraído dos três modais para `src/features/transactions/components/transaction-account-field.tsx`. Props: `id`, `label` (configurável — "Conta", "Conta de origem", "Conta de destino"), `fieldName?` (default `"accountId"`, aceita `"fromAccountId"` e `"toAccountId"` para o transfer modal), `control`, `errors`, `accounts`, `isOpen`, `onOpenChange`, `tabIndex?`. Comportamentos preservados: `__none__` para estado vazio, `onEscapeKeyDown` (previne default, stopPropagation, fecha o select), `aria-invalid` baseado no erro do campo, mensagem de erro abaixo. `Controller` removido dos imports de create/edit modais (ficou sem uso). `Select*` e `Label` removidos do edit modal (idem). Transfer modal: `Controller` também removido. Todos os três modais usando `TransactionAccountField`. Teste unitário criado: renderização de label padrão, labels personalizadas (origem/destino) e acessibilidade do combobox. Arquivos criados: `transaction-account-field.tsx`, `test/unit/.../transaction-account-field.component.test.tsx`. Arquivos editados: `transactions-create-modal.tsx`, `transactions-edit-modal.tsx`, `transactions-transfer-modal.tsx`. Validação: lint ✅ (0 erros), testes (14/14) ✅.

- 2026-04-29: Frontend/Transactions — Etapa 3: autocomplete de descrição no edit modal + extração de `TransactionDescriptionField`: (1) `use-transactions-create-support.ts` atualizado para habilitar sugestões de descrição quando `isEditOpen` — condição `isCreateOpen` expandida para `(isCreateOpen || isEditOpen)` nas duas queries; (2) edit modal recebe estados de autocomplete (`isDescriptionSuggestionsOpen`, `isDescriptionFocused`, `activeSuggestionIndex`), ref `editDescriptionInputRef`, debounce de description, e o `useEffect` de reset de índice — Escape também fecha sugestões agora; (3) `TransactionsDescriptionAutocomplete` ganhou prop `id?: string` (default `"transaction-description"`) para permitir IDs distintos entre create e edit; (4) `TransactionDescriptionField` extraído encapsulando Label + autocomplete + erro — usado em create (id `transaction-description`) e edit (id `transaction-edit-description`); (5) `Input` removido do edit modal (não mais usado diretamente). Arquivos criados: `transaction-description-field.tsx`, teste unitário correspondente. Arquivos editados: `use-transactions-create-support.ts`, `transactions-description-autocomplete.tsx`, `transactions-create-modal.tsx`, `transactions-edit-modal.tsx`. Validação: lint ✅ (0 erros), testes (33/33) ✅.

- 2026-04-28: Frontend/Transactions — Extração de `TransactionCategoryField` + `useCategoryTreeInteraction` (Etapa 4 do plano de componentização): ~260 linhas de handlers duplicados (findVisibleSiblingOption, focusXxxOption, getXxxValue, handleXxxOpenChange, handleXxxSelectValueChange, handleXxxSearchKeyDown, handleXxxItemKeyDown) unificadas em um hook. `TransactionCategoryField` recebe estado e callbacks como props, encapsulando todo o JSX do category tree (~80 linhas) incluindo busca, criação inline e navegação por teclado. Edit modal passa `onSearchChange` com RAF para manter foco no input (comportamento específico preservado). Props `disabled`/`disabledMessage` cobrem o caso de transferências no edit. Constantes (`CATEGORY_TREE_NONE/CREATE/CREATE_SUB`) e tipo `CategoryTreeOption` movidos para arquivos próprios, removidos dos modais. `KeyboardEventHandler` removido dos imports de ambos os modais. Etapa 3 (description field) postergada — sem reaproveitamento real hoje. Arquivos criados: `hooks/use-category-tree-interaction.ts`, `components/transaction-category-field.tsx`, `test/unit/.../transaction-category-field.component.test.tsx`. Arquivos editados: `transactions-create-modal.tsx`, `transactions-edit-modal.tsx`. Validação: lint ✅ (0 erros), testes (27/27) ✅.

- 2026-04-28: Frontend/Transactions — Extração de campos simples (Etapa 2 do plano de componentização): `TransactionTypeField` (create + edit), `TransactionNotesField` (create + edit), `TransactionDateField` (create + edit + transfer). `TransactionDateField` encapsula toda a lógica mobile (showPicker, inputMode none, prevenção de keydown/paste) e aceita `dateRef?` opcional para controle de foco externo (create modal). No transfer modal, os 4 `useCallback` handlers de data (`handleDateFocus/Click/KeyDown/Paste`) foram removidos por ficarem redundantes após a extração; imports React correspondentes (`ClipboardEvent`, `FocusEvent`, `KeyboardEvent`, `MouseEvent`) também removidos. Arquivos criados: `transaction-type-field.tsx`, `transaction-notes-field.tsx`, `transaction-date-field.tsx` + testes unitários para cada. Arquivos editados: `transactions-create-modal.tsx`, `transactions-edit-modal.tsx`, `transactions-transfer-modal.tsx`. Validação: lint ✅ (0 erros), testes (24/24) ✅.

- 2026-04-28: Frontend/Transactions — Extração de `TransactionAmountField` (Etapa 1 do plano de componentização): campo Valor extraído dos três modais (create, edit, transfer) para `src/features/transactions/components/transaction-amount-field.tsx`. Props: `id`, `control`, `errors`, `amountRef?`, `onAmountChange`, `clearAmountError`, `onAmountBlur?` (só create), `tabIndex?`, `inputMode?` (default `'decimal'`). Edit e transfer passam `inputMode="numeric"` para preservar comportamento original. Lógica da tecla `=` e formatação unificadas. Testes unitários criados: renderização, onChange, tecla `=` + clearAmountError, onAmountBlur, ausência de onBlur quando prop não fornecida. Arquivos criados: `src/features/transactions/components/transaction-amount-field.tsx`, `test/unit/features/transactions/components/transaction-amount-field.component.test.tsx`. Arquivos editados: `transactions-create-modal.tsx`, `transactions-edit-modal.tsx`, `transactions-transfer-modal.tsx`. Validação: lint ✅ (0 erros), testes (16/16) ✅.

- 2026-04-28: Frontend/Transactions — Remoção de `TransactionsEditModalLegacy` (Etapa 0 do plano de componentização): tipo `TransactionsEditModalLegacyProps`, tipo union `TransactionsEditModalProps`, função wrapper e função `TransactionsEditModalLegacy` removidos de `transactions-edit-modal.tsx`. `TransactionsEditModal` agora exporta diretamente a implementação autônoma (tipo interno renomeado de `TransactionsEditModalAutonomousProps` para `TransactionsEditModalProps`). Imports de `ClipboardEvent`, `FocusEvent`, `FormEvent`, `KeyboardEvent`, `MouseEvent` removidos (usados apenas pelo legacy). Testes reescritos para o componente real: não renderiza quando `isOpen=false`, renderiza título, fecha pelo botão Cancelar, fecha pelo backdrop, bloqueia categoria em transferências. Arquivos editados: `src/features/transactions/components/transactions-edit-modal.tsx`, `test/unit/features/transactions/components/transactions-edit-modal.component.test.tsx`. Validação: lint ✅ (0 erros), testes (10/10) ✅, integração create/edit ✅.

- 2026-04-27: Docs/CLAUDE.md — Adição de 3 regras ausentes identificadas em notes.md: (1) descrever e confirmar plano antes de implementar melhorias + trabalhar step by step; (2) não alterar padrões sem confirmação prévia; (3) URLs úteis de produção (backend, health, swagger). Branch após merge deve ser apagada (explicitado). Arquivo editado: `CLAUDE.md`.

- 2026-04-27: Docs/CLAUDE.md — Reestruturação para reduzir consumo de tokens: comandos extraídos para `docs-hidden/agents/COMMANDS.md`, detalhes arquiteturais verbose removidos (entry point verboso, lista de arquivos de tabela, detalhes do Vitest config, árvore de diretórios detalhada). CLAUDE.md reduzido de 202 → 104 linhas (10KB → 5KB, ~53% menor). Arquivos criados: `docs-hidden/agents/COMMANDS.md`. Arquivos editados: `CLAUDE.md`.

- 2026-04-27: Frontend/Transactions — Remoção de `TransactionsCreateModalLegacy`: componente legacy que aceitava `children` e seus tipos associados (`TransactionsCreateModalLegacyProps`, union `TransactionsCreateModalProps`, função wrapper) foram removidos de `transactions-create-modal.tsx`. `TransactionsCreateModal` agora exporta diretamente a implementação autônoma (props renomeadas internamente de `TransactionsCreateModalAutonomousProps` para `TransactionsCreateModalProps`). Testes unitários reescritos para validar o componente real sem `children`: verifica não-renderização quando `isOpen=false`, título "Nova transação" quando `isOpen=true`, presença dos campos principais (Conta, Data, Categoria/Subcategoria, Valor, Descrição) e chamada de `onClose` ao clicar no backdrop. Arquivos editados: `src/features/transactions/components/transactions-create-modal.tsx`, `test/unit/features/transactions/components/transactions-create-modal.component.test.tsx`. Validação: `npm run lint` ✅ (0 erros), `npm run build` ✅, testes unitários (4/4) ✅, testes de integração (create/edit) ✅.

- 2026-04-27: Frontend/Recorrências — Extração do modal de criação/edição de recorrência: bloco JSX inline de ~430 linhas em `recurrences-page.tsx` extraído para `recurrence-form-modal.tsx` co-localizado na feature. Orquestração (estado, queries, handlers, form) permanece no page; modal recebe tudo via props explicitamente tipadas (23 props). `selectedCategoryType` tipado como `Category['type'] | null` para compatibilidade com `formatDerivedTransactionTypeLabel`. Imports exclusivos do modal removidos do page. Comportamento, regras de negócio, acessibilidade e fluxos de conflito 409 preservados integralmente. Arquivos criados: `src/features/recurrences/components/recurrence-form-modal.tsx`. Arquivos editados: `src/features/recurrences/components/recurrences-page.tsx`. Validação: `npm run lint` ✅, `npm run build` ✅.

- 2026-04-27: Frontend/Auth — Guarda de ambiente para `resetToken` em recuperação de senha: `forgot-password.tsx` exibia o token de teste sempre que a API retornasse `resetToken`, sem verificação de ambiente. Adicionada flag `VITE_SHOW_RESET_TOKEN === 'true'` como condição adicional — token e link só aparecem quando a flag está explicitamente ativa. Default seguro: token nunca exibido. Testes ajustados: caso padrão agora verifica ausência do token; novo caso verifica exibição com `vi.stubEnv('VITE_SHOW_RESET_TOKEN', 'true')`; `vi.unstubAllEnvs()` adicionado no `afterEach`. Arquivos editados: `src/routes/forgot-password.tsx`, `test/integration/features/auth/auth.forms.integration.test.tsx`. Validação: `npm run lint` ✅, `npm run build` ✅, testes ✅ (4/4).

- 2026-04-27: Frontend/Auth — Auth store reativo com `useSyncExternalStore`: `auth.store.ts` não tinha mecanismo de notificação — `updateUser`/`logout` mutavam estado mas React não re-renderizava. Adicionados `listeners` (Set interno), `notify()`, `subscribe()` e `getSnapshot()` ao store; `setAuth`, `updateUser` e `logout` passam a chamar `notify()`. `useAuth.ts` atualizado para usar `useSyncExternalStore(subscribe, getSnapshot)` em vez de `getUser()` direto. `Header.tsx` atualizado para usar `useAuth()` em vez de `getUser()` — nome do usuário agora atualiza imediatamente após update de perfil sem refresh. Arquivos editados: `src/features/auth/auth.store.ts`, `src/features/auth/useAuth.ts`, `src/components/app/Header.tsx`. Validação: `npm run lint` ✅, `npm run build` ✅.

- 2026-04-27: Frontend/Auth — Correção de race condition no bootstrap de sessão: o interceptor de request sobrescrevia o header `Authorization` mesmo quando já definido pelo chamador, causando uso do token antigo na chamada `/auth/me` após refresh no `beforeLoad` da rota `/app`. Corrigido adicionando `&& !config.headers.Authorization` na condição do interceptor — se o header já estiver presente, o interceptor não toca. Arquivo editado: `opa-finance-front/src/lib/api.interceptors.ts`. Validação: `npm run lint` ✅, `npm run build` ✅.

- 2026-04-25: Frontend/Transactions — Extração de `invalidateTransactionDependents` em `transactions.api.ts`: as três mutations (`useCreateTransaction`, `useUpdateTransaction`, `useDeleteTransaction`) repetiam o mesmo bloco de 4 `invalidateQueries`. Bloco extraído para `invalidateTransactionDependents(queryClient: QueryClient)`, exportada do arquivo. Invalidações individuais de item (`transactionKey(id)`) mantidas inline por serem específicas da variável da mutation. `transfers.api.ts` não alterado (usa subconjunto intencional). Arquivos editados: `opa-finance-front/src/features/transactions/transactions.api.ts`. Validação: `npm run lint` ✅, `npm run build` ✅.

- 2026-04-25: Backend/Accounts — Índice único parcial em `accounts` declarado no schema Drizzle: o índice `accounts_user_primary_unique ON accounts (user_id) WHERE is_primary` existia no banco (criado manualmente em `0003_jittery_frank_castle.sql`) mas não estava declarado no schema nem no snapshot Drizzle. Adicionado `uniqueIndex("accounts_user_primary_unique").on(table.userId).where(sql\`${table.isPrimary}\`)` em `accounts.schema.ts`; executado`db:generate` (gerou `0018_cuddly_obadiah_stane.sql`) e adicionado`IF NOT EXISTS` manualmente para idempotência em bancos que já possuem o índice. Arquivos editados: `opa-finance-api/src/db/schemas/accounts.schema.ts`,`opa-finance-api/src/db/migrations/0018_cuddly_obadiah_stane.sql` (novo). Validação: `npm run lint` ✅, `npm run build` ✅.

- 2026-04-25: Backend/ErrorHandler — Logger do error handler padronizado com `app.log`: substituído `console.error("❌ ERROR:", error)` por `app.log.error`/`app.log.warn` baseado no status HTTP inferido do erro (`HttpProblem.status`, `ZodError` → 400, `FastifyRouteError.statusCode` → fallback 500). Erros 4xx (cliente) passam a logar em `warn`; 5xx (servidor) em `error`. Inclui `url: req.url` no objeto estruturado. Arquivos editados: `opa-finance-api/src/core/middlewares/handle-route-error.ts`. Validação: `npm run lint` ✅, `npm run build` ✅.

- 2026-04-25: Backend/Server — Logger habilitado em dev com nível `warn`: `server.ts` usava `logger: false` fora de produção, ocultando warnings do Fastify. Alterado para `logger: { level: "warn" }` em dev/test e `logger: { level: LOG_LEVEL ?? "info" }` em produção. Arquivos editados: `opa-finance-api/src/server.ts`. Validação: `npm run lint` ✅, `npm run build` ✅.

- 2026-04-25: Frontend/Lib — Avaliador de expressão aritmética extraído de `utils.ts` para `lib/expression.ts`: `utils.ts` continha 186 linhas de lógica de parsing (`evaluateArithmeticExpression`, `tokenizeExpression`, `parseExpressionTokens`, `parseExpressionNumber`, `sanitizeExpressionInput`). Tudo extraído para `src/lib/expression.ts` com exports públicos para testabilidade. Cópia local duplicada em `transactions.helpers.ts` (avaliador flat sem parênteses + `parseNumberInput`) eliminada — substituída pelas funções da lib (superset: suporta parênteses). `utils.ts` importa `evaluateArithmeticExpression` de `./expression`. Dois import sites de `sanitizeExpressionInput` (`transactions-transfer-modal.tsx`, `use-transaction-form.ts`) atualizados para `@/lib/expression`. Adicionado `test/unit/lib/expression.test.ts` com 38 testes cobrindo `parseExpressionNumber`, `tokenizeExpression`, `parseExpressionTokens`, `evaluateArithmeticExpression` e `sanitizeExpressionInput`. Arquivos criados: `opa-finance-front/src/lib/expression.ts`, `opa-finance-front/test/unit/lib/expression.test.ts`. Arquivos editados: `opa-finance-front/src/lib/utils.ts`, `opa-finance-front/src/features/transactions/model/transactions.helpers.ts`, `opa-finance-front/src/features/transactions/components/transactions-transfer-modal.tsx`, `opa-finance-front/src/features/transactions/hooks/use-transaction-form.ts`. Validação: `npm run lint` ✅, `npm run build` ✅, `npx vitest run test/unit/lib/expression.test.ts` ✅ (38/38).

- 2026-04-25: Backend/Transactions — Correção de update de `notes` em transferências e transações: `data.notes ?? tx.notes` tratava `null` e `undefined` como equivalentes; quando o usuário enviava `notes: null` para apagar a nota, o valor antigo era retido. Corrigido para `"notes" in data ? data.notes : tx.notes` nos dois paths de update (`transferId` branch e path normal). O schema já permitia `notes: z.string().nullable().optional()`. Arquivos editados: `opa-finance-api/src/modules/transactions/transaction.service.ts` (linhas 532 e 637). Validação: `npm run lint` ✅, `npm run build` ✅.

- 2026-04-25: Backend/Recorrências — Split do `RecurrenceService` (R0-R7): `recurrence.service.ts` (2079 linhas, classe monolítica) decomposto em serviços especializados agrupados por caso de uso. Criados: `recurrence.helpers.ts` (funções puras), `recurrence.validators.ts` (ownership/DB helpers), `recurrence.audit.ts` (log best-effort), `recurrence-crud.service.ts` (create/list/getOne/finalize/remove), `recurrence-edit.service.ts` (update/editByScope), `recurrence-materialize.service.ts` (materialize), `recurrence-forecast.service.ts` (forecast). `recurrence.service.ts` convertido em fachada de **82 linhas**. Nenhuma alteração em `recurrence.routes.ts`, `recurrence.schemas.ts`, `recurrence-daily-job.ts`. Validação: `npm run lint` ✅ (0 erros), `npm run build` ✅, `npm run test:unit` ✅ (9/50), `npm run test:integration` ✅ (44/217).

- 2026-04-25: Frontend/Transactions (Split R7 — limpeza final): `transactions-page.tsx` foi convertida em entrypoint fino de composição e a implementação integral foi movida para `opa-finance-front/src/features/transactions/components/transactions-page-content.tsx`, mantendo comportamento e contratos. Arquivos criados/editados nesta sprint: `opa-finance-front/src/features/transactions/components/transactions-page-content.tsx` (novo), `opa-finance-front/src/features/transactions/components/transactions-page.tsx`, `opa-finance-front/src/features/transactions/hooks/use-transactions-ui-state.ts`, `docs-hidden/melhorias/PE_transactions_page_split_front.md`, `docs-hidden/changes.md`. Arquivos removidos: nenhum. Resumo do que saiu da page e para onde foi: toda a implementação da tela foi extraída para `transactions-page-content.tsx`; `transactions-page.tsx` ficou com 15 linhas e apenas orquestração de alto nível. `use-transactions-ui-state.ts` foi reduzido removendo estado/refs zumbis já migrados para modais (deleção/detalhes). Impacto em contratos/UX/testes: nenhum (refator puro). Regressão da sprint validada por `npm run test` ✅ (82 arquivos/243 testes), com `npm run lint` ✅ e `npm run build` ✅. Linhas atuais de `transactions-page.tsx`: 15.

- 2026-04-25: Frontend/Transactions (Encerramento consolidado R0-R7): melhoria `PE_transactions_page_split_front.md` concluída. Resultado consolidado: `transactions-page.tsx` abaixo da meta (<500), modais isolados por responsabilidade (create/edit/transfer/delete/bulk/details), autocomplete de descrição extraído, e `use-transactions-ui-state.ts` reduzido a estado legítimo de página. Contratos de API/search params/atalhos/foco preservados durante as sprints. Evidências finais da rodada: `npm run lint` ✅, `npm run build` ✅, `npm run test` ✅. Entradas por sprint registradas no `changes.md` (R0 a R7) e registro de execução preenchido no PE.

- 2026-04-25: Frontend/Transactions (Split R6 — autocomplete de descrição): extraído o autocomplete de descrição para componente dedicado da feature. Arquivos criados/ editados nesta sprint: `opa-finance-front/src/features/transactions/components/transactions-description-autocomplete.tsx` (novo), `opa-finance-front/src/features/transactions/components/transactions-create-modal.tsx`, `opa-finance-front/src/features/transactions/index.ts`, `opa-finance-front/test/unit/features/transactions/components/transactions-description-autocomplete.component.test.tsx` (novo), `docs-hidden/melhorias/PE_transactions_page_split_front.md`, `docs-hidden/changes.md`. Arquivos removidos: nenhum. Resumo do que saiu da page e para onde foi: a lógica de autocomplete já estava fora da page (dentro do create modal) e foi desacoplada para `transactions-description-autocomplete.tsx`, reduzindo acoplamento do modal e preparando a etapa final de limpeza sem alterar o comportamento. Impacto em contratos/UX/testes: nenhum em contratos de API ou UX (refator puro com comportamento preservado). Checklist R6 validada por testes automatizados (`npx vitest run test/unit/features/transactions/components/transactions-description-autocomplete.component.test.tsx test/integration/features/transactions/transactions.create-edit.integration.test.tsx`) cobrindo sugestões, navegação por teclado e `Escape` sem submit. Lint ✅, build ✅. Linhas atuais de `transactions-page.tsx`: 2757.

- 2026-04-25: Frontend/Transactions (Split R5 — modal de transferência): extração/consolidação do fluxo de transferência para o modal+hook da feature com suporte explícito a recorrência de transferência. Arquivos editados nesta sprint: `opa-finance-front/src/features/transactions/components/transactions-transfer-modal.tsx`, `opa-finance-front/src/features/transactions/hooks/use-transfer-form.ts`, `opa-finance-front/src/features/transactions/mappers/transfer-payload.mapper.ts`, `opa-finance-front/src/features/transactions/components/transactions-page.tsx`, `opa-finance-front/test/unit/features/transactions/hooks/use-transfer-form.test.ts`, `opa-finance-front/test/unit/features/transactions/components/transactions-transfer-modal.component.test.tsx`, `opa-finance-front/test/integration/features/transactions/transactions.transfer.integration.test.tsx`, `docs-hidden/melhorias/PE_transactions_page_split_front.md`, `docs-hidden/changes.md`. Arquivos criados/removidos: nenhum. Resumo: o modal de transferência passou a conter a seção de recorrência (agenda/término), e o `use-transfer-form` passou a montar payload `originType='transfer'`, criar recorrência após criação da transferência e executar rollback best-effort (deleção das transações criadas) em falha de recorrência. Bloqueio same-account preservado via schema. Impacto em contratos/UX: nenhum em contratos de API; UX preservada com adição do subfluxo de recorrência no modal de transferência conforme checklist da sprint. Testes da sprint: `npx vitest run test/unit/features/transactions/hooks/use-transfer-form.test.ts test/unit/features/transactions/components/transactions-transfer-modal.component.test.tsx test/integration/features/transactions/transactions.transfer.integration.test.tsx` ✅. Lint ✅, build ✅. Linhas atuais de `transactions-page.tsx`: 2757.

- 2026-04-25: Frontend/Transactions (Split R4 — modal de edit): sprint R4 aplicada com foco no fluxo de edição. Arquivos editados: `opa-finance-front/src/features/transactions/components/transactions-edit-modal.tsx`, `opa-finance-front/src/features/transactions/components/transactions-page.tsx`, `opa-finance-front/test/unit/features/transactions/components/transactions-edit-modal.component.test.tsx`, `opa-finance-front/test/unit/features/transactions/mappers/transaction-payload.mapper.test.ts`, `docs-hidden/melhorias/PE_transactions_page_split_front.md`, `docs-hidden/changes.md`. Arquivos criados/removidos: nenhum. Resumo: `transactions-page` passou a usar `editTransactionForm.handleTransactionAmountChange` (fonte de verdade do `useTransactionForm({ mode: 'edit' })`) no `transactions-edit-modal`; no modal de edição, categoria/subcategoria ficam bloqueadas para transações vinculadas a transferência, com mensagem explícita de bloqueio; removida prop redundante `isEditFormSubmitting`. Validação de `notes = null` no update adicionada no mapper test. O que saiu da page e para onde foi: nesta sprint, não houve nova extração de bloco visual, mas houve redução de acoplamento do fluxo edit ao hook dedicado de edição. Impacto em contratos/UX: nenhum (refator puro), mantendo bloqueios esperados de transferência e sem alteração de API. Testes da sprint: `npx vitest run test/unit/features/transactions/components/transactions-edit-modal.component.test.tsx test/unit/features/transactions/mappers/transaction-payload.mapper.test.ts test/integration/features/transactions/transactions.create-edit.integration.test.tsx test/integration/features/transactions/transactions.transfer.integration.test.tsx` ✅. Lint ✅, build ✅. Linhas atuais de `transactions-page.tsx`: 2712.

- 2026-04-24: Frontend/Transactions (Split R3 — create modal + inline category flow): extraída da `transactions-page.tsx` toda a UI de criação para `opa-finance-front/src/features/transactions/components/transactions-create-modal.tsx` (novo `TransactionsCreateModalForm`) e criado `opa-finance-front/src/features/transactions/components/transactions-inline-category-flow.tsx` para os modais inline de categoria/subcategoria. `use-transaction-form.ts` foi estendido para `mode: 'create' | 'edit'` (TXPS-DEF-01) e a page passou a consumir explicitamente `mode: 'create'` no fluxo de criação e `mode: 'edit'` no fluxo de edição, sem alterar contratos de API. Arquivos editados/criados: `opa-finance-front/src/features/transactions/components/transactions-create-modal.tsx`, `opa-finance-front/src/features/transactions/components/transactions-inline-category-flow.tsx` (novo), `opa-finance-front/src/features/transactions/components/transactions-page.tsx`, `opa-finance-front/src/features/transactions/hooks/use-transaction-form.ts`, `opa-finance-front/src/features/transactions/index.ts`, `opa-finance-front/test/unit/features/transactions/hooks/use-transaction-form.test.ts`, `opa-finance-front/test/integration/features/transactions/transactions.create-edit.integration.test.tsx`, `docs-hidden/melhorias/PE_transactions_page_split_front.md`, `docs-hidden/changes.md`. Arquivos removidos: nenhum. O que saiu da page e para onde foi: formulário/modal de create e modais inline de categoria/subcategoria migrados para componentes dedicados da feature. Impacto em contratos/UX/testes: nenhum em contrato/UX (refator puro), com cobertura ampliada para create income, recorrência weekly/monthly/yearly (hook), inline category flow e duplicar-para-criar. Validação: `npx vitest run test/unit/features/transactions/hooks/use-transaction-form.test.ts test/integration/features/transactions/transactions.create-edit.integration.test.tsx test/integration/features/transactions/transactions.delete-inline.integration.test.tsx` ✅, `npm run lint` ✅, `npm run build` ✅. Linhas atuais de `transactions-page.tsx`: 2713.

- 2026-04-24: Frontend/Transactions (Split R2 — modal de detalhes revalidado): sprint R2 reexecutada nesta rodada sem alteração de código de produto em `transactions-details-modal.tsx`/`transactions-page.tsx`. Arquivos editados: `opa-finance-front/test/unit/features/transactions/components/transactions-details-modal.component.test.tsx`, `opa-finance-front/test/integration/features/transactions/transactions.list.integration.test.tsx`, `docs-hidden/melhorias/PE_transactions_page_split_front.md`, `docs-hidden/changes.md`. Arquivos criados/removidos: nenhum. Resumo: testes foram alinhados ao contrato atual do modal de detalhes (sem props antigas da page), com cobertura explícita de cópia de descrição/valor (feedback `Copiado!`), fechamento por clique fora e fechamento por `Escape`. O que saiu da page e para onde foi: nenhum nesta rodada (revalidação). Impacto em contratos/UX: nenhum (refator puro de testes). Testes da sprint: `npx vitest run test/unit/features/transactions/components/transactions-details-modal.component.test.tsx test/integration/features/transactions/transactions.list.integration.test.tsx` ✅. Lint ✅, build ✅. Linhas atuais de `transactions-page.tsx`: 3455.

- 2026-04-24: Frontend/Transactions (Split R1 — modais de deleção revalidados): sprint R1 reexecutada nesta rodada sem alteração de código de produto em `transactions-page`/modais. Arquivos editados: `opa-finance-front/test/unit/features/transactions/components/transactions-delete-confirm-modal.component.test.tsx`, `opa-finance-front/test/unit/features/transactions/components/transactions-bulk-delete-modal.component.test.tsx`, `opa-finance-front/test/integration/features/transactions/transactions.delete-inline.integration.test.tsx`, `docs-hidden/melhorias/PE_transactions_page_split_front.md`, `docs-hidden/changes.md`. Arquivos criados/removidos: nenhum. Resumo: testes da R1 foram alinhados ao contrato atual dos modais (`open`, `transaction`/`selectedTransactions`, `onClose`, `onDeleted`) e à string de UI `Confirmar exclusão`, mantendo cobertura de deleção individual, deleção em lote, mensagem de erro e foco/fechamento. Impacto em contratos/UX: nenhum (refator puro de testes). Testes da sprint: `npx vitest run test/unit/features/transactions/components/transactions-delete-confirm-modal.component.test.tsx test/unit/features/transactions/components/transactions-bulk-delete-modal.component.test.tsx test/integration/features/transactions/transactions.delete-inline.integration.test.tsx` ✅. Lint ✅, build ✅. Linhas atuais de `transactions-page.tsx`: 3455.

- 2026-04-24: Frontend/Transactions (Split R0 — baseline revalidado): sprint R0 reexecutada nesta rodada sem alterações de código de produto. Arquivos editados: `docs-hidden/melhorias/PE_transactions_page_split_front.md` (registro da execução) e `docs-hidden/changes.md` (esta entrada). Arquivos criados/removidos: nenhum. Validação da checklist da sprint feita com `npx vitest run test/integration/features/transactions/transactions.list.integration.test.tsx` (cobrindo render/lista/filtros/paginação) + `npm run lint` ✅ + `npm run build` ✅ em `opa-finance-front`. O que saiu da page e para onde foi: nenhum (baseline). Impacto em contratos/UX/testes: nenhum (refatoração não iniciada nesta rodada). Linhas atuais de `transactions-page.tsx`: 3455.

- 24/04/2026: Frontend/Transactions (Split R2 — modal de detalhes): `transactions-details-modal.tsx` completado com lógica encapsulada: ref própria com foco automático ao abrir, `detailCopiedField` state, `detailCopyTimeoutRef` e `handleCopyDetail` (clipboard + timeout de 1500 ms). `dateFormatter`, `formatDateDisplay` e `formatCurrencyValue` agora são internos ao modal. Props removidas: `detailModalRef`, `detailCopiedField`, `dateFormatter`, `onCopyDetail`, `formatDateDisplay`, `formatCurrencyValue`. Da page: removidos `detailModalRef`, `detailCopiedField`, `detailCopyTimeoutRef`, `handleCopyDetail` e o `useEffect` de foco para o modal de detalhes. Contratos e UX: sem impacto. Lint ✅, build ✅. Linhas atuais de `transactions-page.tsx`: 3455.

- 24/04/2026: Frontend/Transactions (Split R1 — modais de deleção): `transactions-delete-confirm-modal.tsx` e `transactions-bulk-delete-modal.tsx` completados com lógica encapsulada: ref própria com foco automático ao abrir, `useDeleteTransaction()` interno, estados de erro e loading. A page passou a aceitar apenas `open`, `transaction`/`selectedTransactions`, `onClose` e `onDeleted`. Removidos da page: `useTransactionsDeleteActions`, `deleteModalRef`, `bulkDeleteModalRef`, e todos os estados/callbacks de deleção. `isBulkDeleting` substituído por `isBulkDeleteOpen` nos botões de ação. `clearDeleteError()` removido dos row click handlers. Lógica de deduplicação de transferências em bulk delete embutida no modal. Contratos e UX: sem impacto (refator puro). Lint ✅, build ✅. Linhas atuais de `transactions-page.tsx`: 3503.

- 24/04/2026: Frontend/Transactions (Split R0 — baseline): iniciado PE `PE_transactions_page_split_front.md` (branch `dev`) para quebrar `transactions-page.tsx` (3543 linhas) em componentes e hooks coesos. Sprint R0 concluída sem alterações de código: verificada integridade do baseline técnico (`npm run lint` ✅, `npm run build` ✅), todos os seis stubs de modais (`transactions-bulk-delete-modal.tsx`, `transactions-delete-confirm-modal.tsx`, `transactions-details-modal.tsx`, `transactions-create-modal.tsx`, `transactions-edit-modal.tsx`, `transactions-transfer-modal.tsx`) já presentes na feature e corretamente exportados via `index.ts`. Contratos, UX e testes: sem impacto (nenhum código alterado). Linhas atuais de `transactions-page.tsx`: 3543.

- 23/04/2026: Frontend/Accounts: correções do review aplicadas sem mudança funcional. Reforçada tipagem de domínio no filtro de tipo (`AccountType | ''`) em `accounts-filters-panel`/`use-accounts-search-params`; reduzido acoplamento no `accounts-page` com extração de navegação/paginação/abertura-fechamento para `use-accounts-page-actions`; criada suíte de integração de `accounts` em `test/integration/features/accounts/accounts.integration.test.tsx` cobrindo listagem, filtros + URL, paginação, detalhe, create/edit/delete e estado de erro.
- 21/04/2026: Frontend/Accounts (hardening de desacoplamento): `accounts-page.tsx` foi reduzido para composição fina com extração de blocos visuais e modais para componentes da feature (`accounts-filters-panel`, `accounts-mobile-list`, `accounts-desktop-table`, `accounts-pagination`, `account-create/edit/details/delete-confirm/primary-confirm`), criação de `use-accounts-page-controller` para estado/refs locais e consolidação de contratos dos hooks (`use-accounts-page-interactions`, `use-accounts-form-actions`, `use-accounts-delete-action`, `use-accounts-linked-actions`) com agrupamento por domínio. Cobertura unitária ampliada em `test/unit/features/accounts/components/*` e testes de hooks ajustados. Validação técnica: `npx vitest run test/unit/features/accounts` ✅, `npm run lint` ✅, `npm run build` ✅.
- 19/04/2026: Refatoração incremental de manutenibilidade (sem alteração funcional): no frontend, `transactions-page` extraiu `TransactionsSortIcon` e `accounts-page` extraiu helper puro `getApiErrorStatus` para `model`; no backend, `transaction.service` consolidou tipagem de snapshot de auditoria com alias (`AuditTransactionRecord`/`AuditNames`) para reduzir duplicação e acoplamento.
- 19/04/2026: Refatoração incremental (manutenibilidade, sem mudança funcional): `accounts-page.tsx` teve os efeitos/atalhos/gestão de foco extraídos para `use-accounts-page-interactions.ts`; `transactions-page.tsx` passou a reutilizar `use-debounced-value.ts` (removendo hook local inline); e `transaction.service.ts` teve a lógica de ordenação/filtros de listagem extraída para `transaction-list.helpers.ts`. Adicionado teste unitário do helper em `test/unit/transaction-list.helpers.unit.test.ts`.
- 19/04/2026: Auth/Security (frontend): persistência de sessão/token endurecida em `auth.store` com validação de token JWT e shape de usuário também nas operações de escrita (`setAuth`/`setAccessToken`), limpando estado inválido sem quebrar fluxo atual. Interceptor de refresh reforçado para rejeitar respostas/fila sem `accessToken` válido e atualizar apenas o token persistido (`setAccessToken`), mantendo compatibilidade com login/logout/refresh.
- 19/04/2026: Auth/Security: hardening adicional no backend para reset de senha com validação tipada do payload do token (`sub` + `type=reset`) em `AuthService.resetPassword`, removendo cast frouxo e mantendo o mesmo comportamento de validação/erros.
- 19/04/2026: Segurança/Auth: CORS do backend foi endurecido em produção (não aceita wildcard com `credentials`, apenas origens explicitamente configuradas em `CORS_ORIGINS`; em `development/test` mantém origem aberta). No frontend, fluxo de sessão/token foi robustecido com validação de dados persistidos e remoção de estado de autenticação parcial no login. Também foi concluído o fluxo de recuperação de senha com as telas `/forgot-password` e `/reset-password`, integração com `/auth/forgot-password` e `/auth/reset-password` e link “Esqueci minha senha” na tela de login.
- 19/04/2026: Documentação: revisão de consistência dos READMEs (raiz/api/front), ajustes de links internos e atualização de navegação da referência de API (`docs/API_REFERENCE_FRONTEND.md`) com atalhos para Swagger/OpenAPI e import do Insomnia.
- 18/04/2026: Frontend/Transactions (refatoração incremental em andamento): iniciado novo plano rastreável `PE_transactions_page_refactor_front.md` (branch `refactor/transactions-page-stepwise`) com gates por etapa e checklist funcional fixo. Concluídas E0 (baseline técnico + testes mínimos de transactions) e E1 (extração de estado/refs de UI de `transactions-page.tsx` para `use-transactions-ui-state.ts`), mantendo comportamento e tipagem. Evidências verdes em ambas etapas: `npm run lint` ✅, `npm run build` ✅, `npx vitest run test/unit/features/transactions test/integration/features/transactions --fileParallelism=false` ✅.
- 18/04/2026: Frontend/Profile (pós-code-review): busca de timezone passou a normalizar `_` como espaço em `normalizeSearch`, permitindo consultas como `sao paulo` para `America/Sao_Paulo`. A suíte de integração de perfil foi ampliada com cenários de alteração de senha (sucesso e credencial inválida com mensagem amigável `Senha atual incorreta.`), elevando a cobertura funcional dos fluxos críticos da tela.
- 18/04/2026: Frontend/Profile (Refatoração R0-R9 concluída): rota `/app/profile` foi convertida para entrypoint fino (`src/routes/app/profile.tsx`) e a tela migrada para `features/profile/components/profile-page.tsx`, com extração de domínio para `model` (`profile.helpers.ts`, `profile.constants.ts`, `profile.types.ts`), hooks de orquestração (`use-profile-forms.ts`, `use-profile-timezone-catalog.ts`), mapper de payload/defaults (`profile.mapper.ts`) e componentização da UI (`profile-personal-section.tsx`, `profile-password-section.tsx`). Fluxos preservados: update de perfil, alteração de senha, autodetect de timezone e fallback local quando catálogo da API falha. Cobertura adicionada em unit (`test/unit/features/profile/model/profile.helpers.test.ts`, `test/unit/features/profile/mappers/profile.mapper.test.ts`) e integração (`test/integration/features/profile/profile.integration.test.tsx`). Validação final verde: `npm run lint` ✅, `npm run test:unit` ✅ (55 arquivos / 150 testes), `npm run test:integration` ✅ (9 arquivos / 24 testes), `npm run build` ✅.
- 18/04/2026: Frontend/Recorrências (hardening pós-refatoração): concluída extração de componentes de responsabilidade da tela (`recurrences-filters.tsx` e `recurrences-list.tsx`) mantendo `recurrences-page.tsx` como orchestrator; `validateSearch` da rota `/app/recurrences` foi robustecido com saneamento de query params inválidos; helpers da feature foram desacoplados de estado global de autenticação (timezone injetada por parâmetro). Cobertura de integração ampliada para cenários críticos (bloqueio de exclusão de regra ativa, finalização e conflito `409`) e suíte de integração estabilizada em execução sequencial (`test:integration` com `--fileParallelism=false`) para evitar flakiness por concorrência. Validação final verde: `npm run lint` ✅, `npm run test:integration` ✅, `npm run build` ✅.
- 17/04/2026: Frontend/Audit (Refatoração R0-R9 concluída): rota `/app/audit` foi convertida para entrypoint fino (`src/routes/app/audit.tsx`) e a implementação da tela migrada para feature dedicada (`src/features/audit/components/audit-page.tsx`), com extração de domínio para `model` (`audit.helpers.ts`, `audit.constants.ts`, `audit.types.ts`), hook de busca/paginação (`use-audit-search-params.ts`), mapper de dados amigáveis (`audit-log.mapper.ts`) e componentização do detalhe (`audit-detail-modal.tsx`, `audit-preview-block.tsx`). Cobertura adicionada em unit (`test/unit/features/audit/*`) e integração (`test/integration/features/audit/audit.integration.test.tsx`). Validação final verde: `npm run lint` ✅, `npm run build` ✅, `npm run test:unit` ✅ (51 arquivos / 137 testes), `npm run test:integration` ✅ (7 arquivos / 14 testes).
- 17/04/2026: Frontend/Consolidado (Refatoração R0-R9 concluída): rota `/app/consolidated` foi convertida para entrypoint fino (`src/routes/app/consolidated.tsx`) e a implementação da tela migrada para feature dedicada (`src/features/consolidated/components/consolidated-page.tsx`) com componentização por responsabilidade (`consolidated-balance-section-table`, `consolidated-monthly-balance-table`, `consolidated-forecast-totals-table`, `consolidated-summary-card`, `consolidated-skeleton`), extração de domínio para `model` (`consolidated.constants.ts`, `consolidated.types.ts`, `consolidated.helpers.ts`), hooks (`use-consolidated-search-params`, `use-consolidated-expansion`) e mapper (`consolidated-balance.mapper.ts`). Cobertura adicionada em unit (`test/unit/features/consolidated/*`) e integração (`test/integration/features/consolidated/consolidated.integration.test.tsx`). Validação final verde: `npm run lint` ✅, `npm run build` ✅, `npm run test:unit` ✅ (48 arquivos / 130 testes), `npm run test:integration` ✅ (6 arquivos / 11 testes).
- 16/04/2026: Frontend/Weekly Cashflow (Refatoração R0-R9 técnico): rota `/app/weekly-cashflow` foi convertida para entrypoint fino (`src/routes/app/weekly-cashflow.tsx`) e a implementação da tela movida para feature dedicada (`src/features/weekly-cashflow/components/weekly-cashflow-page.tsx`), com extração de domínio para `model` (`weekly-cashflow.helpers.ts`, `weekly-cashflow.types.ts`, `weekly-cashflow.constants.ts`) e hooks de orquestração (`use-weekly-cashflow-search-params.ts`, `use-weekly-cashflow-columns-state.ts`). Cobertura unitária adicionada para helpers e hooks em `test/unit/features/weekly-cashflow/*`. Validação técnica verde: `npm run lint` ✅, `npm run build` ✅, `npm run test:unit` ✅ (44 arquivos / 119 testes), `npm run test:integration` ✅ (4 arquivos / 4 testes).
- 16/04/2026: Frontend/Categories (pós-code-review): corrigida a lógica de `toggleExpandAll` em `use-categories-expansion` para colapsar apenas quando a seleção expandida é exatamente igual aos IDs atuais (evitando recolhimento indevido quando a lista filtrada muda mantendo o mesmo tamanho). Também foi adicionada cobertura de integração para a tela de categorias em `test/integration/features/categories/categories.list.integration.test.tsx` (filtro por tipo + expandir/recolher subcategoria). Validação: `npm run lint` ✅, `npm run test:integration` ✅ (4 arquivos / 4 testes).
- 16/04/2026: Frontend/Categories (Refatoração R0-R9 técnico): executada refatoração da tela `/app/categories` com extração de domínio para a feature (`model/categories.constants.ts`, `model/categories.types.ts`, `model/categories.helpers.ts`), mapeadores (`mappers/categories-payload.mapper.ts`), hooks de orquestração (`hooks/use-categories-search-params.ts`, `hooks/use-categories-expansion.ts`) e migração da tela para `features/categories/components/categories-page.tsx`; rota foi reduzida para entrypoint fino em `routes/app/categories.tsx`. Cobertura unitária adicionada para novos módulos (helpers/constants/mappers/hooks). Validação técnica verde: `npm run lint` ✅, `npm run test:unit` ✅ (41 arquivos / 112 testes), `npm run test:integration` ✅ (3 arquivos / 3 testes), `npm run build` ✅.
- 15/04/2026: Documentação/PE Dashboard: ajustado `PE_dashboard_refactor_front.md` com seção explícita de rollback por sprint (gatilhos, estratégia e validação pós-rollback), remoção de ambiguidade no item R3 (`filtros/URL`) e reforço das evidências finais com `npm run test:integration` verde (3 arquivos / 3 testes).
- 15/04/2026: Frontend/Accounts (Refatoração R9 parcial): executada validação automatizada completa do frontend no fechamento de hardening (`npm run test`), com 39 arquivos e 99 testes aprovados (unit + integration), sem regressões detectadas após a refatoração de `accounts`. Mantido pendente apenas o checklist manual final da tela para encerramento total do PE.
- 15/04/2026: Frontend/Accounts (Refatoração R9 parcial): hardening técnico final do piloto de `accounts` executado com revisão de consistência no escopo refatorado (rota fina + `accounts-page` + hooks/model/mappers), sem regressões detectadas nos gates técnicos. Validação técnica verde: `npm run lint` ✅, `npm run test:unit` ✅ (36 arquivos / 96 testes), `npm run build` ✅. Pendente apenas checklist manual final para encerramento completo do PE.
- 15/04/2026: Frontend/Accounts (Refatoração R8 parcial): rota `/app/accounts` foi consolidada como entrypoint fino; a implementação da tela foi movida para `src/features/accounts/components/accounts-page.tsx` e `src/routes/app/accounts.tsx` ficou somente com `validateSearch` + bridge `useSearch/useNavigate` + render da page. Path, contrato e comportamento preservados. Validação técnica verde: `npm run lint` ✅, `npm run test:unit` ✅ (36 arquivos / 96 testes), `npm run build` ✅.
- 15/04/2026: Frontend/Accounts (Refatoração R5 parcial): orquestração de formulário/create/edit foi extraída para `src/features/accounts/hooks/use-accounts-form-actions.ts`, centralizando `openAccountEdit`, submit de criação/edição, mapeamento de payload e tratamento de erro `root` dos formulários (mantendo fechamento de modal e limpeza de `id` na URL após edição). A rota `src/routes/app/accounts.tsx` passou a consumir o hook com `handleSubmit`/`handleEditSubmit`. Novo teste unitário em `test/unit/features/accounts/hooks/use-accounts-form-actions.test.ts`. Validação técnica verde: `npm run lint` ✅, `npm run test:unit` ✅ (36 arquivos / 96 testes), `npm run build` ✅.
- 15/04/2026: Frontend/Accounts (Refatoração R6 parcial): fluxos vinculados de conta principal e visibilidade no dashboard foram extraídos para `src/features/accounts/hooks/use-accounts-linked-actions.ts`, centralizando estado/erros/ações de `handleSetPrimaryAccount` e `handleToggleDashboardVisibility` (com tratamento de conflito `409` por recorrência ativa). A rota `src/routes/app/accounts.tsx` passou a consumir esse hook, preservando comportamento de modais e atalhos. Novo teste unitário em `test/unit/features/accounts/hooks/use-accounts-linked-actions.test.ts`. Validação técnica verde: `npm run lint` ✅, `npm run test:unit` ✅ (35 arquivos / 93 testes), `npm run build` ✅.
- 15/04/2026: Frontend/Accounts (Refatoração R4 parcial): estado de seleção da lista foi isolado em `src/features/accounts/hooks/use-accounts-selection.ts`, centralizando seleção por linha/página, subtotal da seleção, limpeza por filtro, saneamento de IDs inválidos após refresh de dados, limpeza por `Esc` e controle `indeterminate` do checkbox mestre. A rota `src/routes/app/accounts.tsx` passou a consumir handlers/estado do hook em desktop e mobile. Novo teste unitário em `test/unit/features/accounts/hooks/use-accounts-selection.test.ts`. Validação técnica verde: `npm run lint` ✅, `npm run test:unit` ✅ (34 arquivos / 90 testes), `npm run build` ✅.
- 15/04/2026: Frontend/Accounts (Refatoração R3 parcial): sincronização de filtros/URL da tela de contas foi isolada em `src/features/accounts/hooks/use-accounts-search-params.ts` (debounce de busca, sync `searchDraft`↔URL, handlers de `q`/`type` e limpar filtros). A rota `src/routes/app/accounts.tsx` passou a consumir o hook e removeu refs/effects/handlers inline equivalentes. Adicionado teste unitário `test/unit/features/accounts/hooks/use-accounts-search-params.test.ts`. Validação técnica verde: `npm run lint` ✅, `npm run test:unit` ✅ (33 arquivos / 87 testes), `npm run build` ✅.
- 15/04/2026: Frontend/Accounts (Refatoração R2 parcial): centralizados tipos/constantes e mapeamento de payload na feature `accounts`. Foram adicionados `src/features/accounts/model/accounts.constants.ts` e `accounts.types.ts` (catálogo de tipos, labels/opções, sort/direction e guards), `src/features/accounts/mappers/accounts-payload.mapper.ts` (payload create/update), e a rota `src/routes/app/accounts.tsx` passou a usar essas estruturas no `validateSearch`, nos selects de tipo e nos submits. `src/schemas/account.schema.ts` também passou a reutilizar `ACCOUNT_TYPE_VALUES`. Cobertura unitária adicionada em `test/unit/features/accounts/model/accounts.constants.test.ts` e `test/unit/features/accounts/mappers/accounts-payload.mapper.test.ts`. Validação técnica verde: `npm run lint` ✅, `npm run test:unit` ✅ (32 arquivos / 85 testes), `npm run build` ✅.
- 15/04/2026: Frontend/Accounts (Refatoração R1 parcial): extraídos helpers puros da rota para `src/features/accounts/model/accounts.helpers.ts` (normalização de busca, detector de conflito de recorrência, filtro, ordenação e paginação de contas), com adoção na rota `src/routes/app/accounts.tsx` sem alteração de comportamento funcional. Adicionado teste unitário `test/unit/features/accounts/model/accounts.helpers.test.ts`. Validação técnica verde: `npm run lint` ✅, `npm run test:unit` ✅ (30 arquivos / 81 testes), `npm run build` ✅.
- 15/04/2026: Frontend/Accounts (Refatoração R0 parcial): iniciado PE `PE_accounts_refactor_front.md` com baseline técnico do frontend validado para entrada da refatoração da tela de contas; evidências `npm run lint` ✅, `npm run test:unit` ✅ (29 arquivos / 76 testes) e `npm run build` ✅. Pendente apenas smoke manual de `/app/accounts` para fechamento do R0.
- 15/04/2026: Frontend/Dashboard (Refatoração): PE encerrado com status final `5` após validação manual completa do usuário (paridade visual, abertura/fechamento de modais e painéis, rota `/app`, deep-link/back-forward de filtros e revisão de console/network sem regressões).
- 15/04/2026: Frontend/Dashboard (Refatoração R9 parcial): executado hardening técnico final do módulo dashboard com varredura de inconsistências no escopo refatorado (sem `console.*`, `TODO/FIXME` ou `any` no módulo/rota), e validação técnica final verde (`npm run lint`, `npm run test:unit`, `npm run build`). O PE foi atualizado com checklist manual consolidado (R7+R8+R9) para encerramento assistido no navegador.
- 15/04/2026: Frontend/Dashboard (Refatoração R8 parcial): rota `/app` foi convertida para entrypoint fino mantendo `validateSearch` e delegando a composição da tela para `src/features/dashboard/components/dashboard-page.tsx`; `src/routes/app/index.tsx` agora contém apenas boundary de rota (`useSearch`/`useNavigate` + render da page), sem alteração de path/comportamento/contrato externo. Validação técnica verde (`npm run lint`, `npm run test:unit`, `npm run build`), pendente validação manual de deep-link/back-forward para fechamento da R8.
- 14/04/2026: Frontend/Dashboard (Refatoração R7 parcial): modais do dashboard foram extraídos para `src/features/dashboard/components/dashboard-top-category-transactions-modal.tsx` e `dashboard-transaction-details-modal.tsx`, reduzindo a responsabilidade da rota `/app` para composição e navegação; adicionados testes unitários em `test/unit/features/dashboard/components/dashboard-top-category-transactions-modal.component.test.tsx` e `dashboard-transaction-details-modal.component.test.tsx`. Validação técnica verde (`npm run lint`, `npm run test:unit`, `npm run build`), pendente validação manual para fechamento da R7.
- 14/04/2026: Frontend/Dashboard (Refatoração R7 parcial): componentização expandida na rota `/app` com extração dos blocos `Últimas transações`, `Top 5` (despesas/receitas via componente genérico) e `Contas` para `src/features/dashboard/components/dashboard-recent-transactions-card.tsx`, `dashboard-top-categories-card.tsx` e `dashboard-accounts-card.tsx`; adicionados testes unitários em `test/unit/features/dashboard/components/dashboard-recent-transactions-card.component.test.tsx`, `dashboard-top-categories-card.component.test.tsx` e `dashboard-accounts-card.component.test.tsx`. Validação técnica verde (`npm run lint`, `npm run test:unit`, `npm run build`), pendente validação manual de paridade visual para fechamento da R7.
- 14/04/2026: Frontend/Dashboard (Refatoração R7 parcial): iniciada componentização da tela com extração dos blocos de filtros e cards de resumo para `src/features/dashboard/components/dashboard-filters.tsx` e `dashboard-summary-cards.tsx`, mantendo composição na rota `/app`; adicionados testes unitários de componentes em `test/unit/features/dashboard/components/dashboard-filters.component.test.tsx` e `dashboard-summary-cards.component.test.tsx`. Validação técnica verde (`npm run lint`, `npm run test:unit`, `npm run build`), pendente validação manual de paridade visual da R7.
- 14/04/2026: Frontend/Dashboard (Refatoração R6 parcial): interações/atalhos foram extraídos para `src/features/dashboard/hooks/use-dashboard-interactions.ts` (fechamento por `Esc`, foco do modal de detalhe e cópia de campos com timeout), removendo lógica inline equivalente da rota `/app`; adicionada cobertura unitária em `test/unit/features/dashboard/hooks/use-dashboard-interactions.test.ts`. Validação técnica verde (`npm run lint`, `npm run test:unit`, `npm run build`), pendente checklist manual de R6.
- 14/04/2026: Frontend/Dashboard (Refatoração R5 parcial): mapeamentos de query da tela foram centralizados em `src/features/dashboard/mappers/dashboard-query.mapper.ts` (base comum + builders para summary, últimas transações, top categorias e top categoria selecionada), reduzindo repetição de payloads na rota `/app`; adicionada cobertura unitária em `test/unit/features/dashboard/mappers/dashboard-query.mapper.test.ts`. Validação técnica verde (`npm run lint`, `npm run test:unit`, `npm run build`), pendente validação manual de R5.
- 14/04/2026: Frontend/Dashboard (Refatoração R3/R4): validação manual concluída sem regressões para sincronização de filtros/URL e para estados dos blocos (expand/recolher, loading/empty/error e seleção de top categoria). Sprints R3 e R4 encerradas no PE, próximo passo: R5.
- 14/04/2026: Frontend/Dashboard (Refatoração R4 parcial): extraído estado de blocos para `src/features/dashboard/hooks/use-dashboard-panels.ts` (expand/recolher dos painéis, `groupBy` e seleção de top categoria com fallback), removendo lógica inline equivalente da rota `/app`; adicionada cobertura unitária em `test/unit/features/dashboard/hooks/use-dashboard-panels.test.ts`. Validação técnica verde (`npm run lint`, `npm run test:unit`, `npm run build`), pendente checklist manual de R4.
- 14/04/2026: Frontend/Dashboard (Refatoração R3 parcial): extraída sincronização de filtros/periodização/URL da rota para `src/features/dashboard/hooks/use-dashboard-search-params.ts` (incluindo normalização de `accountId`, fallback de conta primária e handlers de período/conta/data), com remoção de handlers/effects equivalentes de `src/routes/app/index.tsx`; adicionados testes unitários do hook em `test/unit/features/dashboard/hooks/use-dashboard-search-params.test.ts`. Validação técnica verde (`npm run lint`, `npm run test:unit`, `npm run build`), pendente checklist manual de R3.
- 14/04/2026: Frontend/Dashboard (Refatoração R2 parcial): centralizados tipos e constantes de período em `src/features/dashboard/model/dashboard.constants.ts` e `dashboard.types.ts`; rota `/app` passou a usar `DASHBOARD_PERIOD_VALUES`/guards no `validateSearch` e a renderizar opções do select via `DASHBOARD_PERIOD_OPTIONS`; adicionada cobertura unitária em `test/unit/features/dashboard/model/dashboard.constants.test.ts`. Validação técnica verde (`npm run lint`, `npm run test:unit`, `npm run build`), pendente validação manual dos itens de R2 no PE.
- 14/04/2026: Frontend/Dashboard (Refatoração R1 parcial): extraídos utilitários puros de data da rota (`getDashboardDateRange`, `formatDashboardDateDisplay`, `formatDashboardDateInput`) para `src/features/dashboard/model/dashboard.helpers.ts`, com adoção na rota `/app` e novo teste unitário em `test/unit/features/dashboard/model/dashboard.helpers.test.ts`; validação técnica verde (`npm run lint`, `npm run test:unit`, `npm run build`), pendente apenas validação manual dos formatadores na UI.
- 14/04/2026: Frontend/Dashboard (Refatoração R0): sprint R0 encerrada; smoke manual confirmou abertura de `/app` sem erro no console e carregamento normal dos dados iniciais via API. Próximo passo: iniciar R1 (extração de utilitários puros).
- 14/04/2026: Frontend/Dashboard (Refatoração R0): validado manualmente que `/app` abre sem erros no console do navegador; permanece pendente apenas a validação de carregamento dos dados iniciais para fechamento completo da sprint R0.
- 14/04/2026: Frontend/Dashboard (Refatoração R0 parcial): baseline técnico concluído no início da execução do PE (`npm run lint` e `npm run build` verdes); sprint R0 permanece aberta aguardando smoke manual do dashboard e validação de console no navegador.
- 14/04/2026: Frontend/Refatoração por telas: PEs de `dashboard`, `accounts`, `categories`, `weekly-cashflow`, `consolidated`, `audit`, `recurrences` e `profile` foram aprofundados com matriz R0-R9 (objetivo, arquivos-alvo, risco, dependências, validação) e checklist de testes por sprint; `PE_TEMPLATE_melhoria.md` também foi atualizado para exigir seção de testes por etapa.
- 14/04/2026: Frontend/Refatoração por telas: criado PE específico do dashboard em `docs-hidden/melhorias/PE_dashboard_refactor_front.md` no mesmo padrão incremental R0-R9 adotado nas demais telas.
- 14/04/2026: Frontend/Refatoração por telas: criado rollout oficial para replicar o padrão do PE de `transactions` nas demais telas em `docs-hidden/melhorias/PE_front_refactor_rollout_telas.md`, com PEs individuais iniciados (status `0`) para `accounts`, `categories`, `weekly-cashflow`, `consolidated`, `audit`, `recurrences` e `profile`.
- 14/04/2026: Frontend/Transactions (Refatoração piloto R9): checklist manual final concluído sem regressões (incluindo `validateSearch`, deep-link/back-forward, recorrência, atalhos desktop e revisão de console/network); melhoria `PE_transactions_refactor_front_piloto.md` encerrada com status `5` (concluído).
- 12/04/2026: Refatoração `transactions` (piloto) avançou para `R3`: sincronização de filtros e URL foi extraída para hooks da feature (`src/features/transactions/hooks/use-transactions-search-params.ts` e `src/features/transactions/hooks/use-transactions-filters.ts`), com preservação de `validateSearch` na rota e centralização de handlers de filtro/sort; validação técnica verde (`npm run lint` e `npm run build`).
- 12/04/2026: no PE de refatoração `transactions` foi simplificada a seção "Checklist de testes por sprint", removendo itens repetitivos de `lint/build` e mantendo foco apenas nas validações funcionais manuais por sprint.
- 12/04/2026: Refatoração `transactions` (piloto) avançou para `R2`: tipos e constantes de domínio foram centralizados na feature (`src/features/transactions/model/transactions.constants.ts` e `src/features/transactions/model/transactions.types.ts`), com ajuste de imports da rota e uso das constantes em `validateSearch`/ordenação de categorias; validação técnica permaneceu verde (`npm run lint` e `npm run build`).
- 12/04/2026: Refatoração `transactions` (piloto) em execução: `R0` concluído com baseline técnico verde no frontend (`npm run lint` e `npm run build`) e `R1` concluído com extração de funções puras de `src/routes/app/transactions.tsx` para `src/features/transactions/model/transactions.helpers.ts` e `src/features/transactions/mappers/transaction-payload.mapper.ts`; rota passou a consumir imports da feature, sem alteração de rota/contrato.
- 12/04/2026: Frontend/lint: em `opa-finance-front`, a configuração de ESLint para TS/TSX passou a desativar a regra base `no-unused-vars` e manter apenas `@typescript-eslint/no-unused-vars`; também foram removidas diretivas `eslint-disable` obsoletas em hooks/interceptors.
- 12/04/2026: revisão pré-execução do plano `docs-hidden/melhorias/PE_transactions_refactor_front_piloto.md` com inclusão da Sprint R0 (baseline obrigatório), ajuste de checklist "Pronto para execução", formalização de gate de rota/contrato, definição de evidência funcional por checklist manual (sem script de testes dedicado no frontend) e reforço de diretriz anti-abstração prematura.
- 12/04/2026: Frontend/build: no `opa-finance-front`, o plugin Vite React foi migrado de `@vitejs/plugin-react-swc` para `@vitejs/plugin-react` (configuração em `vite.config.ts` e dependências/lockfile atualizados) para alinhar com a recomendação do Rolldown/Vite quando não há plugins SWC customizados.
- 10/04/2026: atualizado `opa-finance-front/AGENTS.md` com seção de restrições obrigatórias para mudanças no frontend (sem alterar regra de negócio/layout sem necessidade/rotas/bibliotecas principais, evitando camadas e abstrações artificiais) e com stack técnica de referência consolidada (React, TypeScript, Vite, Tailwind CSS, TanStack Router/Query, Axios, Zod, React Hook Form).
- 10/04/2026: plano incremental da refatoração de `transactions` foi reestruturado de 6 para 9 sprints (`R1-R9`) para melhorar legibilidade operacional, com ajuste da matriz de rastreabilidade, tasks por sprint, gates de teste e registro de execução no arquivo `docs-hidden/melhorias/PE_transactions_refactor_front_piloto.md`.
- 10/04/2026: seção de plano incremental da refatoração piloto de `transactions` foi atualizada para modelo por sprint R1-R6 com controles de entrada/saída, risco, rollback por sprint, gates técnicos/funcionais e assumptions explícitas em `docs-hidden/melhorias/PE_transactions_refactor_front_piloto.md`.
- 10/04/2026: plano piloto de refatoração da feature `transactions` foi consolidado com blueprint arquitetural detalhado (12 seções: diagnóstico, responsabilidades, arquitetura alvo, estrutura de pastas e execução step-by-step com critérios por etapa) em `docs-hidden/melhorias/PE_transactions_refactor_front_piloto.md`.
- 10/04/2026: criado plano piloto de refatoração frontend da feature `transactions` com execução incremental e rollback controlado por fase em `docs-hidden/melhorias/PE_transactions_refactor_front_piloto.md`; definido checklist fixo de validação e critério de bloqueio de avanço por regressão funcional.
- 09/04/2026: Sprint D (Recorrência `REC-S4-03` a `REC-S4-08`) concluída: `notes.md` atualizado com fechamento do plano de recorrência; publicada validação final de entrega (`PE_recorrencia_validacao_final.md`); publicadas diretrizes de evolução (`PE_recorrencia_diretrizes_evolucao.md`), matriz de rastreabilidade (`PE_recorrencia_matriz_rastreabilidade.md`), estratégia de testes (`PE_recorrencia_estrategia_testes.md`) e runbook operacional do job diário (`PE_recorrencia_runbook_job_diario.md`); checklist e critérios de aceite da melhoria de recorrência foram marcados como concluídos no plano.
- 09/04/2026: Sprint D (Recorrência `REC-S4-01`/`REC-S4-02`): documentação técnica foi atualizada para refletir o estado atual do módulo de recorrências e integrações correlatas. Foram adicionados os módulos `Users`, `Reports` e `Recurrences` em `opa-finance-api/docs/modules/`; o índice da referência de API recebeu a seção `Recurrences`; e os READMEs da raiz/API passaram a listar os novos módulos para navegação consistente.
- 09/04/2026: Correções pós-revisão (timezone/recorrência): seletores de timezone em Perfil/Criar usuário passaram a reagir ao valor atual do formulário (evitando lista stale quando o filtro não contém a opção selecionada); validação frontend de `single` em recorrências passou a considerar “hoje” no timezone da recorrência, alinhando o bloqueio antecipado com a regra do backend.
- 09/04/2026: Sprint C (Recorrência `REC-S3-21`): formulário de criação de usuário (`/app/register`) passou a incluir campo `timezone` com default `Australia/Adelaide`, busca + dropdown IANA (catálogo do backend com fallback local) e ação de autodetectar timezone do dispositivo antes de salvar; payload de registro e validação frontend foram atualizados para exigir `timezone`.
- 09/04/2026: Sprint C (Recorrência `REC-S3-20`): catálogo de timezone no frontend passou a ter fallback resiliente quando `Intl.supportedValuesOf('timeZone')` não está disponível, combinando lista mínima segura + timezone atual do dispositivo; no perfil, esse fallback permanece ativo quando a carga do catálogo do backend falha.
- 09/04/2026: Sprint C (Recorrência `REC-S3-19`): UX do seletor de timezone no perfil foi refinada com label amigável por opção (`Cidade (UTC±HH:mm) · IANA`), manutenção de persistência em valor IANA e ação explícita para autodetectar/applicar o timezone atual do dispositivo.
- 09/04/2026: Sprint C (Recorrência `REC-S3-18`): edição por escopo `single` no frontend passou a bloquear antecipadamente ocorrência passada (`occurrenceDate < hoje`) e a exibir orientação explícita para ajuste manual em Transações; mensagens de erro vindas do backend para ocorrência já materializada também foram normalizadas para essa orientação.
- 09/04/2026: Correções pós-revisão: modal de exclusão de conta agora exibe também erros não-`409` no próprio contexto de confirmação; perfil passou a consumir catálogo de timezone do backend via `GET /users/timezones` (fonte PostgreSQL), reduzindo divergência entre opções do dropdown e validação do servidor, com fallback local quando a carga do catálogo falhar.
- 09/04/2026: Sprint C (Recorrência `REC-S3-17`): tela de perfil passou a permitir atualização de timezone com busca + dropdown IANA e persistência via `PUT /users/:id`; payload/tipos de perfil foram estendidos para `timezone`, mantendo feedback de sucesso/erro no fluxo de salvar.
- 09/04/2026: Sprint C (Recorrência `REC-S3-16`): telas de Conta/Categoria foram alinhadas ao bloqueio de recorrência ativa. Em contas, conflitos `409` de exclusão/inativação agora preservam o modal e exibem motivo real retornado pela API (incluindo recorrência ativa) com orientação de finalizar/remapear recorrências; em categorias/subcategorias, conflitos por recorrência ativa ganharam orientação explícita no modal de exclusão.
- 09/04/2026: Sprint C (Recorrência `REC-S3-15`): tratamento de conflito concorrente (`409`) no modal de edição de recorrência agora exibe feedback com ação direta `Recarregar dados da recorrência`, recarregando a regra do backend e reidratando o formulário antes de nova tentativa de salvar.
- 09/04/2026: Recorrência/UX hardening: no modal/lista de recorrências, ações de finalizar/excluir passaram a limpar erro stale antes da execução; exclusão de regra `active` agora orienta diretamente no clique (sem depender de tooltip em botão desabilitado); e criação de transação com recorrência ajustou mensagem de rollback para refletir corretamente quando a reversão automática falha.
- 09/04/2026: Sprint C (Recorrência `REC-S3-14`): ação de exclusão foi protegida no frontend com bloqueio defensivo para regras `active` (mensagem orientando finalizar antes), mantendo exclusão efetiva apenas para recorrências `finalized`.
- 09/04/2026: Sprint C (Recorrência `REC-S3-04/REC-S3-05`): tela de recorrências recebeu tratamento de estados de carregamento/erro/vazio com fallback de retry e CTA de criação; filtros ganharam ação `Limpar filtros`; e UX desktop do modal foi reforçada com fechamento por `Esc`/clique fora, bloqueio de fechamento durante submit e desativação de salvar quando dados de suporte ainda estão carregando.
- 09/04/2026: Sprint C (Recorrência `REC-S3-01/REC-S3-02/REC-S3-03`): criada tela desktop de gerenciamento de recorrências (`/app/recurrences`) com listagem paginada, filtros e ações operacionais; implementado formulário completo de criação/edição de recorrência (transação/transferência, agenda, término, conta/categoria/subcategoria) e integração com edição por escopo (`all`, `this_and_next`, `single`) no frontend via `PUT /recurrences/:id/edit-scope`. Navegação lateral atualizada com novo item `Recorrências`.
- 09/04/2026: Sprint B (Recorrência `REC-S2-17`): exclusão de categoria/subcategoria agora é bloqueada quando existe recorrência `active` vinculada (checagem em `CategoryService.delete` e `SubcategoryService.delete`), com novos testes de integração para ambos os cenários.
- 09/04/2026: Sprint B (Recorrência `REC-S2-18`): atualização de `timezone` no perfil (`PUT /users/:id`) passou a sincronizar apenas recorrências `active` não deletadas do usuário, preservando materializadas históricas; a sincronização gera trilha de auditoria por recorrência com `metadata.operation=recurrence-timezone-sync`.
- 09/04/2026: Sprint B (Recorrência `REC-S2-21/REC-S2-22/REC-S2-26`): cobertura de contrato para timezone foi expandida com testes de integração para cadastro/perfil, incluindo timezone válido, fallback padrão `Australia/Adelaide`, erro `400` para timezone inválido e retorno de timezone no `/auth/me`.
- 09/04/2026: Sprint B (Recorrência `REC-S2-23/REC-S2-24/REC-S2-25`): adicionados novos testes unitários (`recurrence-service.unit`, `timezone-db-utils.unit`, bordas extras em `recurrence-schedule-utils.unit`) e expansão de integração para regras críticas recém-adicionadas.
- 09/04/2026: Sprint B (Recorrência `REC-S2-27`): job diário ganhou log estruturado por lote (`event=recurrences.job.user.batch_result`) com métricas de execução/falhas por batch para facilitar monitoramento operacional.
- 07/04/2026: Recorrência/Testes: `updateRecurrenceSchema` passou a rejeitar `originType` no payload de atualização para evitar update sem efeito com incremento de versão; lock de `test:integration` foi endurecido para validar também a assinatura do comando do PID antes de bloquear execução, reduzindo falso positivo por reuso de PID.
- 06/04/2026: Testes/API: execução de integração ganhou proteção contra concorrência local com lock em `scripts/test-integration.sh`; `npm run test:integration` agora usa o wrapper e bloqueia segunda execução simultânea para evitar conflito no banco de teste compartilhado.
- 06/04/2026: Testes/API: lock de integração foi endurecido com arquivo de PID e limpeza de lock órfão; em caso de travamento abrupto, a próxima execução identifica PID inativo, remove lock stale e prossegue automaticamente.
- 06/04/2026: Recorrência/REC-S2-08: reforçada coerência de payload por tipo de origem. `createRecurrenceSchema` agora bloqueia campos de transferência em `originType=transaction` e bloqueia conta/categoria/subcategoria de transação em `originType=transfer`; no `update` da recorrência foi adicionado guard por origem e ajuste de consistência para resetar `subcategoryId` quando `categoryId` muda sem nova subcategoria. A suíte crítica de recorrências ganhou cobertura desses cenários.
- 06/04/2026: Recorrência/REC-S2-07: implementado endpoint `GET /recurrences/forecast` com projeção até o fim do ano e separação `real` vs `projected` vs `combined`; inclui filtro opcional por `accountIds` com validação de ownership, respeito a `endType`/`endOccurrences` sem consumir projeção e projeção de transferências em duas pernas (income/expense). Documentação da API frontend e import do Insomnia foram atualizados.
- 06/04/2026: Recorrência/Materialização: removida dependência antecipada da categoria de sistema `Transferência` no início de `/recurrences/materialize`; o carregamento do `transferCategoryId` passou a ser lazy (somente quando houver recorrência de transferência no lote), evitando falha indevida em materialização de recorrências apenas de transação.
- 06/04/2026: Recorrência/REC-S2-06 (testes críticos): adicionada suíte de integração `test/integration/recurrences/recurrences-critical-rules.test.ts` cobrindo idempotência de materialização, bloqueio de `single` com alteração de agenda, edição `this_and_next` com split de regra, conflito otimista (`409`) e política de exclusão (`active` bloqueia, `finalized` permite exclusão lógica); `buildTestApp` de integração passou a registrar `recurrenceRoutes`.
- 06/04/2026: Transações/API: endpoint `GET /transactions/top-categories` passou a aplicar ordenação determinística em empates (`totalAmount desc` + `name asc`) para categorias e subcategorias, eliminando flakiness em testes e respostas inconsistentes.
- 06/04/2026: Testes/API (arquitetura): testes de integração foram consolidados em `test/integration/**` (incluindo `setup` e `helpers`), com ajuste de imports para alias `@/` e script `test:integration` atualizado para executar somente `test/integration`.
- 05/04/2026: Testes/API (arquitetura): unitários foram movidos de `src/**` para `test/unit/**` para seguir o padrão centralizado de testes do projeto; arquivos foram renomeados para nomes de domínio (sem prefixo de sprint) e scripts ajustados para `test:unit` (`test/unit`) e `test:integration` (`test/**` excluindo `test/unit/**`).
- 05/04/2026: Testes/Recorrência (S2): adicionada cobertura unitária específica para as sprints `REC-S2-01`, `REC-S2-02`, `REC-S2-03` e `REC-S2-04` com novos testes de schema/contrato em `src/modules/recurrences/recurrence.s2-01-02.schemas.test.ts`, `src/modules/transactions/transaction.s2-03.recurrence.test.ts` e `src/modules/transfers/transfer.s2-04.recurrence.test.ts`.
- 05/04/2026: Testes/API: suíte de testes separada em níveis no `opa-finance-api` com scripts `test:unit` (testes em `src/**`) e `test:integration` (testes em `test/**`), além de ajuste no `vitest.config.ts` para incluir apenas `src/test` e excluir `dist/**`, evitando execução indevida de artefatos compilados.
- 05/04/2026: Testes/Recorrência (unitário): iniciada cobertura unitária do módulo com testes em `src/modules/recurrences/recurrence.schemas.test.ts` (validação de escopos `single/this_and_next/all` e obrigatoriedade de `occurrenceDate`) e expansão de `src/core/utils/recurrence-schedule.utils.test.ts` (bordas mensais e anuais, incluindo ajuste de dia inexistente e regra de ano não bissexto).
- 05/04/2026: Recorrência/REC-S2-05 (hardening): correções de consistência na edição por escopo. `this_and_next` passou a executar de forma atômica (encerramento da regra atual + criação da nova regra na mesma transação), `single` de transferência passou a atualizar os dois lados (débito/crédito) de forma transacional, foi adicionado bloqueio para origem=destino em `single` de transferência e ajuste de coerência para resetar `subcategoryId` em `single` de transação quando a categoria é alterada sem nova subcategoria explícita.
- 05/04/2026: Sprint B (Recorrência / REC-S2-05): adicionada edição por escopo no backend com novo endpoint `PUT /recurrences/:id/edit-scope` (`single`, `this_and_next`, `all`). Regras aplicadas: `single` edita apenas ocorrência já materializada na data alvo (sem alterar agenda e sem retroagir passado), `this_and_next` encerra regra atual na véspera e cria nova regra a partir da ocorrência-alvo, `all` mantém atualização da regra inteira. Documentação da API e import do Insomnia foram atualizados.
- 05/04/2026: Transferências/Recorrência: adicionado guard de período no schema de `POST /transfers` com `recurrence` para impedir `endDate` anterior ao início efetivo da regra (`startDate` ou `date` da transferência).
- 05/04/2026: Transferências/Auditoria: logs de criação de transferência passaram a incluir `recurrenceId` em `metadata` quando a transferência é criada com recorrência no mesmo submit, melhorando rastreabilidade operacional.
- 05/04/2026: Sprint B (Recorrência / REC-S2-04): `POST /transfers` passou a aceitar bloco opcional `recurrence`, criando recorrência de transferência no mesmo submit e na mesma transação de banco; incluída regra da primeira ocorrência para evitar duplicidade, com registro inicial em `recurrence_occurrences` (quando materializada no submit) e retorno de `recurrenceId` na resposta.
- 05/04/2026: Recorrência/Operação: `POST /recurrences/materialize` ganhou controle de lote por chamada (`maxRecurrences`, default `200`, máximo `500`) e passou a retornar metadados operacionais de paginação (`totalActiveRecurrences`, `truncatedByBatch`, `remainingRecurrences`) para evitar chamadas longas e reduzir risco de timeout em bases maiores.
- 05/04/2026: Recorrência/Resiliência: cálculo de agenda em `create/update/materialize` passou a tratar melhor dados legados inválidos, movendo normalização de cursor para bloco protegido e retornando erro de validação amigável quando a regra não pode ser recalculada.
- 05/04/2026: Recorrência/Transações: ajustada finalização imediata no `POST /transactions` com `recurrence` para também cobrir `endType=until_date` quando a ocorrência materializada no submit já atinge a data final (`endDate <= date`), evitando regra permanecer `active` de forma transitória.
- 05/04/2026: Recorrência/Utilitário: `core/utils/recurrence-schedule.utils.ts` passou a validar estritamente datas ISO antes de parsear (`parseIsoDate`), evitando normalização silenciosa de dados inválidos legados durante cálculo de agenda.
- 05/04/2026: Recorrência/Contrato: validação de datas no módulo `recurrences` foi endurecida para calendário real (`YYYY-MM-DD` válido) em `startDate`, `endDate` e `untilDate` (materialize), padronizando com o mesmo util compartilhado já usado em transações.
- 05/04/2026: Recorrência/Materialização: corrigido comportamento de retry após falha (`REC-S2-02`) removendo persistência de ocorrência `failed` na mesma chave de idempotência (que bloqueava reprocessamento futuro); falhas agora ficam em log estruturado e podem ser reprocessadas na próxima execução.
- 05/04/2026: Recorrência/Materialização: `cursorDate` passou a ser normalizado para a primeira data válida da regra em cada execução, evitando geração em dia desalinhado em dados legados com `nextOccurrenceDate` inconsistente.
- 05/04/2026: Transações/Contrato: `PUT /transactions/:id` passou a validar data no mesmo padrão estrito de criação (`YYYY-MM-DD` válido), removendo inconsistência de parse livre por timezone/hora.
- 05/04/2026: Recorrência/Transações: validação de datas da recorrência embutida no `POST /transactions` foi endurecida para calendário real (`YYYY-MM-DD` válido) em `recurrence.startDate` e `recurrence.endDate`, evitando normalização silenciosa de datas inválidas.
- 05/04/2026: Recorrência/Arquitetura: lógica de cálculo de agenda (primeira ocorrência e próxima ocorrência) foi extraída para util compartilhado `core/utils/recurrence-schedule.utils.ts` e reaproveitada por `TransactionService` e `RecurrenceService`, removendo duplicação e reduzindo risco de divergência entre criação e materialização.
- 04/04/2026: Recorrência/Transações (`REC-S2-03`): correções de consistência no `POST /transactions` com `recurrence`: data da transação passou a validar estritamente `YYYY-MM-DD` válido; criação com recorrência agora registra ocorrência inicial em `recurrence_occurrences` quando materializada no submit; cálculo de `nextOccurrenceDate` foi ajustado para evitar duplicidade quando `startDate` vem no passado; recorrências com `endType=by_occurrences` e limite `1` são finalizadas imediatamente quando a 1ª ocorrência já foi materializada no submit.
- 04/04/2026: Sprint B (Recorrência / REC-S2-03): `POST /transactions` passou a aceitar bloco opcional `recurrence`, criando a regra de recorrência de transação no mesmo submit e na mesma transação de banco; incluído ajuste de agenda para evitar duplicar a 1ª ocorrência quando a data do lançamento já coincide com a primeira data da regra. Resposta de criação agora retorna `recurrenceId` (quando aplicável).
- 04/04/2026: Recorrências/Materialização: correções de robustez no motor (`REC-S2-02`): cálculo de agenda agora respeita `dayOfWeek/dayOfMonth/monthOfYear` já na primeira ocorrência; `nextOccurrenceDate` é recalculado em updates de agendamento; atualização de estado da recorrência em materialização passou a usar controle otimista por `version`; cálculo de data corrente por timezone passou a usar PostgreSQL (`now() at time zone`) em vez de `Intl`; falhas de materialização agora geram log estruturado e registro de ocorrência com status `failed`.
- 04/04/2026: Sprint B (Recorrência / REC-S2-02): implementado motor manual de materialização com endpoint `POST /recurrences/materialize` (por usuário), geração real de ocorrências com idempotência por chave única (`recurrence_id + occurrence_date + origin_type`), suporte a recorrências de transação e transferência (transferência atômica em dupla entrada), avanço de `nextOccurrenceDate`, atualização de `lastMaterializedDate/At`, finalização automática por limite (`endType`) e resumo de execução (`created/skipped/failed/finalized`).
- 03/04/2026: Documentação/Processo: `PE_TEMPLATE_melhoria.md` atualizado com padrão consolidado de planejamento: nova legenda de status (`0..5` com `pronto para execução` e `pronto para deploy`), seção de prontidão, blocos de definições pendentes/fechadas, conflitos previstos, critérios de engenharia (incluindo regra de idioma), e estrutura de sprints `S1..S4` com IDs rastreáveis.
- 03/04/2026: Planejamento/Recorrência: legenda de status atualizada para `2 em desenvolvimento` e adição da etapa `4 pronto para deploy` (novo fluxo: `0 planejamento`, `1 pronto para execução`, `2 em desenvolvimento`, `3 em teste`, `4 pronto para deploy`, `5 concluído`).
- 03/04/2026: Planejamento/Recorrência: legenda de status refinada no plano para separar `pronto para execução` de `em execução (código)` (`0 planejamento`, `1 pronto`, `2 em execução`, `3 em teste`, `4 concluído`).
- 03/04/2026: Planejamento/Recorrência: plano `PE_recorrencia_lancamentos_mvp.md` movido para estado pronto para execução (`Status [1]`), com seção de validação de prontidão adicionada.
- 03/04/2026: Planejamento/Recorrência: padronizada nomenclatura técnica em inglês no plano (`status: active/finalized`, `scopes: single/this_and_next/all`), mantendo rótulos da UI em pt-BR para aderir ao acordo global de idioma.
- 03/04/2026: Documentação/Processo: formalizada regra global de idioma no projeto: codificação sempre em inglês (identificadores e contratos técnicos) e UI sempre em pt-BR (labels/mensagens/textos ao usuário), registrada em `docs-hidden/notes.md` e reforçada no plano de recorrência.
- 03/04/2026: `db:seed:run` recebeu confirmação extra para modos destrutivos (`empty` e `reset`): agora exige digitação de código dinâmico de 4 caracteres (1 letra + 3 números) antes de executar.
- 03/04/2026: Adicionado wrapper operacional `db:seed:run` com menu/flags para delegar seeds e init em um único ponto (`--mode empty|reset|demo|init|init-demo`), incluindo confirmações interativas padrão.
- 03/04/2026: Adicionado fluxo de inicialização única de banco para servidor novo: `db:init` (`db:migrate` + `db:seed:system`) e `db:init:demo` (inclui `db:seed:demo`), simplificando bootstrap de ambiente zerado.
- 03/04/2026: Nomenclatura de seed: arquivo de reset foi renomeado de `src/db/seed.ts` para `src/db/seed.reset.ts`, com atualização do script `db:seed:reset`, para refletir melhor o comportamento destrutivo de limpeza + recriação.
- 03/04/2026: `seed.system.ts` atualizado para o estado atual do projeto sem perder a essência: mantém foco em garantir a categoria global de transferência, agora com normalização de nome (`Transferencia`/`Transferência`), `description` técnica, validação de `type/system/userId`, atualização idempotente quando já existe e reset opcional com ordem segura de limpeza (incluindo `audit_logs`) e confirmação explícita em produção.
- 03/04/2026: Segurança operacional de seeds: removido script genérico `db:seed` do `package.json` para forçar uso explícito das opções `db:seed:empty`, `db:seed:reset` e `db:seed:demo`.
- 03/04/2026: Seed/API reorganizado em três cenários operacionais: `db:seed:empty` (zera banco), `db:seed:reset` (limpa e recria dados iniciais) e `db:seed:demo` (cria/atualiza apenas usuário demo). `db:seed` agora aponta para `db:seed:reset`. Em produção, `empty` e `reset` exigem confirmação explícita via env (`SEED_CONFIRM=PROD_EMPTY_OK` / `SEED_CONFIRM=PROD_RESET_OK`).
- 03/04/2026: Seed/API ganhou modo seguro para apresentação: criado `src/db/seed.demo.ts` e script `npm run db:seed:demo`, que atualiza apenas o usuário demo (`demo@opafinance.fake`) sem limpar banco global; em `NODE_ENV=production` exige confirmação explícita `DEMO_SEED_CONFIRM=PROD_DEMO_OK`.
- 03/04/2026: Seed/API refinado para período histórico: geração de transações passou de janela fixa anual para intervalo de `2025-01` até o mês atual (sem datas futuras no mês corrente), mantendo consistência dos lançamentos para testes de tendência no consolidado.
- 03/04/2026: Seed/API atualizado para versão atual do schema: `src/db/seed.ts` passou a popular `description` em categorias e subcategorias, adicionou `notes` nas transações de exemplo e trocou o ano fixo para ano dinâmico (`BASE_YEAR`), evitando seed desatualizado ao longo do tempo.
- 01/04/2026: Frontend/Consolidado (Melhoria 18): adicionada seção `Saldo mensal (Receita - Despesa)` com uma linha de saldo por mês (`receitas - despesas`) e total anual, em formato compacto de tabela para leitura rápida de tendência mensal. Células seguem padrão visual existente (`-` centralizado para zero/indefinido; valores positivos em verde e negativos em vermelho).
- 01/04/2026: Frontend/Consolidado (Melhoria 17): adicionada linha `Variação % vs mês anterior` nas tabelas de `Receitas` e `Despesas`, calculada com base no total mensal da seção; janeiro não possui base e exibe `-`, meses com base zero também exibem `-` (ou `0,0%` quando ambos são zero). A coloração segue tendência contextual: em receitas, alta em verde e queda em vermelho; em despesas, alta em vermelho e queda em verde.
- 31/03/2026: Subcategorias/Descrição: adicionado campo `description` (opcional, nullable, até 500) em toda a stack de subcategorias. Backend atualizado com schema Drizzle (`subcategories.description`), migration `0013_subcategory_description.sql`, validação Zod (create/update), persistência no service e auditoria (`beforeData/afterData`). Swagger/docs de categorias/subcategorias atualizados. Frontend atualizado em tipos/payloads, schemas de formulário e modais de criar/editar subcategoria com textarea de descrição; listagem mobile/desktop passa a exibir descrição de subcategoria de forma discreta quando existir. Validação executada com `npm run lint`/`npm run build` (API e Front) e suíte de testes (`subcategories` + `audit-write-integration`) passando.
- 31/03/2026: Frontend/Categorias (Melhoria 16 - Sprint B): campo `description` integrado no front de categorias (tipos/payloads API, schema Zod de criar/editar com limite de 500 chars, modais de criar/editar com textarea, envio normalizado para `null` quando vazio e exibição discreta da descrição em mobile/desktop). Validação executada com `npm run lint` e `npm run build` no `opa-finance-front`.
- 31/03/2026: Docs/API: atualizada documentação de categorias com novo campo `description` em `POST/GET/PUT /categories` e revisão do módulo `modules_categories.md` com regras e contrato do campo opcional.
- 31/03/2026: Backend/Categorias (Melhoria 16 - Sprint A): adicionado campo `description` em categorias (Drizzle schema + migration `0012_wise_category_description.sql`), atualizado contrato de categorias (Zod + Swagger em create/update/list/get), persistencia no service (create/update), auditoria com `description` em `toAuditCategory` e testes de categoria/auditoria ajustados para cobrir o novo campo.
- 31/03/2026: Documentacao/Processo: formalizado acordo de manter `docs-hidden/changes.md` sempre atualizado a cada alteracao relevante; organizado fluxo de melhorias em `docs-hidden/melhorias/` com template padrao `PE_TEMPLATE_melhoria.md`, itens em andamento na raiz e concluídos em `done/`.
- 31/03/2026: Scripts/DB: corrigida a expansão de variável no `db-sync-prod-to-dev-ssh.sh` durante restore via dump temporário (`TMP_DUMP`), evitando erro `cannot create : Directory nonexistent`.
- 25/03/2026: Docs/Insomnia: alinhados identificadores e nomes das requests de relatórios para `consolidated` (`req_reports_consolidated` e `req_reports_consolidated_years`), removendo resquícios de `trial balance`.
- 22/03/2026: Refactor/Nomenclatura: limpeza fina do módulo `Consolidado` para reduzir ruído técnico, removendo resquícios de `trialBalance` em schemas/services/routes/backend, setup de testes e hooks/componentes do frontend.
- 22/03/2026: Scripts/DB: `db-sync-prod-to-dev-ssh.sh` ficou mais seguro contra falhas silenciosas em pipeline (`set -o pipefail`) e o restore passou a usar dump temporário no container antes do `psql`.
- 22/03/2026: Frontend/Transações: ajustado warning de hooks no cálculo de conta principal, memorizando a lista de contas (`useMemo`) para estabilizar dependências.
- 22/03/2026: Frontend/Transações: no modal de nova transferência, conta de origem passa a iniciar com a conta principal e a conta de destino com a próxima conta da lista, acelerando transferências recorrentes.
- 22/03/2026: Frontend/Transações: paginação padrão da listagem alterada de `10` para `30` itens por página (incluindo fallback de preferência inválida).
- 22/03/2026: Frontend/Transações: seção avançada de filtros agora permanece oculta por padrão (sem autoexpansão quando há filtros ativos), abrindo somente por ação manual do usuário.
- 22/03/2026: Frontend/Transações: filtro de conta agora inicia preselecionado com a conta principal ao entrar na tela (aplicado uma única vez por abertura da página, sem forçar após mudanças manuais).
- 22/03/2026: Frontend/Semanas: na modal `Configurar colunas dinâmicas`, adicionada ação direta `Remover` em cada coluna selecionada para agilizar a gestão das colunas exibidas.
- 19/03/2026: Frontend/Semanas: adicionada alternância explícita para ordenação dinâmica por percentual (ligado por padrão) ou por valor, com persistência da preferência no estado local da visão.
- 19/03/2026: Frontend/Navegação: tela de `Fluxo Semanal` foi renomeada para `Semanas` (navbar, título desktop/mobile e mensagem de estado vazio), mantendo rota técnica `/app/weekly-cashflow`.
- 19/03/2026: Frontend/Relatórios: tela de `Balancete` foi renomeada para `Consolidado` na navegação e nos títulos; rota da aplicação alterada de `/app/trial-balance` para `/app/consolidated`.
- 19/03/2026: Backend/Relatórios: endpoints de relatório foram renomeados de `/reports/trial-balance` e `/reports/trial-balance/years` para `/reports/consolidated` e `/reports/consolidated/years`, com atualização de chamadas no frontend, docs de API e coleção Insomnia.
- 19/03/2026: Backend/Relatórios: módulo de arquivos do relatório foi renomeado de `trial-balance.*` para `consolidated.*` (routes, service, schemas e testes), mantendo compatibilidade funcional.
- 19/03/2026: Balancete/Consolidado: corrigido cálculo/exibição de lançamentos sem subcategoria; agora os valores entram também em uma linha dedicada (`Sem subcategoria`) para fechar com o total da categoria.
- 19/03/2026: Frontend/Consolidado: linha de “sem subcategoria” passou a exibir o nome da categoria pai com marcador visual `*` para facilitar identificação.
- 19/03/2026: Scripts/DB: `db-sync-prod-to-dev-ssh.sh` passou a limpar schema `public`/`drizzle` antes do restore (evitando falha de dependência em constraints), exibir sugestão pós-sync e oferecer execução opcional de `npm run db:migrate`.
- 19/03/2026: Scripts/Versionamento: corrigido cálculo de release no `promote-dev-to-main.sh` (branch `dev` deixava de chegar ao Node), adicionado push opcional automático para `origin` e remoção da branch `dev` após promoção (local e remota quando aplicável).
- 19/03/2026: Deploy/Docker: ajustado build para ambientes de CI/Portainer com `npm ci --include=dev` nos Dockerfiles de frontend/backend e correção de contexto no `.dockerignore` do backend para incluir scripts de geração de versão.

- 11/03/2026: Frontend/Auditoria (Sprint 3 inicial): criada rota desktop `/app/audit` com integração ao endpoint `GET /audit-logs`, tipos e hook dedicados, filtros (entidade/acao/periodo), paginação, estados de loading/erro/vazio e entrada de navegação no sidebar (`Histórico`), com fallback de degradacao para mobile.
- 10/03/2026: Backend/Auditoria: reforcada consistencia em operacoes de `create` com gravacao de log no mesmo contexto transacional para `transactions`, `accounts`, `categories` e `subcategories`.
- 10/03/2026: Backend/Schema: realinhado contrato de categorias com o campo `color` no schema Drizzle (`categories.color`) e adicionada migration idempotente `0010_lame_loki.sql` para garantir compatibilidade entre ambiente e service/API.
- 10/03/2026: Backend/Arquitetura: schema do Drizzle foi reorganizado para `src/db/schemas/*.schema.ts` por dominio (`users`, `accounts`, `categories`, `subcategories`, `transactions`, `audit` e `enums`), mantendo compatibilidade via `src/db/schema.ts` como barrel de reexport.
- 10/03/2026: Backend/Testes: concluida cobertura dos itens `AUD-S2-10`, `AUD-S2-11` e `AUD-S2-12` no endpoint `GET /audit-logs` com cenarios de bloqueio por usuario, combinacoes de filtros, pagina vazia e validacao de limites extremos de paginacao.
- 10/03/2026: Backend/Testes: ampliada cobertura de integracao da auditoria para operacoes criticas de `accounts`, `transactions`, `categories` e `subcategories`; suite dedicada de auditoria executada com sucesso (`10/10`).
- 10/03/2026: Backend/Auditoria: definida e implementada lista central de campos sensiveis proibidos no log com mascaramento automatico (`[REDACTED]`) em `beforeData`, `afterData` e `metadata` no `AuditService`.
- 10/03/2026: Backend/Testes: adicionados testes do modulo de auditoria em `test/audit`, cobrindo listagem de `GET /audit-logs` (autenticacao, filtro, paginacao e validacao de datas) e integracao de escrita para logs de conta (`create/update/delete`).
- 10/03/2026: Backend: auditoria evoluiu para o escopo completo de escrita no MVP, com logs de `create/update/delete` adicionados em `accounts`, `categories` e `subcategories`, mantendo consistencia transacional nas operacoes de alteracao/remocao.
- 10/03/2026: Backend: adicionado endpoint autenticado `GET /audit-logs` com paginacao e filtros (`entityType`, `action`, `startDate`, `endDate`) e documentacao atualizada no modulo `Audit`.
- 09/03/2026: Documentacao: criado `opa-finance-api/docs/modules/modules_audit.md` no mesmo padrao dos demais modulos, e atualizados os READMEs da raiz e da API para incluir o novo modulo `Audit`.
- 09/03/2026: Backend: iniciada a base da auditoria (MVP) com tabela `audit_logs` (enums de acao/entidade, snapshots/migrations `0008` e `0009`, indices por usuario/entidade/acao + `created_at`), novo `AuditService` e integracao inicial no modulo de transacoes para registrar eventos de `create`, `update` e `delete` (incluindo cenarios de transferencia).
- 08/03/2026: Frontend: modal de nova transacao passou a ter busca de subcategorias dentro do dropdown, com campo de filtro no topo e foco automatico ao abrir, alinhado ao comportamento da busca de categorias.
- 08/03/2026: Frontend: modal de nova transacao passou a ter busca de categorias dentro do dropdown, com campo de filtro no topo e foco automatico ao abrir, facilitando selecao em listas grandes.
- 08/03/2026: Frontend: campo de valor (criar/editar transacao e transferencia) passou a aceitar modo expressao com `=`, incluindo parenteses e precedencia (`+`, `-`, `*`, `/`); ao pressionar `=`, o valor atual e limpo e o campo entra no modo expressao, bloqueando letras e aceitando apenas numeros/espacos/simbolos matematicos.
- 08/03/2026: Frontend: no modal de nova transacao, o campo de valor passou a usar `inputMode=\"decimal\"`, facilitando digitacao com separador decimal e mantendo suporte a `.` e `,`.
- 08/03/2026: Versionamento: `scripts/promote-dev-to-main.sh` passou a calcular a release com base explicita na branch `dev` (em vez de `HEAD`) e a validar existencia de tag tambem no remoto `origin`, evitando versao incorreta e falha tardia no push.
- 08/03/2026: Versionamento: `scripts/start-dev-cycle.sh` e `scripts/promote-dev-to-main.sh` passaram a validar sincronizacao local vs `origin` antes de operar em `main/dev`, reduzindo risco de release/tag em base desatualizada.
- 04/03/2026: Versionamento: `src/generated/app-version.ts` de frontend e backend passaram a ser tratados como artefatos gerados locais, ignorados no Git; as pastas `src/generated/` agora mantem apenas `.gitkeep`.
- 02/03/2026: Versionamento: `scripts/promote-dev-to-main.sh` recebeu validacoes extras de seguranca e passou a criar a tag apenas depois de regenerar os arquivos de versao e, se necessario, criar um commit local de release.
- 02/03/2026: Versionamento: `scripts/version-cycle.sh` ganhou opcao de consulta para mostrar a versao calculada atual de frontend e backend sem alterar branches ou tags.
- 02/03/2026: Versionamento: criado `scripts/version-cycle.sh` com menu interativo no estilo operacional dos scripts de banco, consolidando em um unico ponto o inicio de ciclo em `dev` e a promocao de `dev` para `main`.
- 02/03/2026: Versionamento: adicionado suporte a ciclo de release por branch com scripts `scripts/start-dev-cycle.sh` e `scripts/promote-dev-to-main.sh`; o `PATCH` agora passa a contar commits desde o inicio do ciclo atual via `versioning.cycleStartCommitCount` nos `package.json`.
- 02/03/2026: Organizacao da documentacao raiz: a pasta `docs/` foi reservada para documentacao oficial, foi criado o documento `docs/versionamento.md` com o contrato de versao do projeto e o conteudo historico/operacional anterior da raiz foi movido para `docs-hidden/`.
- 02/03/2026: Frontend: modal de criar categoria ganhou atalho de ação principal no botão `Criar categoria`, com `Ctrl/Cmd+Enter` disparando o mesmo submit do formulário e tooltip discreto no padrão dos demais modais com input.
- 02/03/2026: Frontend: modal de criar subcategoria passou a seguir o mesmo padrão do modal de categoria, com `Ctrl/Cmd+Enter` no botão `Criar subcategoria` e tooltip discreto para a ação principal.
- 02/03/2026: Frontend: modais de editar categoria e editar subcategoria passaram a exibir tooltip no botão principal e aceitar `Ctrl/Cmd+Enter` para salvar, alinhando o comportamento com os demais modais com input.
- 02/03/2026: Frontend: tela de categorias passou a expandir e recolher subcategorias ao clique na linha/card da categoria, em desktop e mobile, mantendo os botões de ação isolados para evitar disparos acidentais.
- 02/03/2026: Frontend: geração de versão passou a aceitar override por `APP_VERSION`, agora roda também em `predev` e adotou convenção dinâmica curta no formato `major.minor.commitCount-dev` em desenvolvimento e `major.minor.commitCount` em produção.
- 02/03/2026: Backend: geração de versão passou a adotar convenção dinâmica curta no formato `major.minor.commitCount-dev` em desenvolvimento e `major.minor.commitCount` em produção, mantendo override por `APP_VERSION` quando necessário.
- 02/03/2026: Frontend e Backend: `predev` passou a gerar versão dinâmica de desenvolvimento e `prebuild` passou a gerar versão limpa de produção, sem incluir hash de commit no `APP_VERSION`; o commit segue disponível separadamente em `APP_GIT_COMMIT`.
- 04/04/2026: Recorrências: corrigidos pontos de consistência no módulo inicial: `UPDATE` agora usa controle otimista atômico (`WHERE id + version`), validação de subcategoria passou a considerar categoria atual da regra quando não enviada no payload e validação de período impede `endDate < startDate` (create/update).
- 04/04/2026: Sprint B (Recorrência): criado módulo inicial `recurrences` com rotas protegidas (`POST/GET/PUT/DELETE` + `PUT /:id/finalize`), listagem paginada por filtros, validações de ownership para contas/categorias/subcategorias, bloqueio de exclusão para regra `active` (exclusão lógica para `finalized`) e controle de concorrência otimista via `expectedVersion` com retorno `409`.
- 04/04/2026: Backend: validação de timezone no cadastro/perfil deixou de depender de `Intl` (ICU do Node) e passou a validar via catálogo do PostgreSQL (`pg_timezone_names`), reduzindo risco de falso negativo entre ambientes.
- 04/04/2026: Sprint A (Recorrência): criada fundação de dados com `recurrences` e `recurrence_occurrences`, incluindo enums de frequência/status/término, campos de exclusão lógica/versionamento e índice único de idempotência (`recurrence_id + occurrence_date + origin_type`); adicionada migration `0014_recurrence_foundation.sql`.
- 07/04/2026: Sprint B (Recorrência `REC-S2-09`): implementado job diário seguro de materialização com lock distribuído em banco (`job_locks` + migration `0015_recurrence_job_locks`), retries com backoff configurável, timeout de execução, batch configurável e logs estruturados; bootstrap integrado ao `server.ts` e parâmetros adicionados em `env`/`.env.example`/README.
- 07/04/2026: Recorrências/Job diário: reforçada robustez operacional do `REC-S2-09` com seleção de usuários devidos por `next_occurrence_date <= hoje` (evitando perda por janela exata de minuto), retries alinhados para `maxAttempts` configurável (default 3), drenagem de múltiplos lotes por usuário na mesma execução e tratamento de timeout sem liberação antecipada do lock (evita sobreposição concorrente).
- 07/04/2026: Sprint B (Recorrência `REC-S2-10`): edição/versionamento passou a preservar materializadas com validação baseada na fonte canônica (`recurrence_occurrences`) além de `last_materialized_date`; `update` e `editByScope(this_and_next)` agora usam data materializada efetiva para bloquear retroatividade e recalcular `nextOccurrenceDate` sem risco de sobreposição em cenários de estado stale.
- 08/04/2026: Sprint B (Recorrência `REC-S2-12`): implementada trilha de auditoria de recorrências com `entityType=recurrence` para eventos `create`, `update`, `delete`, `finalize` e `materialize` (via `metadata.operation`), incluindo migração do enum de auditoria (`0016_audit_recurrence_entity.sql`), atualização dos contratos de filtro de auditoria e teste de integração dedicado (`recurrences-audit.test.ts`).
- 09/04/2026: Sprint B (Recorrência `REC-S2-13`): reforçada validação de ownership no módulo de recorrências. `GET /recurrences` agora valida ownership de `accountId` no filtro; `GET /recurrences/:id` valida ownership/coerência dos vínculos da regra (conta/categoria/subcategoria ou contas de transferência); e `POST /recurrences/materialize` passou a validar ownership por recorrência antes de processar, marcando item como falha (`failedRecurrences`) quando houver vínculo inválido sem interromper o lote inteiro. Adicionados testes de integração para filtro por conta de terceiro e cenário de vínculo adulterado na materialização.
- 09/04/2026: Sprint B (Recorrência `REC-S2-15`): reforçada garantia de atomicidade na materialização de transferências recorrentes. A criação das duas transações (`expense` + `income`) agora valida retorno de 2 registros na mesma transação de banco e dispara erro quando a dupla não é persistida integralmente. Incluído teste de integração que força falha de FK no destino para comprovar rollback total (sem `recurrence_occurrences` e sem transações parciais persistidas).
- 09/04/2026: Sprint B (Recorrência `REC-S2-16`): conta agora bloqueia exclusão e ocultação/inativação (`isHiddenOnDashboard=true`) quando houver recorrência ativa vinculada (transação: `accountId`; transferência: `fromAccountId`/`toAccountId`). Implementada checagem central no `AccountService` e adicionados testes de integração para os dois cenários de bloqueio (`DELETE /accounts/:id` e `PUT /accounts/:id`).
- 09/04/2026: Sprint C (Recorrência `REC-S3-06`, `REC-S3-07`, `REC-S3-08`): adicionada alternância `Mostrar/Ocultar projeção` nas telas `Semanas` e `Consolidado`, integração com `GET /recurrences/forecast` para compor visão real+projetado sem materialização no banco, cards/tabela de projeção no Consolidado, destaque visual de semanas com parcela projetada (`proj`) e fallback com aviso quando o forecast falhar.
- 09/04/2026: Sprint C (Recorrência `REC-S3-09`): no formulário de recorrência de transação, reforçada a UX de categoria/subcategoria com exibição do tipo derivado da categoria em campo somente leitura (`Receita`/`Despesa`), além de identificação de tipo no seletor de categorias; mantida remoção de seleção manual de tipo nesse fluxo.
- 09/04/2026: Sprint C (Recorrência `REC-S3-10`): garantido reset automático de `subcategoria` sempre que `categoria` mudar no modal de recorrência (incluindo mudanças por fluxo de edição), evitando vínculo stale entre categorias diferentes.
- 09/04/2026: Sprint C (Recorrência `REC-S3-11`): adicionada opção `Tornar recorrente` no modal de nova transação com estado dedicado e reset no ciclo de abrir/limpar/fechar, preparando o fluxo para configuração completa na sprint seguinte.
- 09/04/2026: Sprint C (Recorrência `REC-S3-12`): ao marcar `Tornar recorrente` no modal de nova transação, foi adicionada seção de configuração de recorrência com pré-preenchimento dos dados base (conta/categoria/subcategoria/valor/descrição/notas), campos de agenda/término e sincronização inicial da data de início com a data da transação.
- 09/04/2026: Sprint C (Recorrência `REC-S3-12`): fluxo de `Tornar recorrente` na criação de transação passou a efetivamente persistir a recorrência junto da transação, com validação local dos campos de recorrência e rollback best-effort da transação quando a criação da recorrência falha, evitando configuração “apenas visual”.
- 09/04/2026: Sprint C (Recorrência `REC-S3-13`): validada e mantida a criação manual de recorrência na tela dedicada (`/app/recurrences`), com entrada por `Nova recorrência` no topo e CTA em estado vazio, preservando o fluxo manual em paralelo ao atalho via modal de transação.
- 04/04/2026: Backend: domínio de usuário passou a incluir `timezone` (default `Australia/Adelaide`) no schema e na migration; validação IANA aplicada em `POST /auth/register` e `PUT /users/:id`, com exposição de `timezone` nos contratos de retorno.
- 03/04/2026: Backend/Frontend: adicionado onboarding de categorias com `POST /categories/bootstrap-defaults` (idempotente). Na tela de categorias, quando não há categorias do usuário, agora aparece ação de `Inserir categorias básicas` junto ao botão manual de criação; docs de API e import do Insomnia foram atualizados.
- 03/04/2026: Backend: refatorado seed de demo/reset para compartilharem a mesma base em `src/db/seed.shared.ts` (contas, categorias, subcategorias e transações), evitando divergência entre scripts e mantendo evolução sincronizada.
- 03/04/2026: Backend: `seed.demo` e `seed.reset` agora incluem uma categoria de receita extra (`Renda Extra`) com lançamentos mensais adicionais em 2025 para o usuário demo, garantindo visualização explícita de receitas nesse ano em relatórios e consolidado.
- 02/03/2026: Frontend: versão base do `package.json` foi alinhada para `1.0.0`, substituindo o placeholder `0.0.0` agora que o app já está em produção.
- 02/03/2026: Frontend: menu de criação da tela de categorias recebeu o mesmo padrão visual de atalhos da tela de transações, com tooltip discreto e letra destacada em `Categoria (C)` e `Subcategoria (S)`.
- 02/03/2026: Frontend: tela de contas passou a seguir a convenção de atalhos por contexto. No modal de detalhe da conta, `O` alterna visibilidade no dashboard, `R` abre exclusão e `E` abre edição; nos modais com input da tela de contas, a ação principal usa `Ctrl/Cmd+Enter` e os botões de cancelar exibem dica de `Esc`.
- 02/03/2026: Frontend: modal de nova transferência recebeu atalho de ação para o botão principal, com `Ctrl/Cmd+Enter` para `Transferir` ou `Salvar`, seguindo o mesmo padrão do modal de nova transação.
- 02/03/2026: Frontend: padrão de tooltip de atalho foi extraído para componente reutilizável e aplicado nos modais principais de `transactions`, `accounts` e `categories`, especialmente nos botões de `Cancelar` com dica de `Esc`, além dos atalhos já existentes na tela de transações.
- 02/03/2026: Frontend: no modal de nova transação, `Salvar` e `Cancelar` voltaram ao texto normal sem sublinhado, mantendo a explicação de atalhos via tooltip. Foi adicionado atalho para `Limpar` com `Ctrl/Cmd+Shift+L`.
- 02/03/2026: Frontend: botões com atalhos na tela de transações passaram a exibir tooltip discreto no hover/focus para explicar as teclas, reduzindo ruído visual permanente na UI.
- 02/03/2026: Frontend: atalhos da tela de transações receberam indicação visual mais sutil por letra sublinhada. No detalhe da transação comum, `D` duplica, `E` edita e `R` exclui; no modal de nova transação, o padrão final ficou em `Ctrl/Cmd+Enter` para salvar e `Esc` para cancelar, removendo os atalhos com `Alt` por conflito com o sistema.
- 01/03/2026: Frontend: tela de transações ganhou atalhos de ação nos modais, mantendo o fluxo atual da UI: `Alt+S` e `Ctrl/Cmd+Enter` para salvar nova transação, `Alt+C` para cancelar o modal de criação e `D` para duplicar transação a partir do modal de detalhes. Os botões passaram a sinalizar esses atalhos visualmente.
- 01/03/2026: Backend: revisão operacional de banco para `prod/dev/test`; `db-reset-ssh.sh` passou a aceitar alvo direto (`prod|dev|test`), a confirmação extra de produção deixou de disparar por substring em reset de teste, e o `package.json` ganhou atalhos explícitos `db:reset:prod`, `db:reset:dev` e `db:reset:test`. README e guia de fluxo de dev foram atualizados para refletir PostgreSQL em `dev/test` e o uso do túnel antes das migrations de teste.
- 01/03/2026: Backend: mensagens de validação Fastify/AJV para campos obrigatórios passaram a ser traduzidas para rótulos amigáveis no error handler; resposta de `POST /transfers` ganhou schema explícito para preservar campos internos como `id`, `accountId`, `transferId` e `updatedAt`.
- 01/03/2026: Backend: consolidado fluxo de backup/restore em `npm run db:archive`, com menu interativo via SSH/Docker, seleção de banco, validações e confirmações extras antes de restaurar produção.
- 01/03/2026: Backend: ajustadas migrations antigas de `transactions.notes` e `categories.color` para serem idempotentes, evitando falha ao recriar o banco de teste em histórico com arquivos duplicados.
- 01/03/2026: Backend: error handler passou a normalizar validações do Fastify/AJV para `400` e priorizar `401` em rotas protegidas sem bearer token; schema de resposta de `/auth/check-password-strength` foi alinhado para retornar `strength` como texto.
- 01/03/2026: Frontend: tela de transações passou a sincronizar `page`, `limit`, `sort` e `dir` com a URL de forma canônica; filtros digitados (`description`/`amount`) agora são gravados imediatamente na query string, mantendo o estado ao recarregar a página sem perder o debounce da busca.
- 22/02/2026: Frontend passou a exibir versões em linha única no sidebar (`vfront / vapi`) consumindo `GET /version`.
- 22/02/2026: Implementada ocultação de contas no dashboard com persistência no backend (`is_hidden_on_dashboard`), com toggle no detalhe da conta e badge "Oculta no dashboard".
- 22/02/2026: Dashboard atualizado para ignorar contas ocultas em seletor/lista/total e métricas (resumo, últimas transações, top categorias).
- 22/02/2026: Frontend agora gera versão automática antes do build/release (`npm run prebuild`) via script `version:generate` e exibe `v{version} ({commit})` no rodapé da sidebar (desktop/mobile).
- 22/02/2026: Backend agora gera versão automática antes de `dev` e `build` (`predev`/`prebuild`) via script `version:generate` e expõe `GET /version` com `version`, `commit` e `buildTime`.
- 18/01/2026: Ajuste no frontend para garantir pré-seleção da conta no modal de nova transação (fallback para primeira conta quando não há conta principal).
- 18/01/2026: Atalhos N/T na tela de transações ignorados quando há qualquer modal aberto.
- 18/01/2026: Limpeza do erro root ao reabrir o modal de nova transação.
- 18/01/2026: Modal de nova transação volta para conta principal ao abrir (exceto duplicação).
- 18/01/2026: Garantia de pre-seleção da conta principal ao abrir nova transação.
- 18/01/2026: Confirmação de conta principal pré-selecionada e auto-sugestão reativada no modal de nova transação.
- 18/01/2026: Nova transferência inicia com a conta principal como origem quando o campo está vazio.
- 18/01/2026: Adicionado botão para definir conta principal na edição de conta.
- 18/01/2026: Adicionado modal de confirmação ao definir conta principal.
- 18/01/2026: Botão de definir conta principal movido para o final do modal de edição.
- 18/01/2026: Redeploy do front solicitado via Portainer apontando para a branch main.
- 18/01/2026: Atualizado title/description do front e adicionado novo favicon.
- 18/01/2026: Foco inicial no select de categoria ao abrir modal de criar subcategoria.
- 18/01/2026: Subcategoria agora preseleciona a ultima categoria criada.
- 18/01/2026: Subcategoria agora preseleciona a categoria da ultima subcategoria criada.
- 18/01/2026: Modal de criar categoria preseleciona o tipo da ultima categoria criada.
- 18/01/2026: Teste local conectado ao Postgres remoto via tunnel SSH.
- 18/01/2026: Logout agora limpa o cache do React Query para evitar dados do usuario anterior.
- 18/01/2026: Badge "Principal" exibido na lista de contas.
- 18/01/2026: Badge "Principal" adicionado tambem na tabela de contas.
- 18/01/2026: Badge "Principal" exibido no modal de detalhes da conta.
- 18/01/2026: Ultima categoria de subcategoria persistida via localStorage por usuario.
- 18/01/2026: Criado hook useUserPreference para padronizar preferencias do usuario.
- 18/01/2026: Preferencia de ocultar valores agora usa useUserPreference.
- 18/01/2026: Criado hook useAppPreference e tema passou a usar essa preferencia global.
- 18/01/2026: Limite de paginação em transações persistido por usuário.
- 18/01/2026: Limite de paginação em contas persistido por usuário.
- 18/01/2026: Preseleção de categoria/subcategoria agora persiste o ultimo tipo/categoria usada.
- 18/01/2026: Subcategorias agora ordenadas alfabeticamente na tela de categorias.
- 18/01/2026: Modal de transação permite criar categoria/subcategoria sem sair da tela.

## 2026-01-24

- Backend: em dev, JWT_SECRET e REFRESH_TOKEN_SECRET foram alinhados para evitar falha de refresh token.
- Frontend: modal de subcategoria na transação ajustado para evitar loop de render e incluir categoryId no schema.
- Frontend: criação de subcategoria agora invalida caches do select na transação.
- Frontend: tecla Esc fecha os modais de nova categoria/subcategoria dentro da transação.
- Frontend: typeahead por letra no dropdown de categoria e subcategoria da transação.
- Frontend: seleção automática de categoria/subcategoria recém-criadas no modal de transações.
- Frontend: categorias agora ordenadas por tipo (Receita/Despesa) e nome; add expandir/recolher tudo (desktop/mobile); delete em cascata com confirmação e status.
- Frontend: modal de transação agora limpa o formulário ao fechar e adiciona botão "Limpar".
- Frontend: foco automático em modais de categoria/subcategoria e retorno do foco ao select; ajuste do ref do select de tipo.
- 12/04/2026: Frontend/Transactions (Refatoração piloto R4 parcial): extraído estado e comportamento de seleção/bulk para `use-transactions-selection` (co-location na feature), com redução de acoplamento na rota de transações; paginação manteve feedback de loading sem reset para página 1 e com dados anteriores preservados durante fetch.
- 12/04/2026: Frontend/Transactions (Refatoração piloto R4): extraído `use-transactions-pagination` para centralizar handlers de paginação/limite e removido estado duplicado de seleção da rota, consolidando seleção/bulk/paginação em hooks da feature sem alteração de comportamento visual/funcional.
- 12/04/2026: Frontend/Transactions (Refatoração piloto R5 parcial): mapeamento de payload de create/edit movido para mapper da feature (`buildTransactionCreatePayloadFromForm`/`buildTransactionUpdatePayloadFromForm`) e submit de edição removido do JSX inline para handler dedicado, mantendo comportamento e contratos.
- 12/04/2026: Frontend/Transactions (Refatoração piloto R5): criado `use-transaction-form` para centralizar handlers de valor (modo expressão) e orquestração de submit create/edit (incluindo rollback best-effort quando recorrência falha), preservando validações Zod/RHF e removendo lógica longa da rota.
- 12/04/2026: Frontend/Transactions (Testes - etapa 1): definido e documentado o escopo inicial dos testes automatizados em `PE_transactions_front_tests.md` com stack `vitest + testing-library + jest-dom + msw`, incluindo fronteiras de MVP (unit + hooks + integração mínima da rota) e itens fora de escopo.
- 12/04/2026: Frontend/Transactions (Testes - etapa 2): instaladas dependências de teste (`vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `msw`, `jsdom`) e adicionados scripts npm `test`, `test:unit` e `test:integration` no frontend, com validação inicial dos comandos sem arquivos de teste.
- 12/04/2026: Frontend/Transactions (Testes - etapa 3): configurado Vitest com `jsdom` no `vite.config.ts`, criado setup global com `jest-dom` e lifecycle do `msw` (`test/setup`), além do util `renderWithProviders` e estrutura inicial `test/unit` + `test/integration`.
- 12/04/2026: Frontend/Transactions (Testes - infraestrutura MSW): adicionados utilitários reutilizáveis em `test/setup/msw.utils.ts` (respostas padrão, paginação e parse de query), `barrel` em `test/setup/msw.ts` e handlers base ajustados para usar helpers, padronizando a base para os próximos testes de integração.
- 12/04/2026: Frontend/Transactions (Testes - render util): `test/setup/render.tsx` evoluído para providers reais do app (`ThemeProvider` + `QueryClientProvider`) e adicionado helper de rota (`renderRouteWithProviders`) com TanStack Router em memória (`createMemoryHistory`) para testes de fluxo com URL.
- 12/04/2026: Frontend/Transactions (Testes - etapa 4): adicionados primeiros testes unitários de baixo risco para helpers e mappers (`transactions.helpers` e `transaction-payload.mapper`), cobrindo parse de valor/paginação/data-texto e construção/validação de payloads de transação/recorrência.
- 12/04/2026: Frontend/Transactions (Testes - etapa 5): adicionados testes de hooks com `renderHook` para `use-transactions-selection`, `use-transactions-pagination` e `use-transactions-filters`, cobrindo seleção/bulk, navegação de páginas/limite e sincronização canônica de search params.
- 12/04/2026: Frontend/Transactions (Testes - etapa 6): criado teste de integração de listagem em `/app/transactions` com `msw` (`transactions-list.component.test.tsx`), validando render da tabela com dados mockados.
- 12/04/2026: Frontend/Transactions (Testes - etapa 7): criado primeiro teste de fluxo `create/edit` com `msw` (`transactions-create-edit.flow.test.tsx`), validando `POST`/`PUT` de transações e atualização da listagem; adicionado polyfill global de `scrollIntoView` no setup de testes para compatibilidade com Radix Select em `jsdom`.
- 12/04/2026: Frontend/Transactions (Testes - estabilização): reduzido uso frágil de `waitFor` nos testes de integração, adotando padrão mais determinístico com `findBy*`/`findAllBy*` e `waitForElementToBeRemoved` para fechamento de modal, mantendo a suíte estável em reexecuções consecutivas.
- 12/04/2026: Frontend/Testes: definida convenção oficial de estrutura e nomenclatura (`test/setup`, `test/unit`, `test/integration`) em `opa-finance-front/test/README.md`; testes de integração de `transactions` renomeados para o padrão `<feature>.<scenario>.integration.test.tsx`.
- 12/04/2026: Frontend/Testes: expandido guia de testes em `opa-finance-front/test/README.md` com comandos de execução (total/unit/integration/arquivo isolado), padrão de escrita (unit/integration), regras de sincronização anti-flakiness e checklist mínimo pré-PR; `opa-finance-front/README.md` atualizado com scripts e link direto para o guia.
- 12/04/2026: Frontend/Testes: checklist mínimo pré-PR atualizado para incluir `npm run build` como gate obrigatório junto de lint e testes.
- 13/04/2026: Frontend/Transactions (Refatoração piloto R6): extraída orquestração de transferência para `use-transfer-form` (create/edit/repetir, submit, shortcut `Ctrl/Cmd+Enter`, reset/close e busca de contraparte) e orquestração de recorrência para `use-transaction-recurrence-draft`; mapeamento de payload de transferência movido para `transfer-payload.mapper` e tipagens de recorrência/transferência centralizadas em `transactions.types`, reduzindo handlers longos na rota sem mudança de contrato. Adicionado teste unitário para mapper de transferência (`transfer-payload.mapper.test.ts`).
- 13/04/2026: Frontend/Transactions (Refatoração piloto R7 parcial): extraídos `transactions-toolbar`, `transactions-table-desktop` e `transactions-transfer-modal` para `features/transactions/components`, com substituição dos blocos TSX correspondentes na rota por componentes presentational com props explícitas, preservando comportamento e contratos.
- 13/04/2026: Frontend/Transactions (Refatoração piloto R7 parcial): extraído `transactions-details-modal` para `features/transactions/components` e substituído bloco TSX de detalhes na rota por composição com props explícitas (sem mudança de comportamento), mantendo atalhos D/E/R e ações de transferência/duplicação/edição/exclusão.
- 13/04/2026: Frontend/Transactions (Refatoração piloto R7): extraído `transactions-edit-modal` (conteúdo completo) e `transactions-create-modal` (casca de modal) para `features/transactions/components`, com rota migrada para composição por props explícitas e manutenção dos fluxos/atalhos existentes.
- 13/04/2026: Frontend/Transactions (Testes de componente R7): adicionada cobertura unitária para os componentes extraídos no R7 (`transactions-toolbar`, `transactions-table-desktop`, `transactions-create-modal`, `transactions-details-modal`, `transactions-transfer-modal`, `transactions-edit-modal`), validando renderização e callbacks principais.
- 13/04/2026: Frontend/Transactions (Fechamento de gaps R0-R6): adicionados testes unitários para `use-transactions-search-params`, `use-transaction-form`, `use-transfer-form` e `use-transaction-recurrence-draft`, além de teste de integração do fluxo de transferência (`create/edit`) com MSW para cobrir cenários críticos das sprints R3, R5 e R6.
- 13/04/2026: Frontend/Transactions (Refatoração piloto R8): criado `transactions-page` em `features/transactions/components` e rota `routes/app/transactions.tsx` reduzida para entrypoint fino (`validateSearch` + bridge de `search`/`navigate`), consolidando o boundary final rota-feature sem alteração funcional.
- 13/04/2026: Frontend/Transactions (Refatoração piloto R9 parcial): hardening técnico concluído com suíte consolidada (`test:unit`, `test:integration`, `lint`, `build`) e documentação/rastreabilidade final atualizadas; status da melhoria movido para `3` (teste/observação) aguardando apenas checklist manual final.
- 17/04/2026: Frontend/Recurrences (Refatoração R0-R9): rota `/app/recurrences` foi convertida para entrypoint fino e a tela migrou para `features/recurrences/components/recurrences-page.tsx`; extraídos `model` (`recurrences.helpers.ts`, `recurrences.constants.ts`, `recurrences.types.ts`) e hook de search params (`use-recurrences-search-params`) para reduzir acoplamento da rota. Adicionada cobertura inicial da feature com testes unitários (`recurrences.helpers`, `use-recurrences-search-params`) e integração (`recurrences.integration.test.tsx`) cobrindo listagem, URL filters/paginação e modal de criação com fechamento via `Escape`.
- 19/04/2026: Backend/Transactions: refatoração incremental no `TransactionService` com extração de helpers de filtros (`buildBaseFilters`, `applyAmountFilters`, `applyTextFilters`) e snapshots de auditoria (`resolveAuditSnapshot`, `resolveAuditSnapshots`), reduzindo duplicação entre `list/summary/topCategories/update/delete` sem alterar contratos de API, ownership, paginação ou comportamento funcional.
- 19/04/2026: Frontend: melhorias incrementais de manutenibilidade sem mudança funcional/visual nos fluxos de Transações/Contas/Auth. Em `transactions-page`, cálculos puros de opções de categoria/subcategoria, sugestões de descrição e conta destino padrão de transferência foram extraídos para `transactions-page.helpers`; em `accounts-page`, classes/total exibido foram centralizados em helpers e o ícone de ordenação virou componente dedicado; em `login/forgot-password/reset-password`, o tratamento de erro de formulário/API foi padronizado via `setApiRootFormError`. Incluída cobertura unitária para os novos helpers.
- 19/04/2026: Frontend/Transactions: refatoração incremental adicional em `transactions-page` com extração da orquestração de suporte dos modais de criação/edição para o hook `use-transactions-create-support` (subcategorias por categoria, árvore categoria/subcategoria e sugestões de descrição), além de mover regra visual de tom de valores para helper puro (`getTransactionAmountToneClass`). Comportamento/UX preservados e cobertura unitária expandida.
- 19/04/2026: Frontend/Accounts: `accounts-page` teve a ação de exclusão extraída para `use-accounts-delete-action`, removendo bloco assíncrono inline do JSX e padronizando feedback de erro via `accounts-errors.helpers` (`resolveAccountDeleteErrorFeedback` + `getApiErrorStatus` compartilhado). Também foi removida duplicação de parser de status em `use-accounts-linked-actions`. Cobertura unitária adicionada para hook e helper.
- 19/04/2026: Frontend/Auth: `login`, `forgot-password` e `reset-password` passaram a compartilhar o mesmo fluxo de submit com erro root de API e estado de pending via helpers de `auth-form.helpers` (`submitWithApiRootError` + `isAuthFormPending`), reduzindo duplicação e padronizando loading/erro dos formulários sem alterar UX.
- 19/04/2026: Frontend (Accounts/Auth/Transactions): consolidado tratamento de erro de API em helpers reutilizáveis com baixo acoplamento. `getApiErrorStatus` foi centralizado em `lib/apiError`, novo helper `lib/form-api-error` padronizou `setError('root')`/campo com mensagens de API, e pontos duplicados em `use-accounts-form-actions`, `use-transfer-form`, `transactions-page` (modais de criar categoria/subcategoria) e `auth-form.helpers` passaram a reutilizar os mesmos utilitários. Comportamento e mensagens mantidos; cobertura unitária adicionada para `apiError` e `form-api-error`.
- 21/04/2026: Frontend/Transactions (Refatoração de responsabilidade): extraídos fluxos async de exclusão individual/lote para `use-transactions-delete-actions` e criação inline de categoria/subcategoria para `use-transactions-inline-category-actions`, removendo handlers async inline do `transactions-page`. Modais de confirmação de exclusão foram componentizados (`transactions-delete-confirm-modal` e `transactions-bulk-delete-modal`) mantendo comportamento/atalhos/mensagens. Cobertura ampliada com testes unitários dos novos hooks/componentes e integração para exclusão individual/lote (incluindo falha parcial) e criação inline de categoria/subcategoria.
- 25/04/2026: Frontend/Transactions (Split de modais - estabilização): `transactions-create-modal`, `transactions-edit-modal` e `transactions-transfer-modal` passaram a concentrar sua própria orquestração de formulário/refs/callbacks; `transactions-page-content.tsx` foi reduzido para entrypoint fino e a suíte obrigatória (`lint`, `build`, `test`) ficou verde após compatibilização dos contratos legados usados nos testes de componente.
- 25/04/2026: Frontend/Transactions (reversão de empurrão arquitetural): removido `transactions-page-content-body.tsx`; a implementação voltou a ficar explícita em `transactions-page.tsx` e `transactions-page-content.tsx` permaneceu apenas como alias compatível para permitir replanejamento do split sem esconder o monólito em outro arquivo.
- 25/04/2026: Frontend/Transactions: removido `transactions-page-content.tsx`, que estava sem responsabilidade real e sem usos no código/testes; a feature segue apontando diretamente para `transactions-page.tsx` sem impacto funcional.
- 30/04/2026: Frontend/Recurrences: modal de criação/edição passou a reutilizar campos visuais da tela de Transações para conta, categoria/subcategoria, tipo derivado, valor, descrição e observações, preservando contratos e fluxo de transferências.
- 30/04/2026: Frontend/Recurrences: campo de valor do modal de recorrência passou a finalizar expressões aritméticas no blur, alinhado ao comportamento do modal de nova transação.
- 30/04/2026: Frontend/Transactions: comportamento de máscara e cálculo de expressão do campo de valor foi centralizado em `TransactionAmountField`, removendo handlers duplicados em transações, transferências e recorrências.
- 30/04/2026: Documentação/Recurrences: criado plano de melhoria `PE_recorrencias_modo_revisao_timeline_mvp.md` para registrar o desenho de recorrências automáticas vs revisão obrigatória, pendências, timeline/modal de detalhes e regras de parcelamento.
- 01/05/2026: Documentação/Recurrences: fechadas definições `RCREV-DEF-01` a `RCREV-DEF-05` no plano de recorrências com revisão, cobrindo forecast de pendências, ajustes na confirmação, bloqueio de reabertura de ignoradas, snapshot em `this_and_next` e navegação para lançamentos materializados.
- 01/05/2026: Documentação/Recurrences: marcados como resolvidos os conflitos `RCREV-CNF-01` a `RCREV-CNF-04`, definindo criação aninhada para evitar duplicidade, contagem de ocorrências consumidas, snapshot de pendência e confirmação atômica de transferências.
- 01/05/2026: Documentação/Recurrences: resolvido `RCREV-CNF-05` definindo limite default de 24 e máximo de 120 ocorrências na timeline, com limite obrigatório para recorrências sem fim e indicação visual de janela parcial.
- 01/05/2026: Documentação/Recurrences: revisão pré-execução incorporada ao plano, adicionando pendências `RCREV-DEF-06`, `RCREV-DEF-07`, `RCREV-DEF-08`, `RCREV-DEF-09` e `RCREV-DEF-14` sobre status `failed`, deduplicação de pendências no forecast, separação de `reviewPayload`, metadados da timeline e migration/backfill.
- 01/05/2026: Documentação/Recurrences: fechadas definições `RCREV-DEF-06`, `RCREV-DEF-07`, `RCREV-DEF-08`, `RCREV-DEF-09` e `RCREV-DEF-14`, removendo pendências de decisão sobre `failed`, forecast, `reviewPayload`, metadados de timeline e migration/backfill.
- 06/05/2026: Frontend/Backend Recurrences: corrigido fluxo de término `until_date` para criação e edição de recorrências. O frontend passou a limpar campos incompatíveis ao alternar o tipo de término e a enviar `endDate` apenas quando o término for por data final; o backend passou a tratar `until_date` como série finita na timeline, calculando total/paginação e interrompendo projeções em `endDate`. Adicionados testes de criação, edição e preservação de término.
